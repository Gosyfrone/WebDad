// Route sans overlay : intercepter `/parametres` vers `null` referme l'overlay
// ouvert et laisse la page Paramètres s'afficher (children) au premier plan.
export default function CloseOverlayOnParametres() {
  return null
}
