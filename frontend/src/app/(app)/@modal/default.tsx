// Slot parallèle `@modal` : rien par défaut. Il n'affiche un contenu que lorsque
// l'intercepting route `(.)posts/[id]` capte une navigation soft vers un post.
export default function ModalDefault() {
  return null
}
