# CHANGELOG — WebDad / Breezy

> Historique complet des sessions de travail, déplacé hors de `CLAUDE.md` (qui dépassait la limite de chargement de contexte). `CLAUDE.md` ne garde que les dernières entrées.

---

*Last updated: 23/06/2026 — ci(message-service) : **fix couverture Codecov 41,34 % → ~97 %**. Constat : Codecov affichait `message-service/internal` à **41,34 %** (728/1761) alors que la suite atteint ~95 % en local. **Cause** : en CI, les tests d'intégration Mongo de message-service étaient **tous ignorés** (`t.Skip`) car (a) Mongo n'était démarré que pour `report-service` et (b) `MONGO_TEST_URI` n'était posée que pour `report-service` (`ci-go.yml`). Seuls les tests unitaires comptaient → 41 %. **Subtilité clé** : copier le pattern report-service (Mongo **sans** auth) ne suffit pas — les tests `mongotest.ReadOnlyDB` (`TestEnsureSchema_ReadOnly_CollModFails`, `TestSvc_ReadOnly_WriteErrorPaths`) créent un utilisateur `readAnyDatabase` et vérifient que ses **écritures sont refusées** ; or sans `--auth` Mongo **n'applique pas les rôles** → ces 2 tests échouent. **Fix `ci-go.yml`** : étape Mongo **dédiée** message-service avec **auth activée** (`MONGO_INITDB_ROOT_USERNAME/PASSWORD`, healthcheck `mongosh -u root`), `MONGO_TEST_URI` = `mongodb://root:ci-test-root@localhost:27017/?authSource=admin` (creds éphémères de conteneur CI, pas un secret de prod) ; report-service **inchangé** (reste authless, zéro risque). **Validé en local** contre `mongo:7 --auth` : `go test -race ./...` **tout vert**, couverture filtrée (main.go/docs.go/mongotest retirés comme en CI) = **97,3 %** (handler 100, repository 96,6, service 96,2, middleware 98,8, realtime 93,5, config 95,5, database 91,2). **⚠️ Action : pousser la branche** pour que Codecov recalcule. Aucun changement de code de production ni de tests — uniquement le workflow CI. Commit suggéré : `ci(message-service): run integration tests against auth-enabled Mongo`.*

*Last updated: 23/06/2026 — test(post-service) : **couche `internal/repository` mesurée et portée à 95,7 %** (objectif #327 « 95 % post-service », branche `327-tests-unitaires-95-post-service`). Constat : les tests d'intégration du repository (`integration_test.go` + `helpers_test.go`) **existaient déjà** (≈ 80 % avec un Mongo) mais étaient (a) **exclus de codecov** (`ignore: **/internal/repository/**`) et (b) **jamais exercés en CI** (le job de couverture ne fournissait un Mongo qu'à `report-service`) → 4,6 % mesurés (helpers purs seuls). **Approche validée (option A) : brancher post-service sur le pattern Mongo CI existant**, sans mock ni interface de dépôt côté prod. **(1) `ci-go.yml`** : l'étape « Start MongoDB » et l'env `MONGO_TEST_URI` couvrent désormais `report-service` **OU** `post-service` (mongo:7 éphémère, scopé via la matrice). **(2) `codecov.yml`** : le glob global `**/internal/repository/**` est remplacé par des exclusions explicites **par service sans Mongo en CI** (message/notification/profil/report/user) → seul le repository de post-service (validé) est mesuré, sans pénaliser les autres flags. **(3) 2 fichiers de tests ajoutés (aucun code de prod touché)** : `errorpaths_test.go` — un **contexte annulé** passé à ~75 méthodes fait échouer la 1ʳᵉ opération Mongo → couvre les branches `if err != nil { return …, err }` en bloc ; `branches_test.go` — branches logiques restées hors des parcours nominaux : boucle reposts de `GetByProfileHashtag` (id non hex, post fantôme, post masqué, hashtag non concordant, bornes de pagination), retours anticipés `LikedPostsByUser` (aucun like / ids tous invalides), court-circuits sur argument vide (`StatsByIDs`/`CommentStatsByIDs`/`LikedCommentIDsByUser`/`DeleteCommentLikesByComments`), branche `ErrNoDocuments` de `DeleteCollection`/`DeleteComment`, `SetNsfw(false)`, bornes `Since/Until` de `ListHidden` ; + 1 cas de tri inverse dans `helpers_test.go` (`sortProfilePosts`). **Résiduel ~4,3 % non couvert volontairement** : branches d'erreur `cursor.All`/`Decode` sur la **2ᵉ** opération après un `Find` réussi (le ctx annulé fait échouer le `Find` en premier) + course `IsDuplicateKeyError` d'`EnsureDefaultCollection` — non déclenchables sans mocker le driver Mongo (le repo utilise `*mongo.Collection` concret, pas une interface ; option « sans changement de prod » préservée). **Vérifs** (Mongo `mongo:7` éphémère sur :27099) : `go test ./internal/repository/...` = **95,7 %** tout vert ; `go test -race` **vert, 0 DATA RACE** (6,7 s) ; les 3 paquets Mongo (database/handler/repository) passent isolément ; `go build ./...`/`go vet ./...`/`gofmt -l` propres. NB : les timeouts observés sur `go test ./...` étaient de la **contention locale** (machine faisant tourner toute la stack dev en // du run), pas un bug — `report-service` exécute le même `go test -race ./...` avec **4** paquets Mongo et passe en CI. `golangci-lint` non installé en local → laissé à la CI. Commit suggéré : `test(post-service): cover repository layer (95.7%) + enable Mongo coverage in CI`.*

*Last updated: 23/06/2026 — feat(messages/profile-nav) : **identité cliquable dans l'en-tête des DM**. Front-only, aucune route backend/Swagger. Dans une conversation privée, l'avatar, le display name et le `@username` de l'interlocuteur ouvrent désormais son profil via `/profil/<username>?from=messages&conv=<conversationId>` (réutilise `ProfilLink`, hover-card conservée). La flèche retour du profil détecte ce contexte et renvoie explicitement vers `/messages?conv=<conversationId>`, ce qui rouvre la conversation au lieu de dépendre uniquement de l'historique navigateur. Retouche mobile : le `@username` et le statut d'activité passent en colonne sous le nom pour éviter que “dernière connexion…” masque le handle, tout en gardant la ligne compacte dès `sm`. Ajout des helpers purs `messagesConversationHref` et `profilFromConversationHref` + tests Vitest. Vérifs : `npm test -- src/lib/routes.test.ts` **21/21 OK** ; `npm test -- src/lib/routes.test.ts --coverage --coverage.include=src/lib/routes.ts` **routes.ts 100 % statements/branches/functions/lines** ; `npm run lint -- --file ...` ciblé **OK** ; lint ciblé additionnel `chat-pane.tsx` **OK** ; `npx tsc --noEmit` reste bloqué uniquement par fixtures tests préexistantes (`i18n.test`, `mention-search.test`, `posts.test`, `search-history.test`). `npm install` a été nécessaire pour réparer `node_modules` après l'absence locale de `@vitest/coverage-v8`/`vitest`. Commit suggéré : `feat(messages): link dm header identity to profile`.*

*Last updated: 23/06/2026 — feat(certifications) : **système de certifications de profil**. Back `profil-service` : nouveau champ décoratif `certification` (`none|political|public_figure`) sur `models.Profil`, défaut explicite à la création + seed admin, validateur Mongo enum et **migration boot idempotente** (`backfillCertification`) pour normaliser les profils legacy à `none` avant revalidation stricte. Nouvelle route **`PATCH /profils/:userId/certification`** (JWT, modérateur OU administrateur) avec annotations swaggo, service `SetCertification`, erreurs 400/401/403/404, tests service+handler. Front : `UserCertification` propagé dans `ProfilDetails`/`RelationUser`/`ResolvedUser`, `updateUserCertification()` dans `lib/admin`, menu Modération › Comptes `...` → **“Attribuer une certification”** sous “Changer le rôle” (Politique / Personnalité publique / Retirer). Nouveau `CertificationBadge` réutilisé profil + hover-card + modération : rosette verified façon X (jaune politique, bleue personnalité publique), logo Breezy pour staff quand le rôle connu vaut modérateur/admin ; tooltip via popover au clic/tap + `title` desktop. i18n ajouté dans les **12 locales**. Swagger régénéré (`doc/openapi.{json,yaml}`, route présente). Vérifs : `go test ./...` profil-service **OK** (avec `GOCACHE` local) ; `vitest run src/lib/admin.test.ts src/lib/api.test.ts src/lib/user-cache.test.ts src/lib/profil-client.test.ts` **62/62 OK** ; `npx tsc --noEmit` échoue uniquement sur erreurs de fixtures/tests préexistantes (`i18n.test`, `mention-search.test`, `posts.test`, `search-history.test`), plus aucune erreur liée à `certification`. `make swagger` direct échoue sous Windows (`bash` via make, error 1312) → exécuté via conteneur Go+swag avec `GOBIN=/root/go/bin` ; première tentative incomplète rejetée, seconde complète OK. Commit suggéré : `feat(profil): add account certifications`.*

*Last updated: 23/06/2026 — test(report-service) : **2 branches d'erreur défensives supplémentaires couvertes de façon DÉTERMINISTE** (database 92.7 → 95.1 %, service 98.2 → 98.8 %), **sans changement de prod**. Constat de départ : codecov reportait encore **90,41 % (773 hit / 11 partial / 71 missed sur 855 lignes)** — chiffre **mesuré AVANT le commit `errorpaths`** (la branche n'était pas encore poussée), codecov comptant des **lignes + partials** (branches à moitié prises), plus sévère que le `go tool cover` (statements). Reproduction locale du comptage ligne par ligne : code actuel (2 commits) ≈ **96,6 %** → déjà > 95 %, donc l'écart codecov se résorbe au push. Pour ajouter de la **marge** sur les 2 trous restants réellement couvrables in-process (la couverture n'est collectée que dans le process, pas dans un sous-process qui `os.Exit`) : **(1)** `service/reportService_errorpaths_test.go::TestSvc_Transfer_AddActionErreur` — la branche `if err != nil` après `AddAction` dans `Transfer` (377-379), inatteignable par ctx annulé (le `GetTicket` initial échouerait d'abord) : on crée un ticket modération **puis** un bug sur la MÊME entité `(post, dup-entity)`, le `Transfer` repasse `category=moderation` → **collision sur l'index partiel unique** `(entity_type, entity_id)` → `AddAction` remonte l'erreur de clé dupliquée. **(2)** `database/init_errorpaths_test.go::TestEnsureSchema_IndexConflit` — le forwarding `if err := ensureIndexes(...)` d'`EnsureSchema` (30-32), inatteignable par ctx annulé (`ensureCollections` échoue d'abord) : on **pré-crée sur `tickets` un index homonyme** `entity_type_1_entity_id_1` aux options différentes (ni unique, ni partiel) → `ensureCollections` réussit (collMod), puis `ensureIndexes` tente l'index unique partiel homonyme → **conflit d'options Mongo** → l'erreur remonte par la 2e sous-étape. Total statements **98,3 %**, proxy-ligne codecov **97,2 %** (miss 23→19, partial 11→9). Résiduel non couvrable (env/`os.Exit`) inchangé : `config.Load` `log.Fatal` + `/.dockerenv` ; `jwt.ParseToken` `!token.Valid` (défensif mort) ; `collMod`/`CreateCollection` erreurs ; `cursor.All` + `AddAction`-`ErrNoDocuments` défensifs (TOCTOU). Vérifs (Mongo `webdad-mongo-report-1` sur :27021) : `go test -race ./...` **vert**, `go vet`/`gofmt -l` propres. **⚠️ Action requise : pousser la branche** pour que codecov recalcule (le 90,41 % est antérieur aux 2 commits de tests).*

*Last updated: 23/06/2026 — test(report-service) : **couverture ~92 % → ~98 %** (objectif 95 % atteint et dépassé), **sans aucun changement de code de production**. Suite de la session du 22/06 : les ~8 % restants étaient des branches d'erreur `if err != nil { return …, err }` sur échec du driver Mongo, jamais déclenchées contre une base saine. **Levier retenu : passer un `context` DÉJÀ annulé** aux opérations → le driver renvoie `context.Canceled` (≠ `mongo.ErrNoDocuments`), ce qui tombe exactement dans ces branches — pas de mock, pas d'interface de dépôt côté prod (option « sans changement de prod » préservée). **5 nouveaux fichiers `_test.go`** (tous `MONGO_TEST_URI`-gated comme l'existant, sauf le client qui est pur) : (1) `repository/reportRepository_errorpaths_test.go` — ctx annulé sur les 12 méthodes → **91.7 → 97.2 %** ; (2) `service/reportService_errorpaths_test.go` — ctx annulé sur Approve/GetTicket/Reply/ChangeStatus/LogRemoval/Transfer/CreateReport(mod+bug)/ListTickets/UpdateThreshold/Settings/IssueWarning/WarningCount/PendingWarnings/AckWarning + `maybeAutoHide` quand `GetSettings` échoue → **92.8 → 98.2 %** ; (3) `handler/report_handler_500_test.go` — requêtes à ctx annulé sur `/reports/settings` & `/warnings/pending` (exerce la branche **500 du handler ET le `default` de `respondError`** dans `errors.go`), appels DIRECTS des handlers `Create`/`PendingWarnings` sans claims (garde `if !ok` → 401, service nil), + `TestParseTime` (vide/invalide/valide) → **92.6 → 99.3 %** ; (4) `database/init_errorpaths_test.go` — ctx annulé sur `EnsureSchema`/`ensureCollections`/`ensureIndexes`/`ensureSettings` + `ConnectMongo` URI malformée (branche `mongo.Connect`, distincte du ping déjà testé) → **80.5 → 92.7 %** ; (5) `client/post_errorpaths_test.go` — `call` vers port fermé (`Do` échoue) + base d'URL avec caractère de contrôle (`NewRequestWithContext` échoue), **synchronisé via un `slog.Handler` custom sur canal** (la goroutine fire-and-forget journalise → on attend le log) → **81.8 → 100 %**. **Total Codecov-équivalent (en retirant `testutil/`/`main.go`/`docs.go` ignorés par `codecov.yml`) : 585/597 = 97.99 %.** Résiduel volontairement non couvert (~12 stmts, incouvrables dans cet env) : `config.Load` `log.Fatal` (os.Exit) + branche `/.dockerenv` ; `jwt.ParseToken` `!token.Valid` avec `err==nil` (défensif inatteignable) ; `init` erreurs `collMod`/`CreateCollection` (état Mongo corrompu requis) ; quelques `return`/`cursor.All` défensifs (repo). Vérifs locales (Mongo `breezy-mongo-report-1` sur :27021) : `go test -race -covermode=atomic ./...` **vert**, `go vet` propre, `gofmt -l` vide. `golangci-lint` non installé en local → laissé à la CI.*

*Last updated: 22/06/2026 — test(frontend) : **couverture `src/lib` portée de 82,2 % à 96,1 % (objectif codecov 95 % atteint)**. Demande : monter le codecov front (`frontend/src/lib`) à 95 %. **Approche (concordance avec le 1er increase, cf. plus bas)** : rester **lib-only / Node-only**, **aucune infra composant (jsdom/RTL)**, **aucune exclusion ajoutée** au scope codecov — on couvre la **couche technique réellement testable** (clients réseau + utilitaires navigateur) en mockant `apiFetch`/`fetch`/globals, et on **laisse les hooks React `use-*.ts`** non testés (≈ 3,7 % du code lib, hors de portée sans renderHook). **Nouveaux tests (`+313`, 625 → 938, 64 fichiers, 100 % vert)** : couche réseau `posts` (12→99 %), `messages`/E2EE crypto mockée (15→99 %), `api`/`profil-client`/`admin` (≈0→99 %), `reports`/`notifications`/`bookmarks`/`provision`/`moderation`, cœur `auth-client` (Bearer + refresh single-flight + logout + bootstrap, via `resetModules`), `media`, `mfa`, `user-cache`, `content-filters` (storage+events), `custom-theme` (apply/persist DOM), `post-translation` (réseau), `monitoring`, `oauth-pending`, `terms-consent`, `hashtag-search`, `mention-search`, `search-tab`, `legal-origin`, `key-store` (IndexedDB factice en mémoire), `sounds` (Web Audio factice). Patterns : routeur `apiFetch` par URL ; `WebSocket`/`AudioContext`/`indexedDB`/`window` stubés ; `vi.resetModules` pour l'état module (single-flight, mémoires). **Vérifs** : `vitest run` **938/938 OK** ; `tsc --noEmit` **0 erreur sur les fichiers ajoutés** (erreurs préexistantes inchangées sur d'anciens `*.test.ts`, non bloquantes en CI). NB outillage local : `@vitest/coverage-v8` absent du `node_modules` (root-owned) → mesure via un `node_modules` symlinké en `~/.cache` + provider aliasé (denominator identique au codecov : 20611 stmts).*

*Last updated: 22/06/2026 — test(report-service) : **montée de la couverture 33 % → ~92 %** (objectif ~95 % discuté, plafond propre atteint). Approche validée (AskUserQuestion) : **tests d'intégration sur un vrai MongoDB** (pas de mock : `mtest` est passé `internal/` en driver v2, donc non importable) + service Mongo en CI, **sans changement de code de production**. **(1) Helper** `internal/testutil/mongo.go` : base Mongo isolée par test via `MONGO_TEST_URI` (sinon `t.Skip` → les autres services et les runs locaux sans Mongo ne cassent pas), schéma appliqué (`EnsureSchema`), `db.Drop` au cleanup. **(2) Tests d'intégration** : `repository` (89.7 %, tous les CRUD + agrégation ticket parent + dédup E11000 + réouverture/seed settings + warnings), `service` (92.8 %, avec un `spyPosts` espion de `PostModerator` : auto-hide/unhide, auto-reopen, verrou `approved`, garde-fou sanction), `handler` (92.6 %, routeur adossé à un vrai service : parcours HTTP complet + chemins d'erreur 400/404/409). **(3) Tests unitaires** purs : `database` (80.5 %, `EnsureSchema` idempotent 2× → branche collMod, `ConnectMongo`), `config` (92 %, `Load`/`buildMongoURI`/`splitCSV`), `middleware` (98.6 %, `RequestID`/`RequestLogger`/`Recovery` + branches claims-absent/`ParseToken` non-HMAC), `main` (`ginMode`). **(4) CI** (`ci-go.yml`) : étape conditionnelle `if: matrix.service == 'report-service'` qui lance `mongo:7` (port 27017) + `MONGO_TEST_URI` scopé via la matrice → les 7 autres jobs ne paient pas le conteneur. **(5) `codecov.yml`** : `ignore` étendu à `**/testutil/**` (aide de test), `**/docs/docs.go` (Swagger généré) et `**/main.go` (racine de composition non testable unitairement). **Plafond ~92 %** : les ~8 % restants sont des gardes défensives `return nil, err` sur erreurs du driver Mongo, non déclenchables sans injection de panne (qui imposerait une interface de dépôt côté production — écartée par l'option choisie « sans changement de prod »). Vérifs locales (Mongo `mongo:7` éphémère sur :27099) : `go test -race ./...` **vert**, `gofmt`/`go vet` propres, `golangci-lint v2.12.2` **0 issue**.*

*Last updated: 22/06/2026 — test(message-service) : **couverture portée de ~28 % à 95,2 %** (issue #330 « tests unitaires 95 % message-service »). **Stratégie validée (AskUserQuestion) : tests d'intégration sur Mongo RÉELLE** (le service tient un `*repository.MessageRepository` concret + collections `*mongo.Collection` concrètes → non mockable sans refactor ; couvrir le repository (235 stmts à 0 %) impose une vraie DB de toute façon) **+ ajout d'un service `mongo` à la CI** pour que codecov reflète le chiffre. **Harness** : `internal/mongotest` — `DB(t)` (base jetable sur un **client partagé** par le binaire de test, anti-churn de connexions) et `DisposableDB(t)` (client dédié pour les tests qui le **déconnectent** afin de simuler une panne) ; gated par `MONGO_TEST_URI` → `t.Skip` si absente (la suite reste verte sans Mongo). **Suites ajoutées** : (1) `repository` 96,6 % — CRUD clés/backups/conversations/membres/messages, receipts, unread `$lookup`, communautés + un test « client déconnecté » qui couvre d'un coup toutes les branches `if err != nil { return err }` ; (2) `service` 92,6 % — tous les flux métier (DM/group/community, join, rôles, membres, send/edit/delete/moderate, pin/mute/clear/read, receipts) + branches d'erreur via client déconnecté **et via injection de panne par document corrompu** (`created_at` en string, `bypassDocumentValidation` → le 1er accès repo passe, le décodage du 2ᵉ échoue) pour exercer les `return err` profonds ; (3) `handler` 98,5 % — chemins succès via routeur+JWT réels, branche « claims absents » (appels directs), et **routeur sur DB en panne** pour traverser tous les `respondError` ; (4) WS/hub 93,5 % (vrai serveur httptest + client gorilla : `Register`/`readPump`/`writePump`/`OnlineFrom`/`Connect`) ; (5) `config`, `middleware/logging`, `middleware.VerifiedOnly`, `database.EnsureSchema` (idempotence) + `ConnectMongo`. **codecov** : `**/main.go` (bootstrap), `**/docs/docs.go` (swaggo généré) et `message-service/internal/mongotest/**` (infra de test) ajoutés aux `ignore` → flag message-service à **95,17 %** (1300/1366). **CI (`ci-go.yml`)** : service `mongo:7` (health-check mongosh) + `MONGO_TEST_URI` posée **uniquement** pour `message-service` (expression *Last updated: 22/06/2026 — test(message-service) : **couverture portée de ~28 % à ~95 % (lignes codecov) / 97 % (statements)** (issue #330). Stratégie validée (AskUserQuestion) : **intégration sur Mongo RÉELLE** + service `mongo` en CI. **Helper `internal/mongotest`** gated `MONGO_TEST_URI` (skip sans DB) : `DB` (client partagé, anti-churn), `DisposableDB` (client dédié déconnecté → simule une panne), **`ReadOnlyDB`** (utilisateur Mongo **lecture seule** unique par appel → les lectures passent mais toute ÉCRITURE échoue : couvre les `if err != nil` profonds sur appel repo en écriture, sans toucher au prod). **Trois techniques d'injection de panne, vraie Mongo, zéro modif prod** : (1) client déconnecté → erreurs sur le 1er appel ; (2) **document corrompu** (`created_at` en string via `bypassDocumentValidation`) → erreurs de décodage du 2ᵉ appel (lecture) ; (3) **client lecture seule** → erreurs d'écriture après lectures réussies. **Résultats** : handler **100 %**, service **96 %**, repository **97 %**, middleware **99 %**, realtime **94 %** (WebSocket réel : Register/read/writePump/Connect + livraison « en ligne »), database 91 % (EnsureSchema idempotence + branches d'erreur via RO/déconnecté). **codecov.yml** : ignore `**/main.go` (bootstrap), `**/docs/docs.go` (swaggo généré), `mongotest/**` (infra de test). **ci-go.yml** : service `mongo:7` + `MONGO_TEST_URI` scopée à `message-service` (expression sur `matrix.service`). **Robustesse** : pool de connexions réduit (anti-saturation) + utilisateur RO unique (anti-collision entre binaires de package parallèles). Vérifs : `go build`/`vet`/`gofmt` clean ; `go test ./...` **vert sans Mongo** (skip) et **vert avec Mongo** (la flakiness observée en local était un OOM machine sous parallélisme `-race` maximal — host à 2 Gi libres — pas un défaut du code : vert en parallélisme réduit comme en CI dédiée). Aucune modification du code de production.*

*Last updated: 22/06/2026 — test(post-service) : **couverture du package `internal/service` portée à 95,8 %** (objectif #327 « 95 % post-service », branche `327-tests-unitaires-95-post-service`). Point de départ : `internal/service` à **83,6 %** (le reste de l'infra de test — `fakeRepo` piloté par champs-fonction, stubs `stubProfil`/`stubFollow`/`profilFnClient`/`capNotifier`, helpers `bg()`/`errBoom`/`postWith()`/`collOf()`/`newOID()` — était déjà en place). **Ajout de 2 fichiers de tests, branches d'erreur uniquement (aucun code applicatif touché)** : `postService_coverage_test.go` et `bookmarkService_coverage_test.go`. Cible : les chemins d'échec de dépendances (repo Mongo / clients profil-follow) jusqu'ici non exercés — `canReadAuthor` (blocage, visibilité, abonnement, fail-closed), `GetPosts`/`visiblePage` (fetch error, filtrage privé, offset, pagination multi-page), `TrendingHashtags`, `VotePoll`/`ClosePoll` (toutes les gardes : not-found, no-poll, not-author, followers-only, repo errors), `LikePost`/`UnlikePost`/`LikeComment`/`UnlikeComment` (invalid id, get error, add/remove error, inc error, idempotence, wrong-post), `CommentStats` (troncature `MaxStatsIDs` + repo error), `ListLikedByUser`/`ListCommentsByAuthor` (bloqué, visibilité, filtrage hidden/private), `RepostPost`/`UnrepostPost`, `CreateComment` (parent invalide/introuvable/autre-post, réponse-à-réponse, add/inc error), `commentAuthor`/`postAuthor`, `ListComments`/`ListReplies`, `DeleteComment`, `PinPost`/`UnpinPost`, `GetByProfile`, et tout `bookmarkService` (collections + signets : create/list/rename/delete errors, fenêtre de rafale `WindowCollectionGone`/`WindowFileError`, `fileBookmark`, `resolvePosts` best-effort, `requirePost`, `translateCollectionNotFound`). Helpers locaux ajoutés : `commentWith()`, `getOK()`, `pollPostAud()` (variante audience de `pollPost`). Note : `translateNotFound` mappe `mongo.ErrNoDocuments` → `ErrPostNotFound` (pas d'`ErrCommentNotFound` distinct). **Vérifs** : `go test ./internal/service/...` = **95,8 %**, tout vert ; `go test ./...` (post-service) tout vert ; `go vet ./internal/service/...` propre ; `gofmt -l` vide. ⚠️ `golangci-lint` non installé en local (cible `make lint` indisponible) → laissé à la CI.*
*Last updated: 22/06/2026 — feat(auth+frontend) : **acceptation des CGU obligatoire et versionnée, modale bloquante post-login**. Besoin : forcer les comptes DÉJÀ EXISTANTS (local + prod) à accepter les CGU à leur prochaine connexion. Choix validés (AskUserQuestion) : **(a) consentement persisté côté back** (vs localStorage), **(b) modale bloquante AVEC bouton « Refuser & se déconnecter »** (consentement libre). Pattern calqué sur `must_change_password` (login bloquant via claim JWT). **Back (auth-service)** : (1) `schema.sql` → `credentials.terms_accepted_version INT NOT NULL DEFAULT 0` + `terms_accepted_at TIMESTAMPTZ` (rétro-compatible règle 5b : `ADD COLUMN ... DEFAULT 0` pose 0 sur TOUTES les lignes existantes → tous les comptes en prod repassent sous la version courante et doivent accepter). (2) `models.CurrentTermsVersion = 1` (rebumper = re-consentement global). (3) Claim JWT `terms_accepted` = `version >= CurrentTermsVersion`, dérivé dans `GenerateToken`. **Robustesse** : `terms_accepted_version` re-chargé depuis la base dans `issueTokens` (un SELECT PK avant chaque émission) plutôt que threadé dans ~8 SELECT → le claim ne retombe jamais faux après refresh/MFA/changement d'e-mail (même piège qu'`email_verified`). (4) Comptes ayant déjà consenti (`Register`, OAuth `complete`) → INSERT à `CurrentTermsVersion` (pas de re-prompt) ; comptes créés par un admin → restent à 0 (doivent accepter). (5) `POST /auth/terms/accept` (JWT) → UPDATE version+`NOW()`, ré-émet la paire de tokens ; route + annotations swaggo + `make swagger`. **Front** : `session.ts` (`terms_accepted` → `termsAccepted`) ; BFF `api/auth/terms/accept` (pose le cookie refresh, renvoie le nouvel access token, calqué sur password/change) ; `components/account/terms-accept-gate.tsx` (modale non-fermable, liens CGU/confidentialité, « J'accepte » → BFF → `setAccessToken` → session rafraîchie, modale disparaît ; « Refuser » → `logout()`), montée dans `(app)/layout.tsx` à côté de `PasswordChangeGate` ; i18n `account.terms_gate.*` (9 clés × **12 locales** : FR/EN dans `i18n.ts`, 10 autres dans `i18n-additional.ts`). Vérifs : `go build` + `go test ./...` auth-service **OK** ; `vitest session.test.ts` **19/19 OK** (nouveau test `terms_accepted`) ; `tsc` : seules les **erreurs préexistantes** dans des `*.test.ts` (aucune sur les fichiers touchés) ; `make swagger` régénère `doc/openapi.{json,yaml}` (endpoint présent).*

*Last updated: 22/06/2026 — i18n(frontend) : **pages légales + pop-up d'acceptation des CGU traduites dans les 12 locales**. Constat : (a) le *chrome* légal (`legal.badge/updated/back/section/mentions/cgu/confidentialite`) était déjà traduit dans les 10 locales additionnelles, mais (b) le **contenu réel** des 3 documents (`legal-content.ts` : mentions légales, CGU, confidentialité) n'existait **qu'en FR/EN** → les 10 autres langues retombaient sur le fallback EN ; (c) les **8 clés de la pop-up d'acceptation** (`auth.register.terms_prefix/link/locked/unlocked`, `auth.register.err.terms_required/terms_read_required`, `onboarding.accept_terms/terms_accepted`), utilisées par `auth/oauth/terms/page.tsx` et `register`, étaient **absentes des 10 locales** additionnelles. **Périmètre validé par l'utilisateur (AskUserQuestion) : contenu complet des 3 docs × 10 langues.** **(1) `i18n-additional.ts`** : +8 clés × 10 locales (zh/es/pt/ru/ja/ko/ar/hi/de/it), insérées après `onboarding.submit` ; `terms_link` aligné sur le terme déjà choisi pour `legal.cgu` dans chaque langue. **(2) `legal-content.ts`** : ajout des 10 consts `ZH/ES/PT/RU/JA/KO/AR/HI/DE/IT` (3 documents chacune, structure miroir du FR : mentions=6 sections, cgu=11 articles, confidentialité=9 sections), registre `legalContent` passé de `Partial<Record<Locale,…>>` à **`Record<Locale,…>` complet (12 locales)** → plus aucun fallback EN. **(3) `legal-content.test.ts`** : étendu de fr/en aux **12 locales** + **test de parité structurelle vs FR** (même nombre de sections, même type p/list et même longueur de listes par bloc). Front-only, aucun contrat back/route/Swagger. Vérifs : `node_modules` non installé en local (tsc/vitest indisponibles) → validation **équivalente par `eval`** des deux fichiers : 12 locales × 3 docs, **parité fine OK** (signatures de blocs identiques au FR partout), 10 locales × 8 clés CGU **toutes présentes**, métadonnées non vides ; à confirmer via `make dev`/CI (`tsc` + `vitest`).*

*Last updated: 21/06/2026 — test(frontend) : **couverture lib des composants modifiés sur la branche #318 (extraction de logique pure)**. Demande : ajouter un max de tests pour couvrir les composants ajoutés/modifiés. Constat : l'infra est **Node-only** (`vitest.config.ts` → `environment: 'node'`), coverage scopée `src/lib/**` ; les tests existants ne couvrent que `lib/*`, alors que le diff de la branche est **100 % composants**. **Décision retenue (après échange) : rester en lib-only** — on n'introduit PAS d'infra de test composant (jsdom / Testing Library) ; on couvre la logique en **extrayant le pur des composants vers `lib/`** (suit le pattern existant, zéro nouvelle dépendance, alimente la coverage déjà mesurée). NB : une infra jsdom/RTL + un test `gif-picker.test.tsx` avaient d'abord été ajoutés puis **retirés** sur ce choix (deps désinstallées, `vitest.config.ts` restauré, `vitest.setup.ts` supprimé). **Tests lib (logique réseau)** : `lib/giphy.test.ts` (11 tests, `apiFetch` mocké) — `searchGifs` (params limit/q, trim, mapping snake_case, défauts, erreurs) + `captureGif` (POST, erreurs) ; `giphy.ts` était **non testé**, c'est la dépendance du `GifPicker`. **Extractions pures** (refactor sans changement de comportement) : (1) `lib/mobile-header-nav.ts` `resolveMobileHeaderNav(pathname)` → `{section,titleKey,showBell,showBack}` extrait de `MobileHeader` + `mobile-header-nav.test.ts` (12 tests) ; (2) `lib/poll-view.ts` `isPollClosedAt`/`isPollClosed`/`pollResultsView`/`formatPollRemaining` extraits de `PostCard` + `poll-view.test.ts` (13 tests). **Résultat : 372 → 408 tests (+36), 37 fichiers, 100 % vert.** Coverage des fichiers touchés : `giphy.ts` 100 %, `poll-view.ts` 100 %, `mobile-header-nav.ts` 100 %. Vérifs : `tsc --noEmit` = **6 erreurs préexistantes** dans des `*.test.ts` (fixtures), **identiques avant/après** (0 régression) ; `eslint` 0 erreur sur les fichiers touchés (warnings `<img>`/`exhaustive-deps` préexistants) ; `graphify update .` OK.*

*Last updated: 20/06/2026 — fix(ui) : **en-tête mobile type-feed sur la page Signets (branche #318)**. Demande : la page Signets devait afficher en haut, comme le feed et la modération, le composant à photo de profil (avatar) + cloche. Cause : le `MobileHeader` (`layout/mobile-header.tsx`) classait `/signets` en section `'other'` → `return null`, et `signets/page.tsx` passait `headerOffset={false}` (pas de réserve `pt-14`). **3 changements** : (1) `mobile-header.tsx` : nouvelle section `'signets'` (détectée via `pathname.startsWith(ROUTES.bookmarks)`), `titleKey = 'bookmarks.title'`, et incluse dans `showBell` → en-tête identique au feed/modération (avatar ← « Signets » → cloche 🔔, choix utilisateur). (2) `signets/page.tsx` : suppression de `headerOffset={false}` (repasse à `true`). (3) `bookmarks/bookmarks-view.tsx` : la **ligne de titre** « Signets » est masquée sur mobile (`hidden … lg:flex`, comme la modération) pour ne pas dédoubler le titre porté par l'en-tête global ; le bouton « … » (renommer/supprimer collection) qui y vivait est **factorisé en composant `ManageMenu`** et rendu à deux emplacements — ligne de titre (desktop) et **barre de pills, épinglé à droite hors défilement** `lg:hidden` (mobile, choix utilisateur) ; la rangée de pills gagne `pt-3 lg:pt-0` pour compenser le titre masqué. Front-only, aucun contrat back/route/Swagger, aucune nouvelle clé i18n (réutilise `bookmarks.title`). Vérifs : `tsc` propre sur les 3 fichiers touchés (erreurs globales = uniquement `*.test.ts` préexistants), `eslint` 0 erreur (seul warning `exhaustive-deps` préexistant dans `mobile-header.tsx`), `graphify update .` OK.*

*Last updated: 20/06/2026 — feat(ui) : **GIF en commentaire/réponse + emoji masqué sur mobile (branche #318)**. Suite du composer de commentaire responsive. **(1) Bouton GIF (PC + mobile)** dans le composer racine ET le composer de réponse : le `GifPicker` (jusqu'ici **local** à `feed/post-composer.tsx`) est **extrait dans `feed/gif-picker.tsx`** (composant partagé, `export function GifPicker`) et réutilisé par les deux composers — zéro duplication. `post-composer.tsx` l'importe désormais (`'@/components/feed/gif-picker'`) et perd ses imports devenus morts (`searchGifs`, `Search` lucide, `Popover*`). **`CommentComposer`** (`feed/comment-section.tsx`) gagne une prop `onAddGif: (gif: GiphyGif) => void` et affiche `<GifPicker>` entre le bouton image et l'emoji. Capture serveur identique au post (`captureGif` → MinIO `/media/<id>`, **jamais l'URL giphy externe** : hotlink 403/404 + CSP) : handlers `addGif` (racine, dans `CommentSection`) et `addReplyGif` (réponse, dans `CommentThread`), chacun avec garde `MAX_MEDIA` + toasts `composer.gif_added`/`gif_failed`. **(2) Emoji masqué sur mobile** : le bouton `EmojiPicker`/`Smile` du composer de commentaire passe `hidden … sm:block` (clavier natif fournit déjà les emojis), aligné sur le composer de post. Front-only, aucun contrat back/route/Swagger, **aucune nouvelle clé i18n** (réutilise `composer.add_gif`/`gif_search`/`gif_empty`/`gif_failed`/`gif_added`/`media_max` déjà présentes). Vérifs : `tsc` propre sur les 3 fichiers touchés (erreurs globales = uniquement `*.test.ts` préexistants), `eslint` 0 erreur (seul warning `<img>` préexistant dans post-composer), `graphify update .` OK.*

*Last updated: 20/06/2026 — fix(ui) : **composer de commentaire/réponse responsive sur mobile (branche #318)**. Problème signalé : sur téléphone l'input de commentaire (et de réponse) était écrasé car la ligne unique `[input][🖼️][😊][Répondre]` laissait trop peu de place au champ ; pratique sur PC, illisible sur mobile. **Décisions utilisateur (AskUserQuestion)** : (a) **factoriser** plutôt qu'éditer les 2 copies dupliquées ; (b) sur mobile, **Répondre à droite**, 🖼️😊 à gauche (zone du pouce). **Refactor (`feed/comment-section.tsx`)** : les deux blocs composer quasi identiques (composer racine `CommentSection` + composer de réponse inline `CommentThread`) sont remplacés par un composant partagé **`CommentComposer`** (props : `inputRef`/`fileInputRef`/`mention`/`content`/`setContent`/`media`/`onRemoveMedia`/`onFiles`/`uploadingMedia`/`onInsertEmoji`/`canSubmit`/`submitting`/`onSubmit`/`placeholder`). Logique inchangée (mention autocomplete, emoji, upload média, submit Enter). **Layout responsive** : conteneur `flex flex-col gap-2 sm:flex-row sm:items-center` → input `flex-1` sur sa propre ligne en mobile, ligne unique en ≥ sm ; sous-bloc actions `flex items-center justify-between gap-2 sm:justify-end` → 🖼️😊 groupés à gauche, **Répondre** à droite sur mobile, recollés à droite de l'input sur desktop (rendu PC identique à avant). Front-only, aucun contrat back/route/Swagger, aucune nouvelle clé i18n. Vérifs : `tsc` propre sur `comment-section.tsx` (erreurs globales = uniquement `*.test.ts` préexistants), `eslint` 0 erreur, `graphify update .` OK.*

*Last updated: 20/06/2026 — fix(ui) : **composer allégé + refonte des résultats de sondage façon Twitter (branche #318)**. **(1) Composer (`feed/post-composer.tsx`)** : le bouton **emoji** (`Smile`/`EmojiPicker`) est désormais `hidden … sm:block` → masqué sur mobile (le clavier natif fournit déjà les emojis), conservé desktop ; le bouton **épinglé** (`Pin` → `pin_profile`) est **supprimé partout** (PC + mobile) car peu utile — l'épinglage reste possible après coup via le menu « … » du post. Nettoyage du code mort associé : état `pinOnProfile`, branche `pinPost` au submit, imports `Pin`/`pinPost`. **(2) Résultats de sondage (`PostPollCard` dans `feed/post-card.tsx`)** : la **vue de vote** (boutons cliquables tant qu'on n'a pas voté / sondage ouvert) est **inchangée** ; seul le **rendu des résultats** est refait (`resultsView = canViewResults && (voté || clos)`). Avant : chaque option = bouton bordé (`rounded-lg border`), barre + label « gagnant » + vert émeraude, le tout dans une carte encadrée. Après (façon Twitter, lisible mobile) : **plus d'encadré** (ni par ligne ni conteneur en mode résultats), chaque ligne = **barre type chart** (`<span absolute>` largeur = `percent%`, `Math.max(percent,2)` pour rester visible), **ligne(s) gagnante(s) en bleu Breezy** (`bg-primary/35`, label en gras) vs **gris** (`bg-muted`) pour les autres — **plus de label « gagnant »**, plus d'émeraude. À droite : **nombre de votes** de la ligne en petit (`text-xs text-muted-foreground`) **à gauche du %** (gras). Pied de sondage (total · « Résultats finaux »/temps restant · audience) inchangé. Clé i18n `post.poll_winner` devenue inutilisée (laissée en place). Front-only, aucune route/contrat back/Swagger ; aucune nouvelle clé i18n. Vérifs : `tsc` propre sur les 2 fichiers touchés (erreurs globales = uniquement des `*.test.ts` préexistants), `eslint` 0 erreur (seulement les warnings `<img>` préexistants), `graphify update .` OK.*

*Last updated: 20/06/2026 — fix(ui) : **scroll d'arrière-plan bloqué quand un menu « 3 points » est ouvert (branche #318)**. Symptôme signalé par l'utilisateur (surtout mobile) : ouvrir le menu d'actions « … » (signaler, etc.) verrouillait le scroll derrière le menu. Cause : les `DropdownMenu` shadcn/Radix sont `modal={true}` par défaut → Radix pose un scroll-lock + `pointer-events: none` sur le body tant que le menu est ouvert. **Correctif** : `modal={false}` sur les **7 menus « 3 points »** de l'app (triggers `MoreHorizontal`) : `feed/post-card.tsx`, `profil/profil-header.tsx`, `bookmarks/bookmarks-view.tsx`, `messages/conversation-list.tsx` (menu d'actions conv, **pas** le menu « + » de création), `messages/chat-pane.tsx`, `moderation/accounts-panel.tsx`, `layout/sidebar-left.tsx`. Le menu se ferme toujours au clic extérieur / Échap, accessibilité clavier inchangée. Non touchés volontairement : language-selector, post-composer, et le menu « + » de conversation-list (pas des menus 3-points). **Suivi (raffinement z-index)** : le menu (portail Radix `z-50`) passait au-dessus de la barre de nav mobile (`mobile-tab-bar` `fixed bottom-0 z-50`, gagne par ordre DOM) et du composer de message privé (`chat-pane` `sticky bottom-0 z-20`). Choix : **abaisser le `z-index` du `DropdownMenuContent`** plutôt que relever la nav/composer (les relever les ferait passer au-dessus des `Dialog` Radix en `z-50` → régression). **6 menus hors messagerie** → **`z-40`** (sous la nav mobile `z-50` et le header `z-[60]`, au-dessus du contenu). **Menu « … » des messages — cas spécial (3 essais)** : la section messages est rendue en **overlay `FeedOverlay` `fixed z-40`** au niveau racine (`(app)/layout.tsx` ligne 78), et le composer (`chat-pane` `sticky z-20`) vit **à l'intérieur** de cet overlay, alors que le menu Radix se porte par défaut sur `<body>` (frère de l'overlay). Conséquence : un portail racine est **soit au-dessus de TOUT l'overlay** (composer inclus, essais `z-50`/`z-40`), **soit caché derrière** (essai `z-[15]`) — aucun z-index ne peut le rendre visible ET sous le composer. **Solution retenue : porter le menu DANS l'overlay.** `ui/dropdown-menu.tsx` : `DropdownMenuContent` accepte un prop `container` (rétro-compatible, défaut `body`) transmis à `DropdownMenuPrimitive.Portal`. `chat-pane.tsx` : une cible de portail `<div ref={setMenuPortal}>` est montée dans la colonne du chat (état `menuPortal`, fil descendant `ChatPane → MessageBubble → MessageActionsMenu`), le menu est porté dedans (`container={menuContainer}`) avec **`z-10`** → au-dessus des bulles (z-auto) mais SOUS le composer `z-20`, **dans le même contexte d'empilement** (l'overlay `z-40`). Le composer passe donc enfin devant. Sûr car l'overlay `wide` (messagerie) n'a ni `transform`/`backdrop-blur`/`overflow` (cf. `feed-overlay.tsx` ligne 84) → le menu `fixed` n'est pas rogné. `collisionPadding={{ bottom: 88 }}` conservé pour qu'il s'ouvre vers le haut près du composer. Vérif : `tsc` propre sur les fichiers touchés. Front-only, aucune route/clé i18n/Swagger. ⚠️ `tsc` global remonte uniquement des erreurs **préexistantes dans des `*.test.ts`** (fixtures), aucun fichier source édité en erreur ; lint non lançable en local (node_modules root-owned) → valider via `make dev`/CI.*

*Last updated: 19/06/2026 — feat(gateway/sécurité) : **validation + propagation du JWT au niveau gateway (branche #26)**. Architecture validée avant code (portée « valider-si-présent » + propagation par en-têtes avec strip). Avant : le gateway proxifiait toutes les routes sans aucun filtre JWT ; seul `AdminJWT` gardait `/admin/monitoring` (servie directement, pas le proxy). Chaque service revalidait déjà le token. **Ajout** : middleware global `PropagateJWT(cfg.JWTSecret)` (`api-gateway/internal/middleware/jwt.go`) branché en `r.Use(...)` APRÈS `RequestLogger` et AVANT le reverse proxy (`router.go`). Logique : (1) **strip systématique** des en-têtes d'identité entrants `X-User-Id`/`X-User-Role`/`X-Email-Verified` (anti-spoof, même sans token) ; (2) pas de token → passe-plat (login, refresh-cookie, vue visiteur publique) ; (3) `Authorization` présent mais format non-`Bearer` ou token invalide/expiré/mauvaise signature → **401 au plus tôt**, n'atteint jamais le service ; (4) token valide → pose `X-User-Id`/`X-User-Role`/`X-Email-Verified` depuis les claims (le proxy fait `rp.ServeHTTP(c.Writer, c.Request)` → les mutations de `c.Request.Header` sont transmises). Le `claims` du gateway gagne `email_verified`. **Refactor** : extraction des helpers `parse(tokenStr, key)` + `bearerToken(c)` ; `AdminJWT` réécrit pour les réutiliser. **Choix** : « valider-si-présent » plutôt qu'allowlist publique (B) ou table de politique par route (C) → zéro config par route, ne casse jamais les routes publiques/`OptionalJWTAuth`, pas de duplication du routage des services. **Défense en profondeur conservée** : les services revalident le JWT (en-têtes propagés = usage futur, pas l'unique source de confiance) → aucun changement côté services, aucune route/handler touché donc **pas de `make swagger`**. **Tests** (`api-gateway/internal/middleware/jwt_test.go`) : statuts (valide/absent/expiré/mauvais secret/bidon/Basic), injection des 3 en-têtes depuis les claims, strip des en-têtes usurpés (sans token), token valide qui écrase un `X-User-Role` usurpé. Vérifs : `go build ./...` + `go vet ./...` + `go test ./...` (gateway) **OK**. ⚠️ `golangci-lint` non installé en local → laissé à la CI.*
*Last updated: 19/06/2026 — fix(post-service) : **500 sur `POST /posts` pour un modo postant > 280 caractères** (désynchro applicatif ↔ validateur Mongo). Le commit #313 a levé la limite à **4000 pour modos/admins** côté front (`post-composer.tsx`) **et** côté handler (`enforceContentLimit` exemptait les rôles privilégiés), **mais le `$jsonSchema` Mongo est resté à `content maxLength:280`** et il est **réappliqué à chaque boot** via `collMod` (`init.go`). Conséquence : un modo postant 281–4000 caractères passait front + handler, puis se faisait **rejeter par Mongo** sur `repo.Create` → `default` de `respondPostError` → **500** (le log Error porte l'erreur Mongo brute). Même classe que la Règle 5b, mais ici on **relâche** une contrainte (max plus grand) → **rétrocompatible, aucun backfill** (aucun doc existant ne viole une borne plus large). **Correctif** : (1) `init.go` posts `content maxLength` 280 → **4000** (commentaires laissés à 280 : plafonnés à 280 pour tous les rôles, front + binding `max=280`, pas d'exemption) ; (2) `enforceContentLimit` borne désormais **explicitement** les modos/admins à 4000 (au lieu de `return true` sans borne) → reste sous le validateur Mongo, cohérent avec `MAX_CHARS_PRIVILEGED=4000` du front et le tag binding `max=4000` (create + update). Le 404 média des logs console est du **bruit du feed** (post existant à variante manquante), pas la cause de l'échec : l'upload ne renvoie d'URL de variante qu'après `PutVariant` réussi. Vérif : `go build ./...` OK. ⚠️ **N'atteint la prod qu'au prochain redéploiement d'image** (l'`EnsureSchema`/`collMod` ne tourne qu'au boot) — hotfix prod immédiat possible via `collMod` sur `breezy-mongo-post-1` (cf. session). Tests non relancés.*

*Last updated: 19/06/2026 — fix(gifs) : **GIF postés cassés en prod (403/404) → capture serveur en MinIO**. Diagnostic : le GIF choisi était stocké dans le post comme **URL GIPHY externe brute** (`post-composer.addGif` → `{url: gif.url}`) et chargé **directement par le navigateur** depuis `media.giphy.com` → hotlink hors gateway. Conséquences : (1) `403 Forbidden` non déterministe selon l'asset (CDN giphy refuse certaines URL originales nues) ; (2) `404` sur `…/200.gif` côté `zervixt` = preuve que `GIPHY_API_KEY` est **absente en prod** → c'est le `fallbackGifCatalog` codé en dur qui est servi, dont plusieurs URL sont mortes ; (3) bombe CSP : la prod envoie `Content-Security-Policy-Report-Only: default-src 'self'` → une fois en *enforce*, toutes les images giphy seront bloquées. Viole la règle non-négociable « everything through the gateway, never external URLs ». **Décision validée par l'utilisateur : Option A — persister en MinIO** (cf. DECISIONS). **Backend/media-service** : nouveau handler JWT `POST /gifs/capture` (`{url}`) qui télécharge les octets depuis une **allowlist d'hôtes GIPHY** (anti-SSRF : `media.giphy.com`, `media0-4`, `i.giphy.com` ; `https` only), lecture bornée `io.LimitReader(maxImageBytes+1)` → 413 si dépassement, `validate.Detect` (doit être `KindImage`, sinon 415), `store.Put` avec `owner = claims.UserID` (Delete/purge RGPD comme un upload), **pas de variantes** (préserve l'animation), réponse forme `uploadResponse` `{id,url:"/media/<id>",mime,kind,size}`. Route ajoutée dans `routes.go` (gateway proxie déjà `/gifs`). **Frontend** : `lib/giphy.ts` → `captureGif(url): UploadedMedia` (`POST /gifs/capture`) ; `post-composer.addGif` devient async (capture → stocke `/media/<id>` via `uploadedMediaUrl`, toast succès/échec, `uploadingMedia` pendant la capture) ; `GifPicker.choose` ne fait plus que fermer la popover. **Swagger** : `make swagger` OK, `/gifs/capture` exposé. Vérifs : media-service `go build ./...` + `go vet` + `gofmt -l` clean. ⚠️ Reste à faire côté ops (VPS) : **poser `GIPHY_API_KEY`** dans `media-service/.env` (corrige le 404 du picker) + **purger les entrées mortes** de `fallbackGifCatalog`. Résiduel connu : les **vignettes du picker** restent hotlinkées giphy (UI éphémère) → à proxifier si la CSP passe en enforce. ⚠️ `tsc`/`lint` non lançables en local (node_modules root-owned).*

*Last updated: 19/06/2026 — fix(composer/feed) : **2 correctifs UI composer**. **(1) Caret désaligné dans le champ de tweet** : le texte est peint par l'overlay `ComposerHighlight` (`overflow-hidden`, sans gouttière) tandis que le `<textarea>` transparent porte le caret ; dès que le texte scrolle, la **scrollbar du textarea (~15px)** rétrécissait sa largeur de contenu vs l'overlay → le textarea revenait à la ligne plus tôt, donc le caret se retrouvait à gauche du texte visible (« un bout du mot derrière la barre »). Fix : scrollbar du textarea masquée (`[scrollbar-width:none] [&::-webkit-scrollbar]:hidden`) → largeur de contenu = celle de l'overlay, scroll toujours piloté/synchronisé. **(2) Encart blanc sous le header au bandeau « a posté »** : le bandeau temps réel (`feed-view.tsx`) était `sticky` **dans le flux** → il insérait une bande pleine largeur sans fond `.panel` entre header et composer (on voyait le `glass-column` = encart blanc). **Décision utilisateur : passage en avant-plan** plutôt qu'un fond panel. Fix : wrapper en `h-0 items-start overflow-visible` → le bandeau **flotte** par-dessus le composer (son `z-20` le place devant) sans réserver de hauteur dans le flux. Front-only, aucun contrat back/route/Swagger, aucune clé i18n. ⚠️ `tsc`/`lint` non lançables en local (node_modules root-owned) → à valider visuellement via `make dev`.*
*Last updated: 19/06/2026 — fix(ci/tests) : **job CI Go réparée — tests auto-générés réalignés sur le code (branche #47)**. La job `CI Go` cassait sur 5 services à cause de tests générés désynchronisés du code réel. Correctifs (tests uniquement, aucun code applicatif touché) : **post-service** — `postService_logic_test.go` : les 2 appels `CreatePost(...)` reçoivent le nouvel argument `nsfw bool` (le `go vet` échouait → bloquait toute la step) ; `post_handler_extra_test.go` : `makePostToken` pose `EmailVerified: true` (sinon le middleware `VerifiedOnly` renvoie 403 au lieu du 400 attendu pour CreatePost/CreateComment JSON invalide). **auth-service** — `oauth_extra_test.go` : suppression des 3 tests Spotify/Facebook (providers absents de `defs`, qui ne contient que `google`+`github`) qui provoquaient nil-pointer/erreurs. **message-service** — `conversation_handler_extra_test.go` : `makeMsgToken` pose `EmailVerified: true` (403→400 sur Create/Send), et les tests `*_NilRepo_500` sur routes `:id` utilisent un ObjectID valide (`507f1f77...`) pour franchir la validation d'ID et atteindre le repo nil → panic récupérée → 500 (au lieu de 400). **user-service** — `handlers_extra_test.go` : `TestGetByID_NilRepo_500` utilise un UUID valide (GetByID renvoie 404 pour un id non-UUID) ; `router_test.go` : `GET /users` déplacé de la liste publique vers la liste protégée (la route `List` exige désormais `auth`). **notification-service** — `notificationService_extra_test.go` : `_ = recover()` et `_, _ = svc.List(...)` pour satisfaire `errcheck`. Vérif : `go vet ./...` + `go test -race ./...` OK sur user/auth/profil/post/message/notification ; `gofmt -l` vide sur les 7 fichiers édités.*

*Last updated: 19/06/2026 — fix(frontend) : **colonne de droite décalée/coupée à l'ouverture des filtres explorer (branche #305)**. Symptôme : lancer une recherche dans l'explorateur fait apparaître la carte de filtres dans la colonne de droite, qui repoussait « Tendances » et « Qui suivre » sous le pli (dernier user coupé, bas de « Tendances » coupé). Cause : la carte `ExplorerFilterCard` était un gros encart (titre `text-lg` + sous-titre + 2 lignes à puces ≈ 150 px) inséré entre la recherche et « Tendances », et l'`aside` (`sidebar-right.tsx`) était épinglé en `h-screen overflow-y-auto` → scroll interne qui ne s'active qu'au survol de la colonne, donc bas « coupé ». **Décision utilisateur : les deux correctifs.** (1) **Carte filtres compacte** (`explorer-filter-controls.tsx`) : petit libellé `text-sm` + réutilisation des pastilles toggle `MobileFilterButtons` (qui gèrent déjà le toast « au moins un filtre ») → ~88 px au lieu de ~150. Suppression du code mort `FilterControls`/`FilterRow`/`CircleIndicator` ; commentaire de `MobileFilterButtons` mis à jour (désormais partagé mobile + carte desktop). Aucune nouvelle clé i18n (réutilise `explorer.filter_title`/`filter_publications`/`filter_users`). (2) **Scroll colonne revu** (`sidebar-right.tsx`) : `h-screen` → `max-h-screen` (la colonne ne fait que la hauteur de son contenu, plafonnée à l'écran) + `overscroll-contain` → la colonne n'est plus forcée pleine hauteur, le scroll interne ne se déclenche que si ça déborde vraiment. Front-only, aucun contrat back/route/Swagger. ⚠️ `tsc`/`lint` non lançables en local (node_modules root-owned) → à valider via `make dev`/CI.*

*Last updated: 19/06/2026 — fix(ci/media-service) : **golangci-lint vert sur le handler GIF**. `unused` cassait la job Go sur `giphyListResponse` dans `media-service/internal/handler/media_handler.go` : le type n'était référencé que par Swagger, jamais par le runtime. Correctif : `SearchGiphy` renvoie maintenant `giphyListResponse{Data: ...}` sur tous les chemins (fallback sans clé, fallback sur erreur GIPHY, succès nominal). Contrat JSON inchangé, linter satisfait, Swagger et code réalignés.*

*Last updated: 19/06/2026 — fix(ci/media-service) : **job Go réparée après rebase**. La branche rebased sur `origin/develop` laissait `media-service` dans un état non formaté pour Go ; la step CI `gofmt -l .` aurait cassé sur 14 fichiers (`main.go`, handlers, middleware, imageproc, validate, etc.). Reformatage appliqué via `gofmt -w` sur tout le service. Vérif : `gofmt -l .` → vide ; `docker run --rm -v "${PWD}\\media-service:/app" -w /app golang:1.25-alpine go test ./...` → OK. Les checks `go vet`/`go build` n'ont pas pu être confirmés localement hors conteneur à cause du cache Go Windows verrouillé (`C:\Users\Maxime\AppData\Local\go-build` non accessible), mais rien d'autre que le formatage n'a été reproduit comme échec CI.*

*Last updated: 19/06/2026 — tweak(gifs) : **catalogue fallback étendu à 24 GIFs**. Le picker fonctionnait mais la grille était trop pauvre (6-8 éléments visibles selon la requête). `frontend/src/lib/giphy.ts` demande désormais 24 résultats par défaut et le catalogue fallback du media-service contient assez d'entrées pour remplir la grille (réactions : applaudissements, fête, facepalm, amour, café, travail, etc.). Service média redémarré. Vérif gateway authentifiée : `/gifs/search?limit=24` → `ms=75`, `count=24`. `media-service go test ./...` OK.*

*Last updated: 19/06/2026 — fix(gifs) : **fallback GIF immédiat avec la clé publique de dev**. Après le fallback, le picker restait visuellement en chargement car le backend tentait encore GIPHY avec la clé publique `dc6zaTOxFJmzC`, refusée en `403`, puis ne renvoyait les GIFs qu'après ~6-7s. `SearchGiphy` court-circuite désormais cette clé publique connue et renvoie directement le catalogue fallback ; une vraie clé GIPHY continue d'utiliser l'API officielle. Vérif gateway authentifiée : `/gifs/search?limit=6` → `ms=93`, `count=6`, premier GIF `Cat typing`. `media-service go test ./...` OK.*

*Last updated: 19/06/2026 — fix(gifs) : **picker GIF fonctionnel sans vraie clé GIPHY**. Diagnostic : la route `/gifs/search` était bien routée et `GIPHY_API_KEY` chargée dans `media-service/.env`, mais la clé publique de dev testée (`dc6zaTOxFJmzC`) est refusée par GIPHY (`403`) depuis le conteneur ; auparavant le picker affichait donc « Impossible de charger les GIFs ». Correctif : `SearchGiphy` renvoie désormais un fallback de GIFs publics GIPHY (`media.giphy.com`) si la clé est absente, si GIPHY timeout/refuse, ou si la réponse est vide ; avec une vraie clé GIPHY, l'API officielle reste utilisée en priorité. Timeout HTTP GIPHY passé de 5s à 15s pour éviter les faux négatifs réseau Docker. `.env` local `media-service/.env` renseigné avec la clé de dev, `media-service` recréé avec l'override dev puis redémarré. Vérifs : appel gateway authentifié `/gifs/search?limit=3&q=chat` → `count=3`, premier GIF `Cat typing`; `media-service go test ./...` OK.*

*Last updated: 19/06/2026 — feat(gifs) : **picker GIPHY dans le composer de post**. Architecture validée avant code : clé GIPHY côté serveur, pas exposée au navigateur. **Backend/media-service** : config optionnelle `GIPHY_API_KEY`, proxy JWT `GET /gifs/search?q=&limit=` (recherche si `q`, tendances sinon), timeout 5s, réponse normalisée `{id,title,url,preview_url,width,height}`, annotations swaggo. **Gateway** : préfixe `/gifs` routé vers `MEDIA_SERVICE_URL` pour éviter le conflit avec `/media/:id/:variant`. **Frontend** : nouveau client `lib/giphy.ts`, bouton `GIF` ajouté dans `post-composer` entre photo et épingle, popover avec champ recherche + grille, sélection ajoutée comme média image externe `{url,type:'image'}` et previews existantes réutilisées. i18n 12 langues (`composer.add_gif`, recherche/empty/error/added). `.env.example` media-service documente `GIPHY_API_KEY`. **Swagger** : `make swagger` Windows échoue toujours à lancer `bash`; doc régénérée via Docker Alpine + `swag` installé dans `/root/go/bin` + copie temporaire CRLF→LF du script, `doc/openapi.{json,yaml}` expose `/gifs/search`. Vérifs : `npx tsc --noEmit` OK ; `go test ./...` OK sur `media-service` et `api-gateway` avec `GOCACHE` local.*

*Last updated: 19/06/2026 — fix(frontend) : **header notifications traversé par la liste au scroll (desktop, branche #288)**. Symptôme (prod, ≥ lg uniquement) : en scrollant la page Notifications, le corps (avatars, noms, boutons) passait **par-dessus** l'en-tête `sticky`. Cause : un **conflit de z-index à égalité** — l'en-tête desktop était `sticky top-0 z-10` (`notifications-view.tsx`) alors que plusieurs éléments de la liste portent `relative z-10` (avatars, liens nom, boutons follow-request, nécessaires pour passer au-dessus du `<Link absolute inset-0>` étiré de chaque ligne) ; à z-index égal, l'ordre DOM tranche → les éléments de liste (plus bas) se peignaient sur l'en-tête. Le fond `--panel` étant **opaque** (hex sans alpha), ce n'était pas un souci de transparence. **Correctif** : en-tête passé `z-10` → **`z-20`** (1 ligne), au-dessus des `z-10` internes et toujours sous l'overlay parent `z-40`. Mobile non concerné (header desktop masqué `hidden lg:block`, titre porté par l'en-tête global). Front-only, aucune route/clé i18n/Swagger. ⚠️ `tsc`/`lint` non lançables en local (node_modules root-owned) → valider via `make dev`/CI.*

*Last updated: 19/06/2026 — feat(frontend) : **gestion des utilisateurs bloqués dans Paramètres + filtre popup realtime feed**. Suite du blocage utilisateur. **Paramètres › Généraux** : nouveau bloc `Utilisateurs bloqués` (`BlockedUsersSettings`) qui charge `GET /users/me/blocks`, enrichit les ids via `getUserById` (user + profil), affiche avatar/nom/@ cliquables vers le profil, et propose un bouton `Débloquer` (`DELETE /users/:id/block`) avec mise à jour locale + événement global `breezy:block-change`. **Feed realtime** : `FeedView` charge aussi les ids bloqués dans `blockedIdsRef` et écoute `BLOCK_CHANGE_EVENT`; le callback WebSocket `/posts/ws` ignore désormais tout ping dont `authorId` est bloqué, donc la pastille “a posté” n’apparaît plus pour un compte bloqué. Si un blocage arrive alors qu’un ping est déjà présent, les pings/posts de cet auteur sont retirés localement. **i18n** : nouvelles clés `block.settings_*`, `block.empty`, `block.unblock_short`, `block.list_failed` ajoutées aux 12 langues. Aucun changement backend/route → pas de Swagger. Vérif : `npx tsc --noEmit` OK.*

*Last updated: 19/06/2026 — feat(social) : **blocage utilisateur depuis posts et profils + barrière serveur**. **Architecture validée avant code** : le blocage est une relation du graphe social → `user-service` (PG), et l’effet de visibilité est appliqué côté `post-service` (jamais uniquement front). **Back user-service** : table idempotente `blocks(blocker_id, blocked_id, created_at)` dans `schema.sql`, routes JWT annotées Swagger `POST /users/:id/block`, `DELETE /users/:id/block`, `GET /users/me/blocks`, route interne `GET /internal/users/:blockerId/has-blocked/:blockedId`; bloquer coupe `follows` et `follow_requests` dans les deux sens, débloquer ne recrée rien. **Post-service** : `FollowClient.HasBlocked` + extension de `canReadAuthor` → feeds, détail post et stats masquent les auteurs bloqués ; `ListCommentsByAuthor` et `ListLikedByUser` retournent vide si le profil consulté est bloqué, donc les onglets Réponses/J’aime ne révèlent plus l’activité. **Front** : menu `...` des posts enrichi avec `Signaler le post` puis `Bloquer l’utilisateur` ; nouveau hook `useBlock` + event `breezy:block-change` ; cartes d’un auteur bloqué disparaissent immédiatement. Profil autre utilisateur : actions déplacées dans un menu `...` (Partager, Message, Signaler, Bloquer) à gauche du bouton de relation ; quand le profil est bloqué, le bouton `Suivre` devient `Débloquer l’utilisateur`, l’activité en ligne est masquée et les onglets Posts/Réponses/J’aime affichent `Utilisateur bloqué`. Déblocage = retour aux règles normales (public visible, privé soumis au follow). **i18n** : clés ajoutées dans les 12 langues (`fr/en/zh/es/pt/ru/ja/ko/ar/hi/de/it`), dont `report.post_action` et `block.*`. **Docs** : `ARCHITECTURE.md`, `DECISIONS.md`, `PROJECT_STATUS.md` mis à jour. **Swagger** : `make swagger` local Windows échoue sur `bash scripts/...` (erreur CreateProcess), donc régénération via Docker Alpine + `swag` avec copie CRLF→LF temporaire et lien `/root/go/bin/swag` → `doc/openapi.{json,yaml}` mis à jour. Vérifs : `go test . ./internal/...` OK (`user-service`, `post-service`, avec `GOCACHE` local), `npx tsc --noEmit` OK.*

*Last updated: 19/06/2026 — chore(auth/oauth) : **retrait propre de Facebook & Spotify des connexions sociales** (branche #287). Sur demande de l’utilisateur, on ne garde que **Google (OIDC) + GitHub (OAuth2)**. **auth-service** : suppression des deux entrées `defs` `facebook`/`spotify` (`internal/oauth/oauth.go`) ; map `Providers` de `main.go` réduite à google/github ; champs `FacebookClientID/Secret` + `SpotifyClientID/Secret` et leurs `os.Getenv` retirés de `config.go` ; commentaires d’en-tête (`oauth.go`, `config.go`, modèle `user.go` → `’local’ | ‘google’ | ‘github’`) mis à jour ; test `oauth_test.go` (le cas « provider inconnu » utilise désormais `”inconnu”` au lieu de `”facebook”`). **Swagger** : `Enums(google, github)` + résumés sur les 3 handlers OAuth + **doc/openapi.{json,yaml} régénérés** (`make swagger`). **Schéma PG** (règle 5b) : les `ALTER TYPE auth_provider ADD VALUE ‘facebook’/’spotify’` retirés de `schema.sql`, mais **on ne `DROP` PAS** les valeurs d’enum (PostgreSQL ne le permet pas ; elles restent inertes sur les bases existantes — commentaire ajouté). **Front** : entrées `facebook`/`spotify` retirées de `OAUTH_PROVIDERS` (`login` & `register`) ; assets `public/facebook.svg`/`spotify.svg` supprimés (callback `[provider]`/BFF déjà génériques → rien d’autre). **`.env.example`** auth nettoyé des 4 vars FB/Spotify (l’utilisateur retire les vrais secrets/ID de ses `.env`). Docs : `DECISIONS.md` (§OAuth), `PROJECT_STATUS.md`. Vérifs : auth-service `go build ./...` + `go test ./...` **OK** ; swagger régénéré sans référence FB/Spotify ; aucune réf Go orpheline (grep). ⚠️ front non vérifiable en local (node_modules root-owned) → à valider via `make dev`/CI.*

*Last updated: 19/06/2026 — fix(frontend) : **menu image natif sur mobile, menu Breezy au clic droit desktop uniquement**. Symptôme (iPhone) : appui long sur l'image d'un post → le menu natif iOS **et** le menu custom Breezy s'affichaient ; « Enregistrer l'image » / « Copier l'image » de Breezy → toast rouge d'échec (la technique `<a download>` + `a.click()` et `navigator.clipboard.write` sont **ignorées / non fiables sur Safari iOS**), alors que le menu natif, lui, fonctionne. **Décision (avec l'utilisateur)** : sur **tactile** on rend la main au **menu natif du navigateur** (appui long iOS/Android) ; le menu custom Breezy (Copier / Enregistrer) ne sert plus qu'au **clic droit souris (desktop)**, où `<a download>` et le presse-papiers sont fiables. **Impl** (`post-card.tsx`/`MediaGallery`) : suppression de l'**appui long tactile** (timer 520 ms `startImageLongPress`/`clearImageLongPress` + refs `imageLongPressTimer`/`imageLongPressed`) ; le menu s'ouvre désormais via **`onContextMenu`** sur le conteneur. Une ref `lastPointerType` (posée sur `onPointerDown`) garde le dernier type de pointeur → `handleImageContextMenu` ouvre le menu Breezy **seulement si `lastPointerType === 'mouse'`**, sinon il **laisse le menu natif** s'afficher (pas de `preventDefault`). Retrait des bidouilles anti-natif sur l'`<img>` (`select-none`, `[-webkit-touch-callout:none]`, `draggable={false}`) → le callout iOS « Enregistrer l'image » revient. `handleImageClick` simplifié (`onOpen?.(i)` direct, plus de garde anti-longpress). `downloadImage`/`saveImage` revenus à leur forme simple `<a download>` (la Web Share API testée au tour précédent est inutile puisque le custom est desktop-only). Le `ImageActionMenu` (clic droit) conserve son `onPointerDown stopPropagation` → les clics d'action survivent au listener de fermeture global. Front-only, aucun contrat back / route / clé i18n. ⚠️ `tsc`/`lint` non lançables en local (node_modules root-owned) → **à valider sur iPhone réel** (appui long = menu natif seul, enregistrement OK) **et desktop** (clic droit = menu Breezy) via `make dev`/CI.*

*Last updated: 18/06/2026 — **sécurité : pentest complet + durcissement (riveta re-testé + audit boîte blanche)**. Rapport interne `PENTEST.md` (gitignoré, comme `/riveta/`). Re-test boîte blanche des 8 findings riveta + audit approfondi (SQLi/NoSQLi/XSS/JS/CSRF/path-traversal/SSRF/IDOR/JWT/upload/secrets/MFA → cœur **solide**). Correctifs appliqués : **RIV-001** rate-limiting par IP (`auth-service/internal/middleware/ratelimit.go`, en mémoire sans dépendance) sur login/register/forgot/resend/mfa-verify ; **BRZ-001** (nouveau, MFA post-riveta) challenge MFA brûlé après 5 codes faux (`mfa.go`, anti brute-force du TOTP même distribué) ; **RIV-002** claim `email_verified` propagé sur **tous** les chemins d'émission de token (register/login/verify/**refresh**/**oauth**) + middleware `VerifiedOnly` gardant la création de **posts/commentaires/conversations/messages** (un token d'inscription non vérifié ne peut plus publier) ; **RIV-003** `GET /users` (annuaire complet) passe authentifié (le front ne l'utilise pas) ; **RIV-004** en-têtes de sécurité Caddy (HSTS/X-Frame-Options/nosniff/Referrer-Policy/Permissions-Policy + CSP **Report-Only** d'abord à cause du script de thème inline) ; **RIV-007** `poweredByHeader:false` + `-X-Powered-By`/`-Server` Caddy + validation UUID sur `GET /users/:id` (404 au lieu de 500) ; **BRZ-004** `nosniff`+`Content-Disposition` sur le download média ; **BRZ-006** throttle IP sur `/api/translate`. Différés/atténués : RIV-005 (atténué par throttle register), RIV-006 (fil public assumé), BRZ-003 (ticket WS — couplé front), BRZ-005 (state OAuth serveur — à confirmer). **BRZ-002 écarté** : la prod utilise un vrai `JWT_SECRET` hex (≠ placeholder). Build OK sur les 9 services Go ; tests auth/user/post/message verts. Détails + PoCs dans `PENTEST.md`.*

*Last updated: 16/06/2026 — feat(frontend) : **sons contextuels Breezy**. Ajout de `frontend/src/lib/sounds.ts` (Web Audio API, 3 signatures distinctes, best-effort si le navigateur bloque l'audio, cooldown anti-rafale, armement sur première interaction utilisateur). `breeze_posted` lit `frontend/public/sounds/breeze_posted.mp3` ; `message_received` et `notification_received` lisent `frontend/public/sounds/message_received.mp3`, avec fallback synthétique si la lecture échoue. Branchements : `PostComposer` joue `breeze_posted` après création/pin réussis du breeze local ; `NotificationsProvider` joue `notification_received` seulement pour une notification WS non lue nouvellement comptée ; `MessagesProvider` joue `message_received` pour un message WS d'autrui (jamais pour ses propres envois). Front-only, aucun contrat backend/Swagger. Vérifs : `npm run lint` OK ; `npx tsc --noEmit` bloqué par une erreur existante dans `src/lib/post-stats.test.ts` (`FeedPost.poll` potentiellement `undefined`) ; `vitest` ne démarre pas car un `package.json` parent lu par esbuild est vide/invalide.*

*Last updated: 16/06/2026 — docs(workflow) : préférence utilisateur ajoutée dans `CLAUDE.md` : relire ce fichier avant chaque réponse et maintenir les docs pertinentes à jour après chaque réponse/unité de travail. Aucun changement produit.*

*Last updated: 18/06/2026 — chore(ci) : **couverture de tests via Codecov (backend Go + frontend)**. **Backend** (`ci-go.yml`) : le step test passe à `go test -race -covermode=atomic -coverprofile=coverage.out ./...` ; nouveau step `codecov/codecov-action@v5` après les tests, **un flag par service** (`flags: ${{ matrix.service }}`) → lecture couverture service-par-service dans Codecov ; `fail_ci_if_error: false` (un hoquet Codecov ne casse jamais la CI). **Frontend** (`ci-frontend.yml`) : `npm test -- --coverage` + upload `codecov-action@v5` (`flags: frontend`, fichier `frontend/coverage/lcov.info`). **vitest.config.ts** : bloc `coverage` (provider `v8`, reporters `text`+`lcov`, `include: ['src/lib/**']` = couche technique réellement testée, exclut tests/`.d.ts`). Nouvelle devDep `@vitest/coverage-v8@^2.1.8` (alignée sur vitest 2.1.8). **`codecov.yml`** racine : targets souples non bloquants (`informational: true`, `threshold 1%`) au démarrage, `carryforward: true` sur les flags (les filtres `paths` de la CI font que tous les jobs ne tournent pas à chaque PR), commentaire PR `require_changes`, `ignore` du généré (`**/*_test.go`, `doc/`, `frontend/src/components/ui/**` shadcn, `*.test.ts`). **Badge** Codecov ajouté en tête du `README.md`. **Action manuelle requise de l'utilisateur** : (1) lier le repo public `Gosyfrone/WebDad` sur codecov.io et coller le `CODECOV_TOKEN` dans les secrets GitHub du repo (upload tokenless possible en public mais token recommandé) ; (2) régénérer le `package-lock.json` (`sudo rm -rf frontend/node_modules && cd frontend && npm install -D @vitest/coverage-v8@^2.1.8`) — non faisable par Claude (node_modules root-owned, artefact bind-mount Docker), **indispensable sinon le `npm ci` de la CI échoue sur un lock désynchronisé**. Aucune logique applicative modifiée (CI + config only).*

*Last updated: 18/06/2026 — refactor(frontend) : **store de l'utilisateur connecté + simplification (branche #252)**. **(1) Store** : nouveau `CurrentUserProvider` (`components/current-user-provider.tsx`, Context React — pas de zustand, cohérent avec les providers existants) monté en tête de `(app)/layout.tsx`. Il fait **un seul** `getMyProfil()` + `getMe()` au montage (au lieu de 3-4 appels parallèles redondants : sidebar + header + composer faisaient chacun leur `getMyProfil` = 6 requêtes au boot), écoute `SESSION_CHANGED` (via `useSession`, **une seule** instance désormais au lieu de 10+) et `subscribeProfilUpdated` (MAJ sans refetch), et expose `useCurrentUser()` → `{ session, profil, usernamePending, preferredLocale, isAdmin, isModerator, isLoading, refresh }`. Gère le visiteur (null). **(2) Migration** des consommateurs vers `useCurrentUser()` : `sidebar-left`, `mobile-header`, `post-composer` (profil/avatar), `feed-view`/`muted-words`/`use-follow` (id via `currentUserId()` sync au lieu de `getMe()` async), `admin-view`/`admin-infra`/`moderation-view` (booléens `isAdmin`/`isModerator`), `accounts-panel`/`ticket-detail`/`user-account-settings`/`password-change-gate`/`username-pending-gate`. `profil-view` laissé tel quel (charge son profil seulement sur sa propre route, déjà resynchronisé). **(3) Dédoublonnage** : `currentUserId()` était défini **3×** (`session.ts`, `posts.ts`, `messages.ts`) → les deux copies réexportent désormais celle de `session.ts`. **(4) Lisibilité** : helper `getMyPostStates()` dans `posts.ts` (remplace le `Promise.all([getLikedIds,getRepostedIds,getBookmarkedIds])` répété **8×**) ; hook `usePollDraft` (`lib/use-poll-draft.ts`, sort ~80 lignes d'état sondage de `post-composer`) ; hook générique `useLongPress` (`lib/use-long-press.ts`, remplace les refs/timer d'appui long du signet dans `post-card`). **(5) Commentaires** : ~12 gros blocs verbeux condensés (posts.ts, layout (app), post-card, post-composer, feed-view, chat-pane, onboarding-gate) en gardant les vrais « pourquoi » (iOS, hydratation, E2EE, barrière de visibilité) ; commentaire **périmé** retiré (`profil-view` prétendait que les onglets Réponses/J'aime étaient des placeholders alors qu'ils sont implémentés). **Reporté** (suite recommandée) : découpage en **fichiers** de `chat-pane.tsx` (1207 l) — réorganisation cosmétique de code E2EE sensible, non vérifiable sans build → écarté ce tour pour ne pas risquer une régression invisible. Vérifs : revue manuelle + greps (ré-exports cohérents, aucune référence orpheline, `useSession` n'est plus instancié que dans le provider). ⚠️ `tsc`/`lint`/`test` **non lançables en local** (`node_modules` root-owned, `npm install` rejeté par l'OS — contrainte connue) → **à valider via `make dev`/CI**.*

*Last updated: 18/06/2026 — feat(auth/mfa) : **double authentification TOTP optionnelle (opt-in), back + front**. Scope volontairement **TOTP seul** (pas de codes de secours, tranché avec l'utilisateur). **auth-service** : 2 colonnes `credentials.mfa_enabled` (BOOL défaut false → rétro-compatible, aucun backfill) + `mfa_secret` (TEXT, secret TOTP **chiffré AES-256-GCM** at-rest) ; le challenge post-mot-de-passe réutilise `account_tokens` avec un nouveau `purpose='mfa_challenge'` (TTL 5 min, CHECK étendu idempotemment). Nouveau `internal/services/mfacrypto.go` (cipher AES-256-GCM, clé `MFA_ENCRYPTION_KEY` base64 32o, **pattern « nil = MFA off »** comme le mailer → endpoints 503 si clé absente) et `internal/services/mfa.go` (`SetupMFA` génère le secret via `pquerna/otp` + **QR PNG côté serveur** `key.Image()` renvoyé en data URI → zéro dépendance front ; `EnableMFA` valide le 1ᵉʳ code puis `mfa_enabled=true` ; `DisableMFA` accepte **code TOTP OU mot de passe** ; `VerifyMFA` lit le challenge **sans le consommer**, valide le TOTP, **puis** consomme dans une transaction → un code faux ne brûle pas le challenge ; `MFAStatus`). Login refactoré : `Login`/`LoginByUserID` renvoient un `LoginOutcome` → si `mfa_enabled`, **aucun JWT**, on émet `{mfa_required, challenge}`. Routes `POST /auth/mfa/{setup,enable,disable,verify}` + `GET /auth/mfa/status` (verify est **publique**, le challenge tient lieu d'auth). `services.New` prend la clé MFA (erreur boot si clé présente mais invalide). `docker-compose.yml` passe `MFA_ENCRYPTION_KEY` (racine) au conteneur. Tests Go : round-trip chiffrement, nonce unique, mauvaise clé/altération GCM, validation TOTP (skew ±1, espaces tolérés). Swagger régénéré (5 routes). **frontend** : `lib/mfa.ts` (client sur `apiFetch`) ; `components/settings/mfa-settings.tsx` inséré **au-dessus du bloc mot de passe** (`UserAccountSettings`) → **interrupteur on/off style iOS** (même markup que `visibility-settings`) : off→on ouvre le panneau QR + clé manuelle + champ code, on→off ouvre la confirmation (code ou mot de passe), re-cliquer pendant un flux = annuler ; l'interrupteur ne bascule réellement qu'après confirmation. BFF : `app/api/auth/login/route.ts` relaie `mfa_required`/`challenge` **sans** poser de cookie ni provisionner ; nouvelle route `app/api/auth/mfa/verify/route.ts` (pose le cookie refresh + provisionne, comme le login). Écran de **challenge au login** (`(auth)/login/page.tsx`) : champ code 6 chiffres → `/api/auth/mfa/verify` → `setAccessToken` + redirection feed ; `invalid_challenge` → retour à l'écran mot de passe. i18n FR/EN (`settings.mfa.*`, `auth.mfa.*` ; 10 autres locales → repli FR). **Limites assumées** : MFA appliquée au **login par mot de passe** uniquement (OAuth = 2FA du provider) ; **pas de codes de secours** → perte du téléphone = verrouillage (à lever plus tard). Vérifs : `go build`/`go vet`/`gofmt`/`go test` **OK**, `tsc --noEmit --incremental false` **0 erreur**, `eslint` **OK** (warnings pré-existants non liés), `vitest` **160/160**. ⚠️ Reste l'e2e `make dev` + scan Microsoft Authenticator pour passer 🟢.*

*Last updated: 18/06/2026 — feat(share) : **bouton Partager fonctionnel sur les posts et les profils (branche #258)**. Le bouton Partager du post existait mais était **inerte** (aucun `onClick` dans `post-actions.tsx`). Nouveau composant réutilisable **`ShareDialog`** (`components/share/share-dialog.tsx`) piloté en `open/onOpenChange`, props `{ url (chemin relatif), kind: 'post'|'profile', title }`, avec **3 voies** : (1) **message privé** — réutilise `UserSearch` (recherche d'une personne) → `startDM` + `sendMessage` (le lien voyage **en clair DANS le message chiffré E2EE**, aucune modif du format de message) ; gère `PeerKeyMissingError`, `IdentityLockedError` (toast → `messages.passphrase.unlock_title`) et l'échec générique ; visiteur non connecté → invite `promptLogin` au lieu de la recherche ; (2) **partage natif** — `navigator.share()` affiché seulement si dispo (détection au montage via `useEffect` pour éviter un mismatch SSR) ; (3) **copier le lien** — presse-papiers + feedback ✓. Helpers purs dans **`lib/share.ts`** (`absoluteUrl` = `window.location.origin` + chemin, `canNativeShare`, `nativeShare`, `copyLink`). **Câblage** : `post-actions.tsx` (bouton Share → `ShareDialog` avec `postHref(post.id)`, donc carte du fil **et** vue photo couvertes d'un coup) ; `profil-header.tsx` (nouveau bouton icône `Share` **à gauche** du bouton Mail, branche `canFollow`, → `ShareDialog` avec `profilHref(profil.username)`). **Liens cliquables dans le chat** : `MentionMessageText` (`mention/mention-text.tsx`) linkifie désormais les URLs http(s) des segments texte — URL de **notre origine** (post/profil partagé) = navigation SPA `<Link>` (chemin relatif), sinon `<a target="_blank" rel="noopener noreferrer">` ; ponctuation finale collée rejetée hors du lien. i18n FR/EN : groupe `share.*` (`title`, `post_desc`/`profile_desc`, `send_dm`(+`_hint`), `via`, `copy`/`copied`, `sent`, `send_failed`, `login_required`, `post_message_prefix`/`profile_message_prefix`) — les 10 autres locales retombent sur FR. **Aucune modif backend** (partage MP = envoi de message existant), pas d'annotation swagger (front-only). Vérifs : `npx tsc --noEmit --incremental false` **OK (0 erreur)**, `eslint` sur les 5 fichiers touchés **OK**.*

*Last updated: 18/06/2026 — feat(share) suite : **aperçus enrichis en DM + classement des destinataires récents + correctif bouton feed (branche #258)**. **(1) Correctif majeur** : le bouton Partager **du fil** ne faisait rien — la carte du fil (`post-card.tsx`) possède sa **propre** barre d'actions (PostActions n'est utilisé que par la vue photo), son bouton Share était resté inerte. Câblé au `ShareDialog` (même `postHref(post.id)`). **(2) Aperçu enrichi dans le chat** : nouveau composant `components/share/shared-link-preview.tsx` + helper pur `extractSharedRef(text)` (`lib/share.ts`, reconnaît `/posts/<id>` ou `/profil/<handle>` dans n'importe quelle URL du message). Le `chat-pane` rend, **sous la bulle**, une carte cliquable (SPA) : **post** = avatar+nom+@+extrait (line-clamp 3) + miniature de la 1ʳᵉ image ; **profil** = photo de profil + nom + @. Données chargées via `getPostById`/`getUserByUsername` avec **cache module-level** (un même lien apparaît souvent plusieurs fois) ; ressource inaccessible (barrière de visibilité) → carte non rendue, le lien en clair reste dans la bulle. **Le format de message E2EE n'est PAS modifié** (le lien voyage en clair dans le chiffré, l'aperçu est reconstruit côté client). **(3) Profil : préfixe retiré** — partager un profil envoie désormais **le lien seul** (plus de « Regarde ce profil : ») ; le post garde son court préfixe. Clé `share.profile_message_prefix` supprimée (FR+EN). **(4) Destinataires récents** : `lib/share.ts` persiste les derniers partages dans `localStorage` (`breezy.share.recent`, dédup, plafond 10, `getRecentShareTargets`/`recordRecentShareTarget`) ; `UserSearch` gagne 2 props optionnelles (`recentUsers`, `recentLabel`) → champ vide = liste des récents, et lors d'une recherche les récents correspondants **remontent en tête** (tri stable par rang de récence). `ShareDialog` enregistre le destinataire après envoi et passe la liste (clé i18n `share.recent`). Usages existants de `UserSearch` (nouveau DM, ajout de membres) inchangés (props par défaut). Vérifs : `tsc --noEmit` **OK (0 erreur)**, `eslint` sur les 6 fichiers **OK** (seule la warning `<img>` pré-existante sur le post cité subsiste ; l'`<img>` de l'aperçu porte un `eslint-disable` ciblé).*

*Last updated: 18/06/2026 — feat(auth/oauth) : **connexion sociale GitHub + Facebook + Spotify (en plus de Google)**. **LinkedIn abandonné** (son app dev exige une Page entreprise rattachée — lourd pour un projet) → **remplacé par GitHub** (OAuth App perso, aucune page requise, orienté dev). Généralisation du flux OAuth (`auth-service/internal/oauth/oauth.go`) en **2 familles** derrière la même interface `AuthURL`/`Exchange` : `kindOIDC` (Google) = discovery + vérif `id_token` ; `kindOAuth2` (GitHub, Facebook, Spotify) = échange code→`access_token` puis appel **userinfo** (`api.github.com/user`, `graph.facebook.com/me?fields=id,email`, `api.spotify.com/v1/me`) mappé en `Identity{Subject,Email}` via `userInfoMap`, **email considéré vérifié** (le provider a authentifié l'utilisateur). **Spécifs GitHub** : (a) scope `user:email` + champ `emailsURL` optionnel = repli `GET /user/emails` quand l'email est privé (`/user` renvoie `email:null`), on retient l'adresse **primaire ET vérifiée** (sinon erreur, pas de provisioning sur email non confirmé) ; (b) `id` GitHub = nombre JSON → helper `idField` ; (c) `User-Agent` posé sur tous les GET userinfo (exigé par `api.github.com`, sinon 403) via le helper `authedGetJSON`. Ajouter un provider = une entrée `defs` + creds. `config.go`/`.env.example` : 6 vars `GITHUB/FACEBOOK/SPOTIFY_CLIENT_ID|SECRET` (provider sans ClientID = ignoré → 404). `main.go` : 3 entrées map. **Schéma PG** : `ALTER TYPE auth_provider ADD VALUE IF NOT EXISTS 'github'|'facebook'|'spotify'` (idempotent, hors transaction via `EnsureSchema` — règle 5b). Swagger `Enums(google,github,facebook,spotify)` sur les 2 handlers + **doc/openapi.{json,yaml} régénérés**. Front : `login` & `register` = **rangée d'icônes `grid-cols-4`** (boutons carrés, icône seule) pilotée par la const `OAUTH_PROVIDERS` ; logos `public/{github,facebook,spotify}.svg` (`linkedin.svg` supprimé) ; spinner `Loader2` sur le provider cliqué ; i18n FR/EN `auth.oauth.continue_with` (interpolation `{provider}`, pas de clé par provider). `handleOAuth`/BFF/callback déjà génériques (param `[provider]`) → aucun changement de logique front. Vérifs : auth-service `go build ./...` + `go vet ./...` + `go test ./internal/oauth/` **OK** ; front non vérifiable en local (node_modules root-owned) → à valider via `make dev`/CI. **Compose** : les 6 creds suivent le même chemin que `GOOGLE_CLIENT_*` → lus depuis `auth-service/.env` via `env_file:`, **aucun bloc `environment:` à toucher**. Seul ajout : `OAUTH_REDIRECT_BASE_URL` câblé dans les 2 compose (dev → `http://localhost:3000` ; prod → retombe sur `${APP_BASE_URL}`, domaine public) car son défaut Go (`localhost:3000`) serait faux en prod. `docker compose config` (base+prod, interpolation imbriquée) **OK**. **⚠️ Credentials à renseigner par l'utilisateur** dans `auth-service/.env` (apps à créer côté consoles GitHub/Meta/Spotify, redirect `http://localhost:3000/auth/callback/<provider>` en local, `<APP_BASE_URL>/auth/callback/<provider>` en prod). Le code marche sans (boutons → 404 tant que les CLIENT_ID sont vides, comme Google aujourd'hui).*

*Last updated: 17/06/2026 — fix(4 correctifs) : **(1) pastille de présence visible PARTOUT, (2) notification au like d'un commentaire, (3) auto-acceptation des demandes au passage public, (4) page messages décalée (feed visible)**. **(1)** La `ActivityPresenceDot` (enfant en `absolute -bottom-0.5 -left-0.5`) était rognée en sliver par l'`overflow-hidden` de `<Avatar>` — sur **tous** les sites, pas que la sidebar. Fix global 1 ligne (`ui/avatar.tsx`) : retrait de `overflow-hidden` du root (l'image et le fallback portent déjà `rounded-full` → avatars toujours ronds, aucun usage non-circulaire vérifié). Position bas-gauche conservée. **(4)** Régression introduite par le squash #250 (develop) : `messages/page.tsx` avait perdu son wrapper `<FeedOverlay wide headerOffset={false}>` (remplacé par un `<div>` brut, commentaire évoquant un FeedOverlay « soft-nav » qui n'existe plus depuis la suppression des `@modal` au refactor #270) → la vue messages devenait un **frère flex** de la colonne principale, laissant le feed visible et décalant l'UI. Wrapper restauré. **(2)** `LikeComment`/`UnlikeComment` (post-service) n'émettaient aucun événement (contrairement à `LikePost`). Nouveau type `comment_like` de bout en bout : `post-service/notifier` (const + émission `RecipientID=comment.AuthorID`, `PostID`, `CommentID`, `Retract` au unlike) ; `notification-service` (const `TypeCommentLike`, ajout à l'enum Mongo `$jsonSchema`, au `switch HandleEvent` et `groupKeyFor` → agrégation `comment_like:<comment_id>`) ; front (`NotificationType`, icône cœur, `describe()`, lien vers post+commentaire dans `notificationHref`). i18n **12 langues** : `notifications.comment_like_one/_other`. Pas de migration (enum imposé à l'écriture seulement, règle 5b) ni de swagger (aucune route/annotation modifiée). **(3)** Visibilité (profil-service/Mongo) ≠ follow_requests (user-service/PG) : le passage privé→public ne déclenchait rien. `user-service` : repo `IncomingFollowRequestFollowerIDs`, service `AcceptAllFollowRequests` (boucle `AcceptFollowRequest` → follow + notifs, tolérant aux erreurs), route interne `POST /internal/users/:ownerId/accept-all-follow-requests` (server-to-server, groupe `/internal` non routé par la gateway, convention existante sans auth). `profil-service` : `UserClient.AcceptAllFollowRequests` + hook **best-effort** dans `Update` quand `current.Visibility==private` ET nouvelle visibilité `public` (échec loggé, ne casse pas le PATCH). `FollowChecker` étendu avec la méthode. **Bug d'infra corrigé au passage** : profil-service n'avait **pas** `USER_SERVICE_URL` dans son bloc `environment` (docker-compose.yml **et** .prod.yml) → son `UserClient` retombait sur `http://localhost:8082` (défaut hors-Docker) → `connection refused` ; l'`is-following` de l'activité était d'ailleurs cassé en silence depuis le début. Var ajoutée + log de l'URL résolue au boot (`main.go`). **Recréer le conteneur profil-service** pour appliquer l'env (air recompile le binaire mais ne recrée pas le conteneur). Vérifs : `go build`/`go vet`/`go test ./internal/...` OK sur post/notification/user/profil. ⚠️ front non vérifiable en local (node_modules root-owned) → à valider via `make dev`/CI.*

*Last updated: 17/06/2026 — feat(posts/comments) : **likes sur les réponses/commentaires, avec animation cœur + auto-refresh**. `post-service` : ajout de `comments.likes_count` (compteur dénormalisé `int32`) + migration idempotente `backfillCommentLikesCount` au boot (règle 5b), nouvelle collection `comment_likes` (index unique `comment_id+user_id`), hydration du transient `liked` sur les lectures de commentaires/replies/réponses profil (JWT optionnel), nouvelles routes `POST/DELETE /posts/{id}/comments/{commentId}/like` et endpoint léger `GET /posts/comments/stats?ids=...` pour polling batch ; suppression de commentaire nettoie désormais aussi les likes en cascade. Swagger annoté dans `comment_handler.go` et **doc/openapi.{json,yaml} régénérés**. Front : `lib/posts.ts` étendu (`likes_count`/`liked` sur `PostComment`, `likeComment`/`unlikeComment`, `getCommentsStats`), `CommentSection`/`CommentThread` affichent un bouton cœur sur les commentaires racine et réponses, avec **animation type like de post**, état optimiste + rollback, invite de login en mode visiteur, et resync périodique des compteurs visibles sans recharger le thread. i18n FR/EN : `comment.like`. Vérifs : frontend `npm run lint` OK, `npm test` OK (**135 tests**), `npx tsc --noEmit` bloqué par des erreurs **préexistantes** dans `.next/types` (routes `@modal`, hors périmètre) ; post-service `go test . ./internal/...` OK ; OpenAPI agrégé vérifié (`/posts/comments/stats`, `/posts/{id}/comments/{commentId}/like` présents).*

*Last updated: 17/06/2026 — feat(profil/user) : **point (.) autorisé dans le username et le display name**. Demande : pouvoir mettre des points dans le nom d'utilisateur et le nom d'affichage. **Username** (handle = URL + cible de mention) → point **interne uniquement**, jamais en début/fin ni doublé (`jean.dupont` OK, `.jean`/`jean.`/`jean..x` refusés). `user-service` : `usernamePattern` passe à `^[a-zA-Z0-9_]+(?:\.[a-zA-Z0-9_]+)*$` (la longueur 3-50 est contrôlée à part, RE2 sans lookahead) ; message `ErrInvalidUsername` mis à jour. Front (4 copies du `usernamePattern`, `register`/`onboarding-gate`/`username-pending-gate`/`admin/create-account-dialog`) → `/^(?=.{3,24}$)[a-zA-Z0-9_]+(?:\.[a-zA-Z0-9_]+)*$/` (lookahead JS pour la longueur). **Display name** (texte libre) → simple ajout de `.` au charset, **sans** contrainte de placement (« J.R.R. », « Jr. » valides) : `profil-service/validDisplayName` (`|| r == '.'`) + `frontend/lib/display-name.ts` (`/^[\p{L}\p{M}\p{N} ._-]+$/u`) ; message `ErrInvalidDisplayName` mis à jour. **Mentions** (pour que `@jean.dupont` soit cliquable/notifié) : regex handle `(\w{3,50})` → `(\w[\w.]+\w)` dans `post-service/internal/notifier/notifier.go` ET `frontend/lib/mentions.ts` — premier et dernier caractère = `\w` (point final de ponctuation exclu : `@bob.` → `bob`), minimum 3 conservé ; `detectMentionTyping` passe à `([\w.]{0,50})` pour suivre la frappe `@jean.dup`. Un handle inexistant (ex. `jean..x`) est simplement non résolu en aval. **i18n** : hints FR/EN `auth.register.err.username_format`, `onboarding.username_hint`, `editprofil.name_invalid` mentionnent désormais le point. **Tests** : `validation_test.go` (point interne OK ; `.jean`/`jean.`/`jean..x`/`...` KO), `plan_update_test.go` (`Jean.Dupont`/`J.R.R. Tolkien`/`Jr.` déplacés en valides), `notifier_test.go` (point interne capturé, point final exclu), `mentions.test.ts` (idem). Vérifs : `go test` user/profil/post **OK** ; `vitest mentions.test.ts` **12 passed**. Pas de changement de route/handler → Swagger non requis. Note : aucun cooldown à lever sur @zeto_admin (local) — `username_changed_at` déjà NULL, renommage possible immédiatement. **+ champ « Nom d'utilisateur » dans l'édition de profil** : `EditProfilDialog` reçoit `initialUsername` + `onSaveUsername` ; nouveau champ `@` en tête du formulaire avec vérif de dispo débouncée (`/api/users/check-username`, indicateur checking/dispo/pris), validation pattern (point inclus) + mots réservés + longueur 3-24, erreurs via `auth.register.err.username_*` et nouvelles clés `editprofil.username_{label,hint,cooldown,error}` (FR/EN). À l'enregistrement : champs profil d'abord (`PATCH /profils/me`), puis si le handle a changé `updateMyUsername` (nouveau `lib/api.ts`, `PATCH /users/me`, mappe 409/429/400 → résultat typé `UsernameUpdateResult`) ; **succès → `window.location.reload()`** (réhydrate header, sidebar `@username`, caches mentions — uniquement si changé). `profil-view.handleEditUsername` orchestre, `profil-header` relaie les props. Vérifs : `tsc --noEmit` (aucune erreur sur les fichiers touchés ; seul le test pré-existant `post-stats.test.ts` remonte, hors périmètre), `eslint` OK. Cooldown serveur désactivé par défaut (`USERNAME_CHANGE_COOLDOWN` non défini) mais 429 géré.*

*Last updated: 17/06/2026 — feat(posts/poll) : **images par choix de sondage (branche #249)**. L'encadré image à gauche de chaque choix dans le composer était **100 % décoratif** (rien de branché). Désormais **cliquable → upload d'une image par choix**, optionnelle (choix mixtes autorisés : certains illustrés, d'autres texte seul). **Backend post-service** : `PollChoice` + `ImageURL string` (`bson:"image_url,omitempty"`) ; `CreatePollRequest.Choices` passe de `[]string` à `[]CreatePollChoiceRequest{ Label, ImageURL }` (label requis 1-80, image optionnelle ≤512) ; `buildPoll` trim + recopie l'`image_url`. **DB** : `image_url` ajouté (string optionnel, maxLength 512) au validateur `$jsonSchema` du choix de sondage — **pas de migration** (champ non requis, `additionalProperties` déjà permissif → vieux sondages restent valides, cf. Règle 5b). Le post-service reste **agnostique du contenu** : il ne stocke que le chemin relatif `/media/<id>` servi par le media-service via la gateway. **Front composer** : `pollChoices` passe de `string[]` à `{ label, imageUrl }[]` ; nouveau composant `PollChoiceRow` (input file caché + encadré cliquable, miniature + bouton retirer, spinner pendant l'upload) ; upload via le client média existant **avec cap 5 Mo** (`exceedsMediaLimit`, toast `media.too_large`, admins exemptés) ; `buildPollPayload` envoie `{ label, image_url? }` (image_url omis si absent). **Front rendu** (`PostPollCard`) : **vignette 32 px à gauche du libellé** quand le choix a une image (mode résultats comme vote). i18n : `composer.poll_choice_image` / `composer.poll_choice_image_remove` (FR/EN, repli FR pour les 10 autres locales). Vérifs : post-service `go build`/`go vet`/`go test ./internal/...` OK ; `make swagger` régénéré (`CreatePollChoiceRequest` + `image_url` dans `doc/openapi.{json,yaml}`). ⚠️ `tsc`/`lint` front non vérifiables en local (node_modules root-owned) → à valider via `make dev`/CI.*

*Last updated: 17/06/2026 — fix(follow) : **« En attente » affiché partout pour une demande sur compte privé**. Le suivi d'un compte privé renvoie `follow()` → `'pending'` et `useFollow` suivait déjà l'état via `isRequested(id)` (clé i18n `follow.requested` = « En attente », déjà présente dans les 12 langues). Mais les rendus **compacts** ignoraient `isRequested` : après une demande, `isFollowing` repassant à `false`, le bouton réaffichait « Suivre ». Câblage de `isRequested` dans les composants concernés : `UserListItem` (nouvelle prop `isRequested` → libellé « En attente » + bouton désactivé, calqué sur le `FollowButton` de `profil-header`), utilisé par **« Qui suivre »** (`who-to-follow.tsx`) et la **modale relations** (`relations-dialog.tsx`) ; côté **Explorer** (`explorer-view.tsx`), `isRequested` propagé dans `SubmittedSearchResults`→`ExplorerUserRow` (recherche) et `SuggestionsSection`→`SuggestionCard` (suggestions). En plus du label, les **suggestions** excluent désormais les comptes déjà en attente **au chargement** (comme les comptes déjà suivis) : `who-to-follow` ajoute `!isRequested(u.id)` à son filtre initial, et Explorer récupère `getPendingFollowRequestIds()` dans son `Promise.all` pour filtrer `nextSuggestions`. La liste étant figée après le 1er rendu, un compte sur lequel on agit **en cours de session** reste affiché en « En attente » ; recherche et modales montrent aussi « En attente » (pas d'exclusion). Front-only, aucune route/contrat backend touché → Swagger non requis. ⚠️ `tsc`/`lint` non lançables en local (`node_modules` root-owned, bind-mount Docker) → à valider via `make dev`/CI.*

*Last updated: 17/06/2026 — feat(ux/mobile) : **refonte de l'en-tête mobile contextuel (branche #273)**. Objectif : un en-tête mobile unique, cohérent sur les pages de navigation principale, avec accès aux notifications déplacé en haut à droite. **`MobileHeader` devient contextuel** selon la route : gauche = avatar (→ tiroir sidebar) ou **flèche ← retour** sur `/notifications` (`router.back()`, repli `/feed`) ; centre = logo Breezy (feed) ou titre de section (« Explorer » / « Messages » / « Notifications ») ; droite = **bouton 🔔 → `/notifications`** (avec pastille non-lus, déplacée depuis la tab bar) sur feed/explorer/messages, vide ailleurs. **`MobileTabBar` passe de 4 à 3 onglets** (feed / explorer / messages) — la cloche notif quitte la barre. **Point d'archi clé** : l'overlay des sections (`fixed inset-0 z-40`, rendu racine) recouvrait le `MobileHeader` qui vivait dans la colonne `backdrop-blur` (contexte d'empilement confiné) → le header était invisible sur les overlays. Correctif : `MobileHeader` **sorti de la colonne, rendu au niveau racine en `fixed top-0 z-[60]`** (au-dessus des overlays), avec décalage `pt-14` sur le feed (`<main>`) et sur le panneau d'overlay (nouvelle prop `FeedOverlay headerOffset`, défaut `true`). **Header sur feed + nav principale (explorer/messages/notifications) + paramètres** : les **paramètres** reçoivent le header en **variante flèche retour** (← `router.back()` + titre « Paramètres », leur en-tête propre étant déjà `hidden lg:block`) avec offset `pt-14`. Les autres pages (signets, admin, modération, profil) gardent le header global masqué (`section === 'other'` → `null`) + `headerOffset={false}` → **comportement actuel préservé** (leur propre en-tête). En-têtes propres des vues **masqués sur mobile** (`hidden lg:…`, portés par le header global) : notifications (titre), explorer (titre — **la barre de recherche reste**), conversation-list (titre + « + »). **Messages** : le « + » de l'en-tête passe en bouton **« Nouveau message »** dans le body (haut-droite, mobile only ; même menu DM/groupe/communauté/découvrir, factorisé) ; et **sur une conversation ouverte (mobile), l'en-tête global est masqué** au profit de l'en-tête propre du ChatPane (nom + retour). Le ChatPane est piégé dans le contexte d'empilement de l'overlay (`z-40`) → son `z` ne peut pas dépasser le header (`z-60`) : plutôt que de le sortir en plein écran, le `MessagesProvider` expose désormais `activeConversationId` (state, la `ref` restant pour la WebSocket) que le `MobileHeader` lit pour s'effacer. Offset géré en interne par `MessagesView` (page en `headerOffset={false}`) : volet LISTE en `pt-14` (dégage le header global), volet CHAT dès le haut (header masqué) ; hauteur racine `100dvh-3.5rem` (tab bar). i18n : `nav.back` (FR « Retour » / EN « Back »), `messages.new_message` (FR « Nouveau message » / EN « New message »), parité FR/EN. Front-only, aucune route/handler backend → Swagger non requis. ⚠️ **Build/lint non vérifiables en local** (node_modules root-owned) ; **à valider visuellement sur mobile** (décalages `pt-14`, hauteur messages `100dvh-7rem`, chat plein écran) via `make dev` / CI.*

*Last updated: 17/06/2026 — fix(posts/poll) : **3 correctifs sondages (branche #249)**. (1) **Durée minimale 1 minute** : on pouvait sélectionner `0/0/0` (jours/heures/minutes) dans le composer → `buildPollPayload` retournait `undefined` et le sondage était silencieusement abandonné. `post-composer.tsx` : `minPollMinutes = (pollDays === 0 && pollHours === 0) ? 1 : 0` ; un `useEffect` remonte `pollMinutes` à 1 dès que jours+heures repassent à 0, et le `<NumberSelect>` des minutes gagne un prop `min` (options générées à partir de `min` au lieu de toujours partir de 0) → l'option 0 disparaît quand jours et heures valent 0. Garde-fou `durationMinutes < 1` conservé côté `buildPollPayload`. (2) **Pourcentage à côté de « Voté »** (`post-card.tsx`/`PostPollCard`) : le choix voté n'affichait que « Voté » ; il affiche désormais « Voté · 42% · 12 vote(s) ». (3) **Nombre de votes précis par choix** : chaque choix montre maintenant le compte exact en plus du `%` (« 42% · 12 vote(s) »), **visible seulement après avoir voté ou une fois le sondage terminé** (`showCounts = showResults && (votedChoiceId || closed)`) — avant vote, seul le `%` reste affiché. i18n : aucune nouvelle clé, réutilisation de `post.poll_votes` (`{count} vote(s)`, déjà FR/EN) pour le compte par choix → parité conservée. Front-only, aucun contrat backend touché → Swagger non requis. ⚠️ `tsc`/`lint`/`build` non vérifiables en local (node_modules root-owned, bind-mount Docker) → à valider via `make dev`/CI.*
*Last updated: 18/06/2026 — feat(moderation) : **filtres dans la corbeille de modération (« Tweets supprimés »)**. La vue chargeait une seule page de 50 sans filtre → inexploitable au volume. **Backend `post-service`** : `GET /posts/moderation/deleted` accepte désormais des filtres **cumulables côté serveur** — `author_id`, et plage de date de retrait `since`/`until` (RFC3339 sur `hidden_at`). Nouveau `repository.HiddenFilter` (auteur + bornes `hidden_at`) ; `ListHidden`/`ListHiddenPosts`/handler `ListHidden` étendus (params optionnels rétro-compatibles, tri `hidden_at` desc + limit/offset inchangés) ; helper `parseQueryTime`. **Frontend** : `lib/moderation.ts` → `listDeletedPosts(filter)` (objet d'options, query string). `deleted-posts.tsx` refondu : **barre de recherche d'utilisateur** (`searchUsers` débouncé → suggestions nom/@username → sélection → chip + `author_id`, accessible mod car `/users/search` est public), **filtre plage de date** (Depuis/Jusqu'à → bornes ISO jour), **« Charger plus »** (pagination offset, `posts.length`), bouton **Réinitialiser**. La barre de filtres reste visible même sans résultat (message dédié `moderation.no_match`). i18n FR/EN : `moderation.{search_user,filter_since,filter_until,filter_reset,no_match,load_more}`, `common.clear`. Vérifs : post-service `go build`/`go vet`/`go test ./...`/`gofmt` OK ; `make swagger` régénéré (`author_id`/`since`/`until` sur la route). ⚠️ Front non typecheck/lint en local (node_modules Docker) — revue manuelle ; à valider via `make dev`/CI.*

*Last updated: 18/06/2026 — fix(ux/moderation) : **lignes de l'annuaire des comptes compactées sur mobile**. Chaque ligne empilait jusqu'à 4 contrôles inline (badge statut + bouton rôle + bannir + supprimer) → débordement à droite sur petit écran. Toutes les actions sont repliées dans une **bulle « … »** (`DropdownMenu`, `MoreHorizontal`) à droite, sur mobile comme desktop : sous-menu **Changer le rôle** (admin), **Bannir/Réactiver** (cibles bornées), **Supprimer le compte** (admin, séparé). Le rôle élevé (mod/admin) passe en **badge** à côté du statut (utilisateur simple → juste le statut). La bulle n'est rendue que si au moins une action est disponible (`hasActions`) → ligne d'un compte non-actionnable (soi-même, ou mod ciblant un mod/admin) sans contrôle superflu. Le spinner d'action (`busy`) s'affiche sur le trigger « … ». i18n FR/EN : `accounts.actions`, `admin.change_role`. Front-only (`accounts-panel.tsx`), aucun contrat back touché. ⚠️ Non typecheck/lint en local (node_modules Docker) — revue manuelle ; à valider via `make dev`/CI.*

*Last updated: 18/06/2026 — feat(moderation) : **cohérence des décisions sur un ticket + encart « profil de risque » de l'utilisateur ciblé**. Demande : (a) le bouton **Clôturer** était un toggle de statut neutre, indépendant des actions — ambigu (« clôturer sans rien faire » vs « clôturer après avoir agi ») ; (b) on pouvait **Valider** (« conforme ») un ticket déjà sanctionné ; (c) une **suppression** de contenu n'avertissait jamais l'auteur ; (d) besoin de voir les **antécédents** de la cible et de la **bannir** depuis le ticket. **report-service** : nouveau type d'action **`warned`** (models ; l'enum `actions.type` n'est pas contraint dans le `$jsonSchema` → aucune migration). `IssueWarning` **journalise `warned`** sur le ticket quand un `ticket_id` est fourni (best-effort) → le ticket devient **auto-descriptif** (la sanction est tracée + visible au journal). `Approve` refuse désormais (nouvelle erreur **`ErrAlreadyActioned` → 409**) si le ticket porte déjà une action `content_removed` ou `warned` (garde-fou serveur, helper pur `hasBlockingSanction` testé). Nouveau **`GET /reports/users/:id/warnings/count`** (mod/admin) → `{ count }` (repo `CountWarnings`). **Front** : `ticket-detail` — **Valider** masqué dès qu'une sanction existe (miroir du garde-fou, backstop toast sur 409) ; **Supprimer** (post & message) ouvre d'abord la **modale d'avertissement pré-remplie** (justification : « Votre publication a été supprimée… Motif : ») et **ne supprime RIEN tant que le modérateur n'a pas validé** (Annuler = aucun effet de bord) ; à la validation, séquence **atomique et idempotente** : suppression → journal `content_removed` → avertissement → **clôture automatique** du ticket (`changeTicketStatus('closed')`). Chaque étape est sautée si déjà journalisée → après un échec partiel (ex. avertissement KO), un nouvel essai ne redéclenche ni la suppression ni un second avertissement (pas de doublon envoyé à l'auteur) ; échec du retrait → message dédié `tickets.action_failed` (WarnDialog ne double pas avec son `warn.error` quand `onConfirm` est fourni). Réutilise `WarnDialog` (nouveaux props `defaultMessage`/`title`/`description`/`confirmLabel`/`onConfirm` action déléguée + `onSubmitted`) ; **garde-fou de clôture** : clôturer un ticket de **modération** sans aucune sanction demande confirmation (« Clôturer comme non fondé ? ») ; action `warned` rendue au journal ; **encart « profil de risque »** sous l'en-tête agrégé (identité de la cible résolue + nb d'avertissements `getUserWarningCount` + bouton **Bannir** avec confirmation, réutilisant `setUserBanned` de `lib/admin` ; caché sur soi-même ; 403 back → message « action restreinte »). i18n FR (réf) + EN (parité). Vérifs : report-service `go build`/`go vet`/`go test ./internal/service` OK (+ `TestHasBlockingSanction`), `make swagger` régénéré (endpoint + 409 sur approve). ⚠️ Front non typecheck/lint en local (node_modules root-owned/vide, `npm install` rejeté par l'OS) — revue manuelle ; à valider via `make dev`/CI.*

*Last updated: 17/06/2026 — feat(moderation) : **auto-masquage des posts trop signalés + verrou de validation + seuil réglable par l'admin**. Trois règles ajoutées au système de signalement (les features « 1 ticket/post agrégé » et « tri par volume » étaient déjà en place). **(1) Auto-masquage** : un POST de modération qui atteint un seuil de signalements est automatiquement masqué (sort des fils) en attendant une décision de modérateur. Les BUGS (onglet admin) sont exemptés. **(2) Verrou de validation** : un nouveau statut TERMINAL `approved` — le modérateur juge l'entité conforme → le post est démasqué ET tout nouveau signalement est refusé (409, définitif). **(3) Seuil réglable** par l'administrateur dans **Admin › Paramètres** (0 = désactivé, défaut 5). **Cross-service** (report-service → post-service) **off-gateway** authentifié par `X-Internal-Secret` (même pattern que l'émission de notifications) : nouveaux endpoints internes `POST /internal/posts/:id/auto-hide|auto-unhide` côté post-service (vérif secret dans le handler), client sortant best-effort fire-and-forget côté report-service (`internal/client/post.go`, échecs loggés). **report-service** : statut `approved` + actions `auto_hidden`/`approved` (models) ; collection **singleton `settings`** (`_id="global"`, `auto_hide_threshold` int32) seedée au boot (`$setOnInsert` idempotent, n'écrase jamais un seuil réglé) ; enum `status` du `$jsonSchema` étendu à `approved` (migration collMod, **règle 5b**) ; `CreateReport` refuse un signalement sur ticket `approved` (`ErrReportingLocked`→409) puis déclenche `maybeAutoHide` (lit le seuil, idempotent via journal d'action) ; méthode `Approve` (statut terminal + démasquage) ; routes `POST /reports/tickets/:id/approve` (mod), `GET /reports/settings` (mod), `PATCH /reports/settings` (admin). **post-service** : champ `auto_hidden` (bool, `omitempty` → pas de backfill) ajouté au modèle + `$jsonSchema` ; barrière de visibilité `GetPost` et tous les filtres de lecture (`notHidden()`, hashtags, stats, liked, reposts) excluent désormais `auto_hidden` ; modérateur le voit toujours (jugement sur pièce) ; `RestoreHidden` lève aussi `auto_hidden` (sinon un post auto-masqué→retiré→restauré resterait invisible). **Front** : `lib/reports.ts` (`approved`, `approveTicket`, `getReportSettings`/`updateReportSettings`) ; `ticket-detail` bouton **« Valider (conforme) »** (moderation only) + bannière verrou + actions `auto_hidden`/`approved` au journal ; `tickets-panel` filtre+badge `approved` ; nouvel onglet **Admin › Paramètres** (`moderation-settings.tsx`) ; `report-dialog` message dédié si entité validée. i18n FR (réf) + EN (parité). **Wiring** : compose ajoute `POST_SERVICE_URL` + `INTERNAL_EVENT_SECRET` au report-service. Vérifs : report-service + post-service `go build`/`go vet`/`go test` OK, gofmt clean, `make swagger` régénéré. ⚠️ Front non typecheck/lint en local (toolchain Next/tsc absente de l'env) — à valider via `make dev`/CI. Détails DECISIONS « Auto-masquage & verrou de validation ».*

*Last updated: 17/06/2026 — feat(dev) : **accès LAN (téléphone) via `make dev-lan`**. Objectif : ouvrir Breezy sur un téléphone du même Wi-Fi sans éditer les `.env` à la main à chaque changement d'IP. Cause des blocages potentiels : (1) `NEXT_PUBLIC_API_URL` est **inlinée au build** Next.js (sur le tel, `localhost` = le téléphone) ; (2) `CORS_ALLOWED_ORIGINS` (gateway) doit whitelister l'`Origin` `http://<IP>:3000` du front, sinon CORS bloque tout. Nouvelle cible **`make dev-lan`** (Makefile) : auto-détecte l'IP LAN (`hostname -I | awk '{print $1}'`, surchargeable `make dev-lan IP=192.168.1.42`), puis exporte `NEXT_PUBLIC_API_URL=http://<IP>:8080`, `CORS_ALLOWED_ORIGINS=http://localhost:3000,http://<IP>:3000` (localhost conservé → le dev PC marche toujours), `APP_BASE_URL=http://<IP>:3000` (liens e-mail cliquables depuis le tel) avant `$(DEV) up --build -d` (le `--build` rebuild le front avec la nouvelle URL API). `make dev` classique **inchangé**. Pré-requis côté compose : `CORS_ALLOWED_ORIGINS` ne venait que de `api-gateway/.env` (via `env_file`, non surchargeable par le shell — précédence Docker `environment:` > `env_file:`) ; ajout de `CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:3000}` au bloc `environment:` du service `api-gateway` (`docker-compose.yml`) pour la rendre interpolable. `API_INTERNAL_URL=http://api-gateway:8080` (côté serveur, réseau Docker) **inchangé**. Note : le pare-feu PC doit autoriser 3000/8080 (`sudo ufw allow 3000,8080/tcp`), tel et PC sur le même LAN. Vérifs : `make -n dev-lan` parse OK, IP détectée, 3 vars bien injectées dans la commande compose. Non lancé en réel (rebuild lourd, à valider par l'utilisateur).*

*Last updated: 16/06/2026 — fix(messages) : **composer DM collé à la tab bar sur iPhone (swipe barre d'adresse + clavier)**. Décalage entre le composer et la tab bar sur iPhone, recréé soit en swipant (barre d'adresse Safari), soit en ouvrant le clavier (décalage permanent). En navigation soft (cas courant) la messagerie est rendue dans `FeedOverlay` = overlay `fixed` (`@modal/(.)messages`). **Causes iOS** : (1) un `fixed; bottom:Npx` se mesure depuis le bas du LAYOUT viewport alors que `fixed; bottom:0` (la tab bar) se cale sur le bas VISIBLE → désync au mouvement de la barre d'adresse ; (2) le clavier rétrécit le visual viewport mais pas le layout viewport (`fixed`/`dvh` l'ignorent) → composer derrière le clavier + scroll coincé. **Correctif** (`feed-overlay.tsx`, variante `wide` = messagerie) : le panneau overlay utilise **le même ancrage que la tab bar** — `inset-x-0 top-14 bottom-0 lg:inset-y-0` (et non plus `bottom-14`) → panneau et tab bar partagent le bas visible, restent collés au swipe. Le composer est décalé au-dessus de la tab bar via le **padding bas** du panneau (`pb-14 lg:pb-0`), pas via `bottom`. Pour le **clavier** : nouveau hook `use-keyboard-inset.ts` (API `window.visualViewport`, `inset = innerHeight − vv.height − vv.offsetTop`, seuil anti-bruit 120px, no-op sur Android/desktop) ; `FeedOverlay` passe `'use client'` et, clavier ouvert, remplace ce padding par `paddingBottom: inset` → le composer remonte pile au-dessus du clavier, et redescend à la fermeture (plus de décalage coincé). `MessagesView` passe en `flex-1 min-h-0` (étirement flex fiable, au lieu d'un `h-[100dvh-7rem]`/`h-full` fragile) → remplit la zone utile et son composer se pose sur le padding ; la hauteur est portée par le conteneur : panneau overlay (clavier-aware) en soft-nav, wrapper `flex h-[calc(100dvh-7rem)] flex-col lg:h-screen` dans `(app)/messages/page.tsx` en accès direct. `chat-pane` garde `pb-2.5` (`env(safe-area-inset-bottom)` redondant retiré). Front-only, pas de route/contrat/i18n → Swagger non requis.*

*Last updated: 16/06/2026 — fix(notifications) : **la page de notifications ne plante plus sur un type inconnu**. Crash runtime `TypeError: TYPE_ICON[n.type] is undefined` : une notification au `type` hors enum (probable notif **legacy** créée avant l'ajout du validateur `$jsonSchema` — l'enum n'est imposé qu'à l'écriture, pas à la lecture ; cf. règle 5b) faisait planter **toute** la liste. Le backend (`notification.go` + enum Mongo) et le union front `NotificationType` sont par ailleurs **alignés** (12 types). Correctif défensif côté rendu (`notifications-view.tsx`) : `TYPE_ICON[n.type] ?? { Icon: Bell, className: 'text-muted-foreground' }` (icône neutre de repli) + branche `default` dans `describe()` (texte vide, la ligne reste affichée avec acteur + date). Un item inattendu ne white-screene plus la page. Front-only, aucune route/contrat touché. ⚠️ build non vérifiable en local (node_modules root-owned), à valider via `make dev`/CI.*

*Last updated: 16/06/2026 — fix(posts-ui) : **suppression du bouton stats/vues dans toutes les barres d'actions de posts**. Le bouton `BarChart2`/`post.views` a été retiré de `PostCard` (feed, profil, explorer, détail, posts imbriqués) et de `PostActions` (vue photo/modale partagée), avec nettoyage des imports inutiles. Aucun handler, route ou contrat backend modifié → annotations Swagger et `make swagger` non requis. Vérifs : `npm run lint` OK ; `npx tsc --noEmit` reste bloqué par l'erreur pré-existante `src/lib/post-stats.test.ts` (`poll?: undefined` incompatible avec `FeedPost.poll`).*

*Last updated: 16/06/2026 — fix(ux) : **feed en arrière-plan refondu — du combo `@modal` (parallel + intercepting routes) vers un feed persistant au niveau du layout**. La fonctionnalité « feed gardé en fond façon X pendant qu'on consulte une autre section » reposait sur un slot parallèle `@modal` + 11 intercepting routes `(.)`, source de 4 bugs UX cumulés : (1) **404 intermittent à la connexion** (le slot parallèle ne résout pas proprement sur certaines navigations/chargements → 404, y compris en nav dure) ; (2) **feed visible et défilable derrière** les autres pages (aucun scroll-lock) ; (3) **blocage après refresh** sur une sous-page (l'interception est perdue → `children` = la vraie sous-page et non le feed ; le truc « `(.)feed → null` pour révéler le feed » ne marchait plus → impossible de revenir au feed) ; (4) **admin/modération pas en overlay**. Diagnostic : ce n'étaient pas 4 bugs indépendants mais **un seul défaut de conception** — `children` est un slot unique qui contient la page réelle, pas « toujours le feed » ; l'illusion ne tenait que pour une nav soft *partie du feed*. **Refonte (Option A, 100 % frontend, 0 service Go, 0 migration)** : `FeedView` est monté **une seule fois dans `(app)/layout.tsx`**, dans la colonne centrale (porte le scroll fenêtre, conserve scroll/posts/WebSocket sur toute la navigation) ; `(app)/feed/page.tsx` rend `null` (le fond transparaît) ; les 10 autres pages s'enveloppent dans `<FeedOverlay>` (`messages` en `wide`) et sont rendues **au niveau racine du layout** (hors colonne centrale) pour que leur `position:fixed` se réfère au viewport — le `backdrop-blur` de la colonne créerait sinon un bloc conteneur qui rognerait l'overlay (même piège que les modales `createPortal`). Nouveau composant `OverlayScrollLock` : pose `overflow:hidden` sur `<html>` dès que `pathname ≠ /feed` → le feed se fige à sa position (préservée) et ne défile plus derrière l'overlay (#2). `FeedView` **gèle** sa lecture des query params (`hashtag`/`tab`) hors `/feed` (refs) pour ne pas refetcher/réinitialiser en arrière-plan quand on ouvre un overlay. **Suppression complète** du dossier `@modal` (slot + 11 intercepting routes) et de `overlay-when-path.tsx` (garde devenue inutile : le routing normal ne rend chaque page que sur sa propre URL). Conséquence : refresh et deep-link suivent le routing normal sans cas particulier soft/hard → **#1 et #3 disparaissent par construction**, **#4 gratuit** (admin/modération = overlays comme les autres). Commentaires `@modal` obsolètes nettoyés dans `login`/`verify-email`/`auth/callback` (la nav dure post-auth `window.location.assign(/feed)` reste, désormais par choix « état d'app propre » et non parade au 404). ⚠️ **Non vérifié en build local** : `node_modules` (bind-mount Docker) est root-owned/vide → `tsc`/`lint` impossibles en local ; à valider via `make dev` / CI. Détails DECISIONS « Feed en arrière-plan persistant ».*
*Last updated: 16/06/2026 — feat(media) : **limite d'upload uniforme 5 Mo, bypass admin**. La demande : plafonner tous les médias (photo/vidéo/pièce jointe) à **5 Mo** pour les utilisateurs, **sauf les administrateurs** qui peuvent uploader plus lourd. Repère X.com (photo 5 Mo, le plus petit cap d'un service de référence) → 5 Mo uniforme, rien à descendre sous 5 Mo. **Backend `media-service`** (source de vérité, cf. §6 « la sécurité ne dépend jamais du front ») : défaut vidéo abaissé **50→5 Mo** dans `config.go` ; nouveau cap blob chiffré `MaxBlobBytes` (env `MEDIA_MAX_BLOB_BYTES`, défaut 5 Mo) — l'`UploadEncrypted` E2EE passait avant par le cap vidéo (50 Mo). `MediaHandler.Upload`/`UploadEncrypted` calculent `isAdmin := claims.Role == roleAdmin` et **bypassent** tous les checks de taille pour les admins (le claim `role` était déjà lu pour `Delete`/purge RGPD) ; les non-admins gardent rejet précoce (avant lecture) + `validate.CheckSize`. **Aucun cap admin à configurer** : ils ne sont simplement pas plafonnés (au-delà du buffer `MaxMultipartMemory`, le reste streame sur disque temp). `.env.example` mis à jour ; compose lit `media-service/.env` via `env_file` → le nouveau var a un défaut côté code, rien à câbler. **Frontend** (garde UX uniquement) : `lib/media.ts` expose `MAX_MEDIA_BYTES`/`MAX_MEDIA_MB` + `exceedsMediaLimit(size)` (admin-aware via `isAdmin()`) ; les fichiers trop lourds sont écartés **à la sélection** (avant upload/chiffrement) dans `post-composer`, `comment-section` (×2), `edit-profil-dialog` (nouveau prop `onTooLarge` sur `ImagePicker`) et `chat-pane` (`handlePickFiles`), avec toast i18n `media.too_large` (« Fichier trop volumineux (max {max} Mo) »), parité **12/12 langues**. Annotations swagger inchangées (le 413 reste générique, sans valeur chiffrée) → doc non régénérée. Vérifs : media-service `go build`/`go vet`/`go test ./...` OK ; frontend `tsc --noEmit` sans erreur sur les fichiers touchés (seuls le test pré-existant `post-stats.test.ts` et l'écriture `tsconfig.tsbuildinfo` root-owned remontent, hors périmètre). Détails DECISIONS « Cap d'upload média ».*

*Last updated: 16/06/2026 — feat(moderation) : **système de signalement & modération complet (nouveau `report-service`)**. **Backend** : nouveau microservice Go+MongoDB (port 8090, préfixe gateway `/reports`), structuré comme notification-service (config/logging/middleware JWT + `ModeratorOnly`/`AdminOnly`/db/models/repository/service/handlers, `EnsureSchema` idempotent). Deux collections : `tickets` (un **ticket parent agrégé par entité** en modération — clé **unique partielle** `(entity_type, entity_id)` where `category=moderation` ; **bug = ticket autonome** `entity_type=app`, pas de dédup ; `report_count` int32 dénormalisé, `reason_tags` map motif→compte, `reports[]` fil enfant, `actions[]` journal modération) et `warnings`. Endpoints : `POST /reports` (tout utilisateur, valide motif + entité + **texte 255 modération / 500 bug**, comptage en **runes**), `GET /reports/tickets` (filtres **cumulables** category/status/min_reports/since/until, **tri volume décroissant**), `GET /reports/tickets/:id`, `POST .../replies` + `PATCH .../status` (mod, **enregistre l'id du modérateur** par action), `POST .../transfer` (admin, **bug→modération**), warns `POST /reports/warnings` + `GET /reports/warnings/pending` + `POST .../ack`. Pièce jointe via media-service (agnostique). **Front** : `lib/reports.ts` + `ui/textarea.tsx` + **`ReportDialog` réutilisable** (motif déroulant incluant **« Bug technique »** → catégorie **déduite du motif**, limite dynamique 255/500, compteur, image ≤ 5 Mb) câblé sur **posts** (menu « … »), **profils** (drapeau) et **messages DM/groupe** (sous Supprimer) ; onglet **Signalements** dans Modération (**après Comptes**) avec filtres cumulables ; **`AdminView`** à onglets **Signalements (bugs)** (+ **transfert**) et **Infrastructure** (`AdminInfra` rendu `embedded`) ; **`TicketDetail`** (en-tête agrégé volume/dernier/motifs, fil enfant user+motif+texte+image, journal d'actions, réponse interne, cycle de vie open/closed/reopened, retrait soft-delete → « Tweets supprimés », **Warn**) ; **`WarningsGate`** (avertissement asynchrone : sondage montage+focus → **modale bloquante** → ack) monté dans `(app)/layout` ; filtres **Comptes** Bannis/Modérateurs/Administrateurs. Réutilise soft-delete + ban + rôles existants (mod ne peut bannir/supprimer un admin). i18n **FR (réf) + EN (parité)**, ~70 clés (10 autres langues → fallback FR). **Wiring** : gateway `/reports`, docker-compose `.yml`/`.dev.yml`/`.prod.yml` (mongo-report + service + volume + depends_on, port 27021 dev), `.env.example`, Makefile (SERVICES + logs/sh/mongo-report-cli), CI (ci-go matrix+paths, ci-integration, deploy), swagger-aggregate. **Journal détaillé + flow de test complet : voir [REPORT.md](REPORT.md).** Vérifs : report-service `go build`/`go vet`/`go test` OK, gofmt clean, `docker compose config` (dev+prod) valide ; frontend `tsc --noEmit` 0 erreur sur les fichiers touchés (thème clair/sombre via tokens shadcn).*

*Last updated: 15/06/2026 — feat(posts) : **« qui peut répondre » paramétrable à la création du post** (façon X, calqué sur l'audience des sondages). Nouveau champ post `reply_audience` ∈ `{everyone (défaut), followers}`, choisi dans le composer, **non éditable** ensuite (parité avec `Poll.Audience`). **Backend post-service** : `Post.ReplyAudience` (`bson:"reply_audience,omitempty"` → jamais de `""` persisté ; absent sur un vieux doc = `everyone` via `ReplyAudienceOf`) ; propriété ajoutée au validateur `$jsonSchema` des `posts` comme enum **optionnelle** (non `required`) ; **migration idempotente au boot** `backfillReplyAudience` dans `EnsureSchema` (pose `reply_audience="everyone"` sur les posts legacy sans le champ — règle 5b, par prudence prod même si l'enum optionnelle tolère l'absence ; testée local = 8 posts normalisés) ; `CreatePostRequest.ReplyAudience` (`binding:"omitempty,oneof=everyone followers"`). **Barrière autoritaire** dans `CreateComment` (point d'entrée unique commentaires **et** réponses) via `canReplyTo` : si `followers` et acteur ≠ auteur → `IsFollowing` requis, **mods/admins bypass** (rôle propagé depuis le JWT), sinon `ErrReplyNotAllowed` (403). Champ transient **`can_reply`** hydraté par viewer **seulement** quand `reply_audience=followers` (check abonnement borné aux posts restreints → coût nul sinon), posé sur tous les chemins de lecture (feed/get/profil/vote/close), **fail-closed** si la vérif d'abonnement est indisponible (composer masqué dans le doute, le serveur bloquerait de toute façon). Nouvelles erreurs `ErrInvalidReplyAudience` (400) / `ErrReplyNotAllowed` (403) mappées. **Frontend** : `ReplyAudience` type + `replyAudience`/`canReply` sur `ApiPost`/`FeedPost` (mapping `can_reply !== false`) ; pilule **« Tout le monde peut répondre » / « Vos abonnés peuvent répondre »** (Dropdown Globe/Users) sous le texte du `PostComposer`, passée à `createPost` (`reply_audience` envoyé seulement si `followers`) ; `CommentSection`/`CommentThread` reçoivent `canReply` → composer racine remplacé par un message **« Seuls les abonnés de l'auteur peuvent répondre »** et boutons « Répondre » masqués quand `false` (`post-card` et `post-photo-modal` propagent `post.canReply`). i18n FR/EN `composer.reply_*` + `comment.restricted_followers` (les 10 autres locales retombent sur le FR jusqu'à une passe de traduction consentie). Vérifs : post-service `go build`/`go vet`/`gofmt`/`go test ./internal/...` OK (+ `TestReplyAudienceOf`, `TestCanReplyTo`) ; `make swagger` régénéré (`reply_audience`/`can_reply` + 403 sur `POST /posts/{id}/comments`) ; frontend `tsc --noEmit`, ESLint, 124 tests OK. Détails DECISIONS « Reply audience ».*

*Last updated: 15/06/2026 — fix(mail) : **logo des e-mails visible partout + moins de spam**. Deux symptômes (logo cassé dans la boîte, mails en spam) avaient la même cause : le template construisait le logo ET les liens depuis `APP_BASE_URL` (= `http://localhost:3000` en dev) → `<img src="http://localhost:3000/logo_breezy.png">` injoignable pour le destinataire + liens `localhost`/`http` (signal anti-spam fort). **Plan 1 — découplage du logo** : nouveau `MAIL_LOGO_URL` (auth-service), URL ABSOLUE publique du logo, indépendante d'`APP_BASE_URL` → le logo se charge même en test local. `brandedEmailHTML` prend désormais l'URL complète du logo (au lieu de la dériver de `baseURL`) ; les 5 appels passent `s.mailLogoURL`. Défaut rétro-compatible `APP_BASE_URL/logo_breezy.png` si non défini (aucun domaine en dur dans le code) ; les `.env`/compose posent `MAIL_LOGO_URL=https://breezy.philippeluu.fr/logo_breezy.png`. `New(...)` gagne un paramètre `mailLogoURL`. **Plan 3 — déliverabilité** : en-tête `Reply-To` (= From) ajouté dans `buildMIME` (mail-service). Rappel important (config serveur, hors repo) : en prod `APP_BASE_URL=https://breezy.philippeluu.fr` (déjà le cas) → liens publics HTTPS, le vrai levier anti-spam. L'alignement SMTP/DKIM était déjà correct (`From = SMTP_USER = breezy.credential@gmail.com`), non touché. Front inchangé, handlers/routes inchangés → Swagger non requis. Vérifs : auth-service `gofmt`/`go build`/`go vet`/`go test ./internal/services/...` OK (test du template adapté à l'URL absolue) ; mail-service `gofmt`/`build`/`vet`/`test` OK.*

*Last updated: 15/06/2026 — feat(settings) : **retour automatique sur l'écran de changement d'e-mail après vérification**. La fonctionnalité de changement d'adresse (backend + BFF + page de vérif) existait déjà ; seul le flux post-vérification a été ajusté pour coller à la demande. Auparavant la page `/verify-email-change` affichait sa propre carte de succès avec un bouton manuel « Retour aux paramètres ». Désormais, à la confirmation réussie, la page resynchronise la session (`notifySessionChanged`, JWT renouvelé portant déjà la nouvelle adresse) puis **redirige automatiquement vers `/parametres?email_changed=1`**. Le composant `UserAccountSettings` lit ce paramètre au montage et affiche un **bandeau de succès « Adresse e-mail modifiée »** (nouvelle clé i18n `settings.email.changed`, parité complète sur les 12 langues), nettoie l'URL via `history.replaceState` (un rafraîchissement ne ré-affiche pas le bandeau), et la nouvelle adresse s'affiche dans le bloc via `session.email`. Front-only, aucune route/contrat backend modifié → Swagger non requis. Vérifs : `tsc --noEmit` (aucune erreur de type ; seule l'écriture de `tsconfig.tsbuildinfo` échoue, artefact root-owned local), ESLint OK sur les 2 fichiers modifiés, parité i18n 12/12.*

*Last updated: 15/06/2026 — feat(settings/auth) : **paramètres généraux séparés des paramètres utilisateur, changement de mot de passe et d'adresse e-mail vérifié**. La carte existante de `/parametres` devient « Paramètres généraux » (langue, visibilité, mots filtrés) ; une carte « Paramètres utilisateur » ajoute les trois champs mot de passe (ancien, nouveau, confirmation) en réutilisant `POST /auth/password/change` et sa rotation de jetons, ainsi qu'un champ nouvelle adresse. Backend auth : nouveaux `POST /auth/email/change/request` (JWT) et `POST /auth/email/change/confirm` (public) ; migration PostgreSQL idempotente `credentials.pending_email` + index unique partiel, nouveau purpose `account_tokens=email_change`. L'ancienne adresse reste active jusqu'à preuve de la nouvelle ; la confirmation verrouille/consomme le jeton, promeut l'adresse, invalide les anciens jetons vérif/reset liés à l'ancienne boîte et révoque les refresh tokens dans une transaction, puis réémet une session portant le nouvel e-mail. Mail de confirmation 24h via le template Breezy ; page publique `/verify-email-change` + BFF qui pose le nouveau cookie refresh. i18n FR/EN avec repli FR pour les 10 autres locales. Vérifs : auth `gofmt`, `go test . ./internal/...`, `go vet ./internal/...` ; migration réelle vérifiée sur PostgreSQL existant ; route Gateway testée (faux token → 400 `invalid_token`) ; frontend `tsc`, ESLint, 124 tests et build Next OK. `make swagger` tenté sur Windows (bash Error 1312), puis Swagger régénéré via conteneur Linux avec les deux routes présentes dans `doc/openapi.{json,yaml}`.*

*Last updated: 15/06/2026 — fix(i18n/settings) : **le bloc Paramètres utilisateur suit désormais la langue sélectionnée**. Les 31 nouvelles clés `settings.general_*`, `settings.user_*`, `settings.password.*` et `settings.email.*` ont été ajoutées aux dictionnaires ZH/ES/PT/RU/JA/KO/AR/HI/DE/IT ; le composant utilisait déjà `useT()`, donc le changement est instantané sans rechargement. Audit automatisé : 2 occurrences FR/EN + 10 dictionnaires additionnels pour chacune des 31 clés, soit parité complète sur les 12 locales. Vérifs : `tsc --noEmit`, ESLint, 124 tests Vitest et build Next production OK. Front-only, aucune route/annotation backend modifiée, donc Swagger non requis.*

*Last updated: 15/06/2026 — fix(settings/mail) : **le changement d'adresse ne prétend plus avoir envoyé un e-mail quand SMTP est absent ou en échec**. Cause confirmée : le transport console du mail-service loggait « NON envoyé » mais retournait `nil`, donc `/internal/send` répondait 202 et auth validait la demande. Le transport console retourne désormais `ErrDeliveryUnavailable`; le handler mail répond 503 `delivery_unavailable` (502 pour une panne SMTP réelle). `RequestEmailChange` est strict : toute erreur de remise supprime transactionnellement le jeton exact et `pending_email` correspondant, puis répond 503 `email_delivery_failed`; inscription et création admin restent best-effort. L'UI affiche une erreur dédiée dans les 12 langues. Tests ajoutés sur le transport console et les contrats HTTP 503/502. Vérifs : auth et mail `gofmt`, `go test`, `go vet`; frontend `tsc --noEmit` + ESLint; `make swagger` tenté (Windows bash Error 1312), agrégat régénéré avec succès dans le conteneur Linux. L'envoi réel nécessite toujours des identifiants SMTP valides dans `mail-service/.env`.*

*Last updated: 15/06/2026 — fix(translation) : **toutes les langues sources sont traduites vers la langue choisie dans l'application**. Cause : les petits dictionnaires de marqueurs du client agissaient comme une allow-list et bloquaient certaines formulations portugaises/espagnoles/italiennes avant `/api/translate`. Nouveau flux : toute phrase significative non clairement déjà dans la langue cible part au fournisseur ; sa détection automatique fait foi et le résultat est rejeté si `source === cible`. Le veto local reste pour les cas évidents, dont le français familier/fautif. Han chinois et kana japonais sont séparés pour autoriser ZH↔JA. Tests : 132 paires source≠cible + 12 mêmes langues ; test BFF réel réussi sur les 12 cibles (PT→KO, ES→EN, FR→JA, JA→ZH, ZH→ES, KO→PT, AR→HI, HI→DE, DE→IT, IT→RU, RU→AR, EN→FR). Frontend : tsc, ESLint, 109 tests et build Next production OK. Aucun changement route/contrat backend, Swagger non requis.*

*Last updated: 15/06/2026 — feat(nav) : **mémoire de navigation de l'onglet Recherche (loupe)**, façon Instagram. Quand on tape la loupe depuis un autre onglet (fil, messages…), on revient **là où on en était** dans la section découverte (profil consulté ou résultats de recherche) au lieu d'une page Explorer vierge ; quand on y est déjà, re-taper = **recherche neuve** (`/explorer`). Cas typique réglé : profil A → bouton « Message » → DM, puis loupe → on retombe sur le profil A. Implémentation : `lib/search-tab.ts` (`isSearchSectionPath` = `/explorer` + `/profil/<username>`, hors profil perso ; mémoire en **variable module** + `sessionStorage` en bonus quand dispo. ⚠️ **Limite connue (acceptée)** : ne fonctionne pas en **navigation privée iOS** (storage restreint / contexte réinitialisé) ; OK en navigation normale. `RouteOriginTracker` mémorise le dernier chemin de section (avec `window.location.search` pour la query) ; l'Explorer mémorise en plus l'URL `?q=` à chaque recherche (le pathname ne changeant pas). Les loupes (`mobile-tab-bar`, `sidebar-left`) utilisent un **`href` dynamique** (état recalculé à chaque navigation : dernier chemin de section, ou `/explorer` si déjà dans la section) — **navigation native d'ancre**, plus fiable sur iOS qu'un `onClick`+`preventDefault` (1ʳᵉ tentative qui ne marchait pas sur iPhone) ; défaut `/explorer` au 1ᵉʳ rendu pour éviter tout mismatch d'hydratation. Front-only, pas de backend/Swagger. Vérifs : `tsc --noEmit` OK, `vitest search-tab.test.ts` 2 OK.*

*Last updated: 15/06/2026 — feat(messages) : **destinataire sans messagerie activée + entrée DM depuis le profil**. (1) Quand on tente un **DM / une invitation groupe ou communauté** vers une personne qui n'a **jamais activé sa messagerie** (pas de clé publique d'identité publiée → `GET /messages/keys/:id` 404), au lieu du message générique « Action impossible », message dédié **« Cette personne n'a pas encore activé ces messages »**. Style de toast dédié **variant `brand`** (nouvelle variante dans `toast.tsx`) : **contour dégradé aux couleurs du bouton « Suivre »** (`--brand-from/via/to`, technique double-fond padding-box/border-box), **intérieur = surface du thème + texte `foreground`** → blanc/texte noir en clair, sombre/texte clair en **dark mode** (`hsl(var(--background))`). Implémentation : nouvelle classe d'erreur `PeerKeyMissingError` levée par `fetchPeerPublicKey` sur un 404 (donc propagée par `startDM`, `createGroup`, `inviteToGroup`/`sealForUser`) ; catchée dans `new-dm-dialog`, `new-group-dialog`, `conversation-info-dialog` (invite) et le point d'entrée `?dm=` de `messages-view`. i18n FR/EN `messages.peer_not_activated`. **Suivi affichage toasts iPhone** : les toasts étaient ancrés en haut (`top-0`) sur mobile → tronqués par l'encoche/la status bar iOS (on ne voyait qu'une bande rouge). Sans `viewport-fit=cover` (qu'on évite car il ferait passer les en-têtes `sticky top-0` sous l'encoche), `env(safe-area-inset-top)` vaut 0. Correctif : toasts **en bas de l'écran partout** (mobile comme desktop), animation `slide-in-from-bottom`, `pb-[max(1rem,env(safe-area-inset-bottom))]`. **Auto-fermeture à 3 s** (`ToastProvider duration={3000}`) + `TOAST_REMOVE_DELAY` ramené de 1 000 000 ms à 1 000 ms (nettoyage de l'état après l'animation de sortie). (2) Bouton **« Message »** du profil (`/messages?dm=<id>`) : le handler ouvre désormais **directement la conversation existante** si elle est déjà chargée (sans requérir la clé du destinataire), sinon `startDM` la crée ; l'erreur n'est plus avalée silencieusement (toast « pas activé » + nettoyage de l'URL). Front-only, pas de backend/Swagger. Vérifs : `tsc --noEmit` OK, `vitest messages.test.ts` 33 OK.*

*Last updated: 15/06/2026 — fix(messages) : **2 correctifs accusés/actions (retours iPhone)**. (1) La coche d'accusé « ne suivait pas » le dernier message (restait sur un message antérieur) : `planReceipts` simplifié → **une seule marque, toujours sous mon DERNIER message** (cohérent avec le rename « Envoyé ») — coche grise « Envoyé » tant que non lu, 2 coches couleur quand lu (DM/tous), 1 coche + nombre si groupe partiellement lu ; ignore les messages supprimés. Tests `planReceipts` réécrits (33 OK). (2) Boutons éditer/supprimer s'affichaient sur **tous** les messages au tap (hover « collant » d'iOS sur `group-hover`) : passage à une **sélection au tap (un seul message à la fois)** via `activeMessageId` dans `chat-pane` (tap sur la bulle (dé)sélectionne, en ignorant les clics sur liens/boutons internes) ; le survol n'est conservé que sur les appareils à survol réel (`[@media(hover:hover)]:group-hover`). **Suivi iPhone** : le tap sur la bulle (`onClick` puis Pointer Events) restait non fiable sur de vrais iPhones (Safari/Brave) → **solution finale : un menu « … » (Radix `DropdownMenu`)** par message. Vrai `<button>` (trigger) = tap infaillible sur iOS, un seul menu ouvert à la fois ; items « Modifier » / « Supprimer ». Le « … » est **toujours visible sur tactile**, révélé au survol sur desktop (`[@media(hover:hover)]`). (3) **Suppression d'image** : le menu « … » est aussi rendu à côté des **messages média seuls** (sans texte) → on peut supprimer une image/vidéo sur PC **et** mobile (avant, les actions n'existaient que dans la bulle texte). i18n `messages.actions`. Front-only, pas de backend/Swagger. Vérifs : `tsc --noEmit` OK, `vitest messages.test.ts` 33 OK.*

*Last updated: 15/06/2026 — feat(messages) : **suppression de message + indicateur « en train d'écrire »** (suite branche #215). « Remis » renommé en **« Envoyé »** côté UI (i18n only, FR « Envoyé » / EN « Sent »). **Suppression « pour tout le monde » (tombstone)** : `DELETE /messages/conversations/:id/messages/:messageId` → `SoftDeleteMessage` pose `deleted_at` et **vide** `ciphertext`/`nonce` (contenu effacé, E2EE déjà aveugle) ; autorisée à l'**auteur** OU à l'**owner/admin** d'un groupe/communauté (modération) via la fonction PURE testée `canDeleteMessage` (aucun droit de modération en DM) ; diffusion via l'événement WS **`message_updated` existant** (le tombstone remplace la bulle, zéro nouveau plumbing). Champ `Message.DeletedAt` (`omitempty`, ajouté au validateur `messages` — nullable, **pas de migration**). Front : `deleteMessage`, `decryptMessage` court-circuite vers un tombstone si `deleted_at`, bouton corbeille (auteur/modérateur, `window.confirm`), bulle « Message supprimé » sobre, aperçu liste = « Message supprimé » (`ConversationPreview.deleted`). **« En train d'écrire »** (éphémère, non persisté, E2EE-safe = `user_id`+`conversation_id`) : `POST /messages/conversations/:id/typing` (`TypingTargets`) → diffuse l'événement WS `typing` aux autres membres ; client : ping **throttlé ~3 s** dans le composer (`sendTyping`), récepteur (`messages-view`) pose une échéance +6 s par membre purgée toutes les 1,5 s, indicateur `TypingIndicator` sous l'historique (DM « écrit… » / groupe « X écrit… » / « Plusieurs personnes écrivent… »). i18n FR/EN `messages.deleted`/`delete_confirm`/`typing`/`typing_user`/`typing_several`. Vérifs : message-service `go build`/`go vet`/`go test ./internal/...` OK (+ `TestCanDeleteMessage`), `gofmt` propre ; frontend `tsc --noEmit` OK, `vitest messages.test.ts` 33 OK ; `make swagger` régénéré (`/typing` + `DELETE .../messages/{messageId}` dans la spec). **Branche #215 complète.** Détails DECISIONS « Message deletion » / « Typing indicator ».*

*Last updated: 15/06/2026 — feat(messages) : **accusés de réception « Remis / Ouvert » (DM + groupes)**. Le destinataire d'un message expose désormais à l'expéditeur deux curseurs serveur (métadonnée d'horodatage, jamais le ciphertext — E2EE intact, cf. CLAUDE.md §6) : `last_read_at` (« ouvert », existant) et nouveau `last_delivered_at` (« remis »). **Backend message-service** : champ `Member.LastDeliveredAt` (`omitempty`, ajouté au validateur `$jsonSchema` des `members` — champ nullable optionnel, donc **pas de migration** requise, conforme règle 5b prod-DB) ; `SetMemberDelivered` + `SetMemberRead` avance aussi la livraison (lire ⇒ avoir reçu, remis ≥ ouvert) ; « remis » marqué quand le serveur **livre** le message (push à une socket ouverte au moment de l'envoi via `hub.OnlineFrom`, ou récupération de l'historique via `ListMessages` → `TouchDelivered`) — aucun ack client. Nouvel événement WS `receipt` `{conversation_id, user_id, delivered_at?, read_at?}` diffusé aux **autres** membres (les expéditeurs) sur `MarkRead`/livraison → coches mises à jour en temps réel. `ConversationView` expose `member_receipts` (curseurs des autres membres, DM/groupes seulement ; pas les communautés) via `attachReceipts`. **Frontend** : `Conversation.memberReceipts` + parsing ; fonctions PURES testées `planReceipts` (DM : 2 coches sous le dernier message lu, 1 coche sous le dernier remis non lu — possiblement 2 messages différents ; groupe : sous mon dernier message → personne lu = 1 coche grise, partie = 1 coche couleur + nombre de lecteurs, tous = 2 coches) et `applyReceipt` (maj temps réel monotone) ; cas `receipt` dans `messages-view`/`handleEvent` ; rendu `ReceiptIndicator` dans `chat-pane` (coche grise `Check` = remis, coches couleur Breezy `CheckCheck`/`Check`+nombre = ouvert) **cliquable (tap PC+mobile) → révèle le libellé « Remis »/« Ouvert » à droite de la coche**. i18n FR/EN `messages.receipt_delivered`/`receipt_read`. Vérifs : message-service `go build`/`go vet`/`go test ./internal/...` OK, `gofmt` propre ; frontend `vitest src/lib/messages.test.ts` 33 OK (15 nouveaux), `tsc --noEmit` OK ; `make swagger` régénéré (`member_receipts` dans `doc/openapi.{json,yaml}`). Reste de la branche à faire : suppression de message + indicateur « en train d'écrire ». Détails DECISIONS « Message read/delivery receipts ».*

*Last updated: 15/06/2026 — fix+ui(feed) : **suivi compteurs dynamiques — bug d'affichage + animation de roll**. (1) Bug : seul le compteur de reposts se mettait à jour dynamiquement ; likes et commentaires restaient figés. Cause : `PostCard` (et `PostActions`) affichent leur barre d'action avec des compteurs en **état local** ; `repostCount` avait un `useEffect` de resync sur la prop, mais `likeCount`/`commentCount` non → le polling mettait bien à jour `post.likesCount`/`post.commentsCount` mais l'affichage ne suivait pas. Correctif : `useEffect` de resync ajoutés pour `likeCount`←`post.likesCount` et `commentCount`←`post.commentsCount` (l'état « liked/reposted par moi » n'est jamais touché, piloté par les actions locales). Ce n'était PAS un problème de transport (le WS ne l'aurait pas corrigé). (2) UI façon X : nouveau composant partagé `components/feed/animated-count.tsx` (`AnimatedCount` + `formatCount` consolidé, dédupliqué de `post-card`/`post-actions`) — petit « roll » vertical clippé à chaque changement de valeur (montée si hausse, descente sinon ; `key` force le rejeu sur hausses consécutives ; masqué si ≤ 0 mais reste monté pour animer le 0→1). Keyframes `count-up`/`count-down` dans `globals.css`. (3) Vue détail plus vive : `STATS_POLL_INTERVAL_DETAIL_MS=3500` (un seul post → coût négligeable) passé à `usePostStatsPolling` dans `PostDetail`. Choix transport tranché (défense) : **polling selon la criticité de la donnée** (compteurs = non urgents/flous → polling ; messages/notifs = critiques → WS), pas « tout en WS » par réflexe. Vérifs : `tsc --noEmit` OK, `eslint` OK, `vitest` 110 OK. Aucun changement backend → Swagger non requis.*

*Last updated: 15/06/2026 — feat(feed) : **compteurs dynamiques (likes/commentaires/reposts) par polling batch périodique, façon X**. Suite logique du bandeau « a posté » temps réel, mais volontairement PAS à la milliseconde : les compteurs des posts AFFICHÉS sont rafraîchis par lots toutes les ~7 s, sans recharger les posts ni toucher l'état « moi » (liked/reposted/bookmarked, piloté par les actions locales optimistes). Choix **polling batch** plutôt que push WebSocket : à notre échelle, 1 seule requête `GET /posts/stats?ids=…` pour toute la page (≠ 1/tweet) est négligeable, alors que le hub WS actuel diffuse à TOUTES les connexions (volume + complexité de coalescence injustifiés). On reprend 2 des 3 idées de X (lazy = uniquement les posts affichés ; batché), pas les deltas (valeurs absolues = plus simple/robuste à notre échelle). **Backend (post-service)** : nouvel endpoint léger `GET /posts/stats?ids=a,b,c` (`optionalAuth`, route STATIQUE avant `/:id`) → `models.PostStat{id, likes_count, comments_count, reposts_count}`. Repo `StatsByIDs` = un seul `$in` projeté sur les compteurs + `author_id` (aucune lecture du contenu/médias), filtre `is_hidden $ne true`. Service `PostStats` réapplique la MÊME barrière de visibilité que le fil (`canReadAuthor`, mémoïsée par auteur) → posts invisibles simplement absents (pas d'erreur) ; borne `MaxStatsIDs=100`. **Frontend** : `getPostsStats(ids)` (map id→compteurs), helpers PURS `applyStatsToPost`/`applyStatsToPosts` (ne touchent que les 3 compteurs, préservent la référence si rien n'a changé → zéro re-rendu inutile), hook réutilisable `usePostStatsPolling(getIds, onStats)` (`STATS_POLL_INTERVAL_MS=7000`, ne sonde que si `!document.hidden`, refetch immédiat au retour au premier plan, garde anti-chevauchement `inFlight`). Branché dans `FeedView`, `PostDetail` et `ProfilView` (onglets Posts/J'aime/Réponses, union des ids). i18n : aucun string nouveau. Vérifs : post-service `go build`/`go vet`/`go test ./internal/...` OK, `gofmt` propre ; frontend `tsc --noEmit` OK, `eslint` OK, `vitest` 110 OK (nouveau `post-stats.test.ts`, 5 cas) ; `make swagger` régénéré → `/posts/stats` présent ; `graphify update .` OK. Détails DECISIONS « Compteurs dynamiques (polling batch) ».*

*Last updated: 15/06/2026 — fix(feed) : **restauration du scroll au retour depuis le détail d'un post**. Front-only, zéro backend. Au clic sur un post, `FeedView` se démonte → la liste paginée et la position de scroll sont perdues au retour. Correctif : store en mémoire module-level (`feedCache: Map<string, FeedSnapshot>` + `lastFeedTab`) dans `feed-view.tsx`. Trois ajouts dans `FeedView` : (1) listener `scroll` passif → `scrollYRef.current` toujours à jour ; (2) `useEffect` cleanup (via `saveRef` qui lit toujours les valeurs courantes) → sauvegarde `{posts, offset, hasMore, tab, scrollY}` à l'unmount, clé `"tab|hashtag|hashtagTab"` ; (3) `useLayoutEffect` sans deps → joue le scroll différé après le premier rendu avec la liste restaurée. Le `useEffect` de chargement initial vérifie le cache avant de lancer le fetch ; `restoredRef` empêche la restauration sur les changements d'onglet ultérieurs (qui doivent refetcher). `lastFeedTab` module-level initialise le `useState<FeedTab>` au bon onglet dès le montage (sinon la clé de cache ne correspond pas si l'utilisateur était sur « Abonnements »). Le cache vit le temps de la session SPA et s'efface sur hard-reload. `npx tsc --noEmit` OK, `npm run lint --max-warnings=0` OK. Aucun changement de route/handler → Swagger non requis.*

*Last updated: 15/06/2026 — feat(feed) : **fil temps réel — bandeau « a posté » (style X)**. Quand un AUTRE utilisateur publie pendant qu'on consulte le fil, un bandeau flottant (avatar + « a posté ») apparaît en haut ; au clic, il révèle les nouveautés (refetch de la 1re page de l'onglet courant, prepend dédupliqué) et remonte en haut. **Backend (post-service)** : nouveau hub de DIFFUSION `internal/realtime/hub.go` (ensemble de connexions, pas indexé par user comme le notification-service) + `internal/handler/ws_handler.go` exposant `GET /posts/ws?access_token=<jwt>` (auth via query param, le navigateur n'autorise pas d'en-tête sur un handshake WS) ; `middleware.ParseToken` exporté pour le handshake. `CreatePost` appelle `broadcastNewPost(authorID, postID)` : fire-and-forget en goroutine, **diffusé uniquement si le compte est public** (`profilClient.Visibility`) — un post de compte privé ne doit pas pinguer les non-abonnés. Le ping ne porte que `{type, post_id, author_id}` ; le contenu reste servi par le fil authentifié normal (barrière de visibilité server-side). Hub injecté via `WithFeedBroadcaster` (no-op par défaut → service testable). `CORS_ALLOWED_ORIGINS` ajouté à la config post-service (origines autorisées pour l'upgrade WS, défaut `http://localhost:3000`). La gateway proxifie déjà l'upgrade WS sous `/posts` (httputil reverse proxy) → aucune modif gateway. **Frontend** : `connectFeedRealtime(onNewPost)` + `getPostAuthor(userId)` exportés de `posts.ts` (reconnexion backoff calquée sur `connectNotifications`). `FeedView` accumule les pings (dédup, exclut mes posts/le filtre hashtag/les posts déjà présents ; en onglet « Abonnements », filtre via `getFollowingIds`), affiche le bandeau (`feed.new_posts` FR « a posté » / EN « posted ») et le révèle via `revealPending`. i18n FR/EN parité. Vérifs : post-service `go build`/`go vet`/`go test ./internal/...` OK, `gofmt` propre ; frontend `tsc --noEmit` OK, `eslint` OK, `vitest` 105 OK ; `make swagger` régénéré → `/posts/ws` présent dans `doc/openapi.{json,yaml}`. Détails DECISIONS « Fil temps réel (WebSocket) ».*

*Last updated: 14/06/2026 — feat(observability) : **logs métier slog sur les 7 services** (user, profil, post, message, notification, media, mail). Même barème que auth-service : ERROR = échec inattendu, WARN = refus notable/best-effort raté, INFO = événement métier faible volume, DEBUG = WS connect/disconnect. Ajout de `logging.FromGin(c)` dans le package logging de chaque service (helper enrichi `request_id`+`user_id`). Points clés : user → provisioning paresseux (ERROR), follow/unfollow/accept/reject (INFO), conflits username/cooldown (WARN), actions admin (INFO) ; profil → `respondProfilError` default (ERROR — reproduit le diagnostic du bug prod `visibility` legacy), migration boot `backfillVisibility` log count si > 0 (INFO), cooldown display_name (WARN) ; post → create/delete/restore/purge (INFO), barrière visibilité privée (WARN), dépendance inter-services indisponible (WARN) ; message → create conv/delete group/add-remove member/change role/purge RGPD (INFO), erreur inattendue (ERROR), WS connect/disconnect (DEBUG, hub.go + ws_handler.go) — E2EE : métadonnées uniquement, jamais le ciphertext ; notification → événement reçu (DEBUG) / erreur traitement (ERROR), `log.Printf` → `slog.Warn` (résolution handle + fan-out mention), WS connect/disconnect (DEBUG) ; media → upload refusé type/taille (WARN), échec stockage MinIO (ERROR), upload réussi (INFO), delete refusé non-propriétaire (WARN), purge RGPD (INFO) ; mail → `log.Printf` → `slog.Error` (envoi échoué), INFO envoi réussi. Vérifs : `go build ./...` + `go vet ./...` + `golangci-lint run` (0 issue) + `go test -race ./...` OK sur les 7 services.*

*Last updated: 13/06/2026 — feat(observability) : **logging structuré slog répliqué sur les 7 services restants** (auth-service, profil-service, post-service, message-service, notification-service, media-service, mail-service). Même motif que user-service : `internal/logging/logging.go` (`Setup(service)` — JSON release / texte debug, `LOG_LEVEL`, attribut `service`), `internal/middleware/logging.go` (`RequestID`/`RequestLogger`/`Recovery` identiques). Câblage : `gin.Default()` → `gin.New()` + `RequestID()→Recovery()→RequestLogger()` dans `main.go` (profil/post/message/notification/media/mail) ou `router.go` (auth). `logging.Setup(serviceName)` en tête de `main.go`, tous les `log.*` → `slog.*`, boot fatals → `slog.Error` + `os.Exit(1)`. `JWT.go` de chaque service (sauf mail, sans JWT) : `c.Set("user_id", claims.UserID)` ajouté après `c.Set(contextKey, claims)` — y compris `OptionalJWTAuth` de post-service. mail-service : nouveau répertoire `internal/middleware/` créé (il n'en avait pas). `LOG_LEVEL=info` ajouté à chaque `<svc>/.env.example`. Vérifs service par service : `go build ./...` + `go vet ./...` + `go test -race ./...` — tous 7 OK. golangci-lint non disponible sur cet hôte (indiqué pour CI).*

*Last updated: 13/06/2026 — feat(observability) : **logging slog étendu à api-gateway**. Même motif que user-service (`internal/logging`, `RequestID`/`RequestLogger`/`Recovery`). Spécificité gateway : `RequestID` propage l'id en aval (`c.Request.Header.Set`) pour la corrélation inter-services ; `RequestLogger` ajoute `upstream` (premier segment du chemin). `proxy.go` : `log.Printf` → `slog.Error`. `router.go` : `gin.Default()` → `gin.New()`, ordre CORS→RequestID→Recovery→RequestLogger. `main.go` : `logging.Setup` en premier, `log.*` → `slog.*` + `os.Exit(1)`. Vérifs : `go build ./...` OK, `go vet ./...` OK, `go test -race ./...` OK.*

*Last updated: 13/06/2026 — feat(observability) : **logging structuré slog + corrélation X-Request-Id sur user-service**. Nouveau package `internal/logging` (`Setup(service)` : handler JSON release / texte debug, niveau `LOG_LEVEL`, attribut `service` sur toutes les lignes). Nouveau `internal/middleware/logging.go` : `RequestID` (lit ou génère 16 B hex, pose `request_id` dans contexte + réponse), `RequestLogger` (une ligne/requête, attributs method/path/status/latency_ms/client_ip/request_id/user_id, niveau error≥500/warn≥400/info sinon), `Recovery` (panic → `slog.Error` + 500). `JWTAuth` pose désormais `user_id` dans le contexte Gin pour corrélation. `router.go` : `gin.Default()` → `gin.New()` + `RequestID`, `Recovery`, `RequestLogger` dans cet ordre. `main.go` : `logging.Setup` en premier, `log.*` → `slog.*` + `os.Exit(1)`. `.env.example` : `LOG_LEVEL=info`. Vérifs : `go build ./...` OK, `go vet ./...` OK, `golangci-lint run` 0 issue, `go test -race ./...` OK (middleware + service).*

*Last updated: 12/06/2026 — ui(profil) : **commentaires de l'onglet Réponses façon feed (encadrés + avatar)**. `CommentRow` (avatar + identité + horodatage + contenu + galerie média) est extrait/exporté de `comment-section.tsx` (`onDelete`/`footer` optionnels, liens profil avec `stopPropagation` pour rester cliquables sous une carte). `ReplyCard` n'affiche plus des blocs texte bruts mais une **carte encadrée** (`rounded-2xl border bg-card/30`) cliquable (`role="link"` + `router.push`) contenant le commentaire parent (si réponse imbriquée) relié par un fil vertical, puis la réponse, tous deux rendus via `CommentRow` — même rendu que dans le feed. Vérifs : `npx tsc --noEmit` OK, `eslint` OK. Aucun changement backend/Swagger.*

*Last updated: 12/06/2026 — fix(profil/notifications) : **3 correctifs Réponses/deep-link**. (1) Notif `comment` (commentaire racine) n'émettait aucun `comment_id` (post-service `emitCommentEvents`) → le deep-link retombait sur le post ; ajout `CommentID: commentID` (l'agrégation reste `comment:<post_id>`, l'`Upsert` stocke le dernier commentaire → clic = dernier commentaire). Les notifs existantes (avant fix) restent sans ancre. (2) Le `PostCard` parent dans l'onglet Réponses portait l'ombre `.glass` ; nouvelle prop `embedded` sur `PostCard` qui retire le chrome flottant (glass/ombre/marges/hover), utilisée par `ReplyCard`. (3) Réponses imbriquées : le backend hydrate désormais le **commentaire parent** (`CommentWithPost.ParentComment`, via `repo.GetComment` si `ParentID` non vide) ; front `ReplyContext.parentComment` + `ReplyCard` affiche post → commentaire parent → réponse, et le libellé « En réponse à @X » cible l'auteur du commentaire parent quand il existe. Vérifs : post-service `go build ./...` + `go test ./internal/...` OK, `npx tsc --noEmit` OK, `eslint` OK, `make swagger` OK (parent_comment dans la spec).*

*Last updated: 12/06/2026 — feat(profil/notifications) : **onglet Réponses façon conversation + deep-link notification vers le commentaire**. (1) Réponses : `ReplyContext` porte désormais le post parent complet (`parentPost: FeedPost | null`) ; `listCommentsByAuthor` mappe `parent_post` (déjà renvoyé par l'API, jusqu'ici ignoré) via `toFeedPost` en récupérant une fois les ids liké/reposté/signé. `ReplyCard` rend le `PostCard` parent interactif en haut, puis la réponse en dessous reliée par un fil (`ml-7 border-l-2`), cliquable vers `/posts/:id?comment=:commentId`. Fallback `profil.replies_deleted_parent` (FR+EN) si post parent indisponible. (2) Deep-link : `notificationHref` route les notifs `comment`/`reply` vers `/posts/:postId?comment=:commentId` (sinon post seul). `post-detail` lit `?comment` → `focusCommentId` → `PostCard` (auto-ouvre les commentaires) → `CommentSection` : ancres `id="comment-<id>"` sur chaque ligne (racine + réponses), effet scroll + surbrillance (ring) avec retry ~4s, et `CommentThread` auto-déplie en silence le thread contenant la réponse ciblée. Aucun changement backend/Swagger. Vérifs : `npx tsc --noEmit` OK, `eslint` OK, `vitest src/lib/notifications.test.ts` 12 OK. `npm run build` bloqué par permissions root sur `frontend/.next` (fichiers du conteneur dev) — code déjà hot-reloadé en dev ; build prod nécessite `sudo rm -rf frontend/.next`.*

*Last updated: 15/06/2026 — feat(i18n) : **12 langues, préférence persistée par compte et traduction de posts réparée**. Ajout de ZH/ES/PT/RU/JA/KO/AR/HI/DE/IT avec 677 clés chacune et audit de parité/placeholders. user-service porte `preferred_locale` nullable et contraint via migration PostgreSQL idempotente ; `GET/PATCH /users/me` lit/écrit la préférence, qui survit au logout et reste propre à chaque compte. Sans préférence, le frontend utilise `navigator.language`; le stockage local est réservé aux visiteurs. `LanguageProvider` se resynchronise au login/refresh/logout et pose RTL pour l'arabe. `TranslatedContent` réagit aux changements de locale et ses libellés sont i18n. Détection des posts rendue conservative : scripts non latins + marqueurs clairs, avec tests pour `je mapelle maxime` et `OMG ... jsuis chokbar` (pas de traduction). Pages légales FR/EN avec repli EN. Swagger `PATCH /users/me` régénéré. Vérifs : user-service `go test . ./internal/...`, migration réelle sur PostgreSQL existant, frontend `tsc`, ESLint, 107 tests et build Next production OK.*

*Last updated: 12/06/2026 — feat(profil) : **onglet Likes + préférence likes_visibility**. profil-service : ajout du champ `likes_visibility` (public|private, défaut public) au modèle Profil, au validateur `$jsonSchema`, au seed admin et à `planUpdate` (via `PATCH /profils/me`). Nouveau handler `GET /profils/:userId/likes-visibility` (route statique avant `/:userId`). post-service : `ProfilClient.LikesVisibility(ctx, userID)` + interface `profilVisibilityClient` étendue. Repo `LikedPostsByUser` (agrégation likes → post_ids triés par date de like DESC → fetch posts `$in` + filtre `is_hidden`, reorder préservé). Service `ListLikedByUser` (check `LikesVisibility` → `ErrForbidden` si private + caller ≠ owner). Handler + route STATIQUE `GET /posts/liked?author_id=` (optionalAuth, avant `/:id`). Frontend : `likesVisibility` dans `ProfilDetails` + `ApiProfil`, `saveMyLikesVisibility` dans `profil-client.ts`, `listLikedByUser` dans `posts.ts` (erreur 403 typée). `profil-view.tsx` : onglet Likes branché avec lazy load, état `likesPrivateLocked`, composant `LikesPrivateTab`. `visibility-settings.tsx` : second toggle `likes_visibility`. i18n FR + EN (`profil.likes_private_*`, `visibility.likes_*`). Vérifs : profil-service `go test ./internal/...` OK, post-service `go test ./internal/...` OK, `npx tsc --noEmit` OK, `make swagger` OK.*

*Last updated: 12/06/2026 — fix(profil) : **édition de profil impossible en prod (500) — `visibility` legacy vide**. Symptôme : en online (`breezy.philippeluu.fr`), tout `PATCH /profils/me` (photo, display_name, n'importe quoi) renvoyait `500 erreur interne` ; OK en local. **Cause racine** (confirmée via les logs prod + scan Mongo) : 16 anciens profils, créés avant l'ajout de `visibility` (PR #204), portaient `visibility: ""`, qui viole l'enum `{public, private}` du validateur `$jsonSchema`. Comme Mongo en `validationLevel: strict` revalide le **document entier** à chaque écriture, ces comptes ne pouvaient plus être édités du tout (`DocumentValidationFailure` code 121 → mappé en 500 opaque par `respondProfilError`). Local = données propres (toutes `public`) → invisible. **Correctif** : (1) `normalizeLegacyProfiles` dans `EnsureSchema` (profil-service) — au boot, repasse idempotemment tout `visibility` vide/absent à `public` (`bypassDocumentValidation`), même esprit que `dropLegacyIndexes`/`ensureSeed` → la prod s'auto-répare au déploiement ; (2) `respondProfilError` logue désormais l'erreur **brute** dans la branche 500 par défaut (au lieu de la masquer) pour rendre tout futur cas diagnosticable. Réparation immédiate de la prod via `updateMany` manuel (16 profils corrigés) en attendant le redéploiement. Aucun changement de route/handler/annotation → Swagger non requis. Vérifs : profil-service `go build`/`go vet`/`go test ./internal/...` OK, `gofmt` propre ; migration validée sur la base Mongo locale (normalise, idempotente 2e passage = 0, doc réparé éditable sous validateur strict). Détails DECISIONS « Legacy data normalization at boot ».*

*Last updated: 12/06/2026 — feat(admin) : **création de compte par un admin + mots de passe/usernames provisoires**. Un administrateur peut créer un compte de force depuis la page `/admin` (bouton « Créer un compte ») : username + e-mail + mot de passe temporaire. Back : auth-service `POST /auth/users` (AdminOnly) crée les credentials avec `must_change_password=true` et **vérification d'e-mail obligatoire** (`email_verified=false` en prod) : l'e-mail de bienvenue porte le lien de vérification + le mot de passe temporaire ; cliquer le lien vérifie l'adresse ET ouvre la session (`VerifyEmail`, qui propage désormais `must_change_password`) → l'utilisateur atterrit sur le feed avec la modale de changement de mot de passe. **Court-circuit DEV/LOCAL** via `ADMIN_CREATE_AUTO_VERIFY=true` (mail réel en ligne) : compte vérifié d'office → connexion directe au mot de passe temporaire. `must_change_password` est propagé dans le JWT (claims) et survit au refresh ; nouveau `POST /auth/password/change` (JWT) vérifie le mot de passe actuel, lève le drapeau, révoque les autres sessions et ré-émet une paire de tokens. user-service `POST /users/admin` (AdminOnly) crée la ligne `users` pour l'id retourné ; si le username est pris, il est suffixé `_<8 hex>` et `username_pending=true` (colonne exposée par `/users/me`, repassée à false sur changement effectif via `PATCH /users/me`). profil-service `POST /profils/admin` (admin) crée le profil (display_name = username effectif) pour neutraliser le gate d'onboarding. Front : `lib/admin.createAccount` orchestre les 3 services (bearer admin, comme `hardDeleteUser`) ; BFF `POST /api/auth/password/change` (relaie le bearer, fait tourner le cookie refresh) ; deux modales bloquantes montées dans le layout `(app)` — `PasswordChangeGate` (drapeau JWT, prioritaire) puis `UsernamePendingGate` (`/users/me`), s'affichant sans rechargement. i18n FR/EN `admin.create.*` / `account.password_change.*` / `account.username_pending.*`. Vérifs : `go build`/`go vet`/`go test ./internal/...` OK sur auth/user/profil, `gofmt` propre, Swagger régénéré (`make swagger`) → `doc/openapi.{json,yaml}` exposent `/auth/users`, `/auth/password/change`, `/users/admin`, `/profils/admin`. tsc/lint/build frontend non lançables localement (node_modules root-owned).*

*Last updated: 12/06/2026 — fix(feed/sidebar) : **display name complet dans `Qui suivre`**. En mode compact de la sidebar droite du feed, le `displayName` n'est plus tronqué sur une seule ligne : il peut maintenant prendre jusqu'à 2 lignes avec retour à la ligne, tout en gardant le bouton `Suivre/Suivi` stable à droite. Le changement reste limité au mode compact du bloc `Qui suivre`. Vérif : frontend `npm run build` OK.*

*Last updated: 12/06/2026 — fix(feed/explorer) : **cartes de droite plus responsives + tendances Explorer alignées sur le feed**. La sidebar droite ajuste maintenant mieux sa largeur (`w-[min(350px,30vw)]` avec min-width), le bloc Tendances garde le compteur de posts complètement à droite sur la ligne principale du hashtag, et `Qui suivre` stabilise la largeur du bouton `Suivi/Suivre` pour éviter qu'il pousse le texte dans les petits espaces. Dans Explorer, chaque ligne de tendance reprend le même pattern visuel que le feed : rang au-dessus, hashtag à gauche, nombre de posts tout à droite. Front-only, aucun changement route/handler backend, donc pas de Swagger. Vérif : frontend `npm run build` OK.*

*Last updated: 12/06/2026 — fix(feed/sidebar) : **correctif de régression CSS sur la sidebar droite**. Le premier compactage avait modifié le layout par défaut du composant partagé `UserListItem`, ce qui pouvait casser le rendu sur d'autres listes. Le correctif remet le comportement standard intact (`items-start`, marges bouton inchangées hors sidebar) et limite désormais les ajustements au seul mode `compact` utilisé dans `Qui suivre`. La sidebar conserve le scroll vertical et le bouton `Suivi`, avec retour à des classes de hauteur plus conservatrices (`h-screen`) pour éviter la casse visuelle. Vérif : frontend `npm run build` OK.*

*Last updated: 12/06/2026 — fix(feed/sidebar) : **cartes Tendances et Qui suivre compatibles zoom**. La colonne droite utilise maintenant une hauteur viewport dynamique (`h-dvh`/`max-h-dvh`), un scroll interne fiable avec padding bas de sécurité, et des espacements réduits. Le bloc Tendances garde le Top 5 mais compacte chaque item en grille courte pour éviter que la dernière tendance passe derrière le bord au zoom. Le bloc Qui suivre affiche trois lignes réellement visibles en mode compact (avatar/texte/bouton resserrés) et conserve le trio initial après clic sur Suivre ; le bouton passe à `Suivi` sans remplacer immédiatement la suggestion. Aucun changement route/handler backend → Swagger non requis. Vérif : frontend `npm run build` OK.*

*Last updated: 12/06/2026 — feat(profil): **onglet Réponses du profil**. Backend post-service : nouveau `GET /posts/comments?author_id=<id>` (optionalAuth, route statique placée avant `/:id`), repo `ListCommentsByAuthor` (tri `created_at` DESC paginé), service `ListCommentsByAuthor` (hydrate le post parent par `post_id`, applique la barrière de visibilité existante `canReadAuthor` — une réponse dont le post parent est masqué ou sur un profil privé inaccessible est silencieusement exclue), handler avec annotation swaggo, Swagger régénéré. Nouveau modèle `CommentWithPost` (embed `Comment` + `ParentPost *Post`). Frontend : type `ReplyContext` + `listCommentsByAuthor` dans `lib/posts.ts` (résout l'auteur du post parent via `resolveAuthor`), onglet « Réponses » dans `profil-view.tsx` branché sur le nouvel endpoint (lazy load au premier clic), `ReplyCard` avec label « En réponse à @X » (icône MessageCircle, cliquable → `/posts/:postId`), i18n `profil.replies_in_reply_to` FR+EN. Vérifs : `go build ./...` OK, `go test ./internal/...` OK, `npx tsc --noEmit` OK, `make swagger` OK. `npm run build` bloqué par permissions root sur `.next` (fichiers créés par le conteneur Docker) — nettoyage requis : `sudo rm -rf frontend/.next`.*

*Last updated: 12/06/2026 — feat(explorer) : **Explorer devient une vraie page découverte**. Vue par défaut : section Top 10 tendances cliquables, bloc Suggestions avec cartes profils (hover profile existant via `ProfilLink`, bouton suivre, contexte "Suivi par …" via abonnés communs best-effort) limité aux profils que l'utilisateur ne suit pas encore, puis section **Publications** basée sur le feed classique (`listFeed`) et non plus seulement sur les posts avec hashtag. Search Explorer : pendant la saisie, le menu opaque affiche directement les tendances/profils trouvés ; les actions `Rechercher "..."` / `Aller à @...` n'apparaissent que si aucune tendance/personne ne matche, avec `Aucun résultat pour "..."`. Appuyer sur Entrée ouvre une vue résultats avec filtres : desktop affiche le bloc **Filtrer** dans la sidebar droite au-dessus du Top 5 tendances ; mobile affiche un bouton `...` ouvrant le menu **Filtrer**. **Publications** activé par défaut, **Utilisateurs** activable, les deux pouvant être cochés ensemble et au moins un restant obligatoire. Les publications sont filtrées côté client sur contenu/auteur/hashtags depuis le feed visible. Backend post-service ajoute `GET /posts?hashtag_any=true` pour récupérer les posts visibles ayant au moins un hashtag ; Swagger annoté/régénéré. Vérifs : post-service `go test . ./internal/...` OK, frontend `npm run build` OK, `doc/openapi.{json,yaml}` régénérés via conteneur Linux. Follow-ups validés : `npm run build` frontend OK, cache `.next` nettoyé après build pour le dev container jusqu'au dernier follow-up ; dernier cleanup/restart/runtime check bloqué par la limite d'usage de l'approbation automatique ; `graphify update .` impossible car `graphify` n'est pas installé / non relancé sur le dernier passage.*

*Last updated: 12/06/2026 — feat(composer) : **autocomplétion hashtag dans la rédaction de posts**. Le `PostComposer` détecte maintenant le token `#...` sous le curseur comme les mentions `@`, interroge les tendances existantes via `/posts/trends?q=...`, affiche une popup de suggestions hashtags, et insère `#hashtag ` avec l'espace final au clic / Entrée / Tab. Pendant la saisie, un rendu miroir colore et souligne les hashtags en bleu dans le textarea sans changer le contenu envoyé. Ajout d'utilitaires purs `hashtags.ts` + tests Vitest. Aucun changement API/Swagger requis. Vérifs : `npm test -- --run src/lib/hashtags.test.ts` OK, `npm run build` frontend OK.*

*Last updated: 11/06/2026 — feat(search) : **barre Tendances alignée sur la recherche hashtag + historique récent**. La recherche de la colonne droite réutilise maintenant la dropdown façon X : pendant la saisie, 3 hashtags suggérés (`/posts/trends?q=...&limit=3`) puis des profils ; clic hashtag/profil navigue directement et mémorise le choix. Quand le champ est vide et focus, les recherches récentes s'affichent avec `Tout effacer` et suppression ligne par ligne, stockées localement par utilisateur (`breezy-suggestion-history`). Aucun changement API/Swagger requis. Vérif : `npm run build` frontend OK.*

*Last updated: 11/06/2026 — feat(feed/search) : **dropdown de recherche tendances façon X**. Sur une page hashtag, la barre de recherche ouvre désormais un menu sombre sous le champ pendant la saisie : 3 premières suggestions de hashtags (`GET /posts/trends?q=...&limit=3`, visibilité conservée), puis des profils utilisateurs. Clic hashtag → `/feed?hashtag=...&tab=top`; clic profil → `/profil/<username>` directement, sans passer par Explorer. Explorer et la dropdown trouvent maintenant les comptes avec `@felipe` **ou** `felipe` (recherche username + display_name fusionnée côté front). Swagger annoté/régénéré pour le paramètre `q` de `/posts/trends`. Vérifs : `docker run ... go test . ./internal/...` post-service OK, `npm run build` frontend OK, `doc/openapi.{json,yaml}` régénérés via conteneur Linux.*

*Last updated: 11/06/2026 — feat(feed/search) : **recherche profils + hashtags unifiée et tendances Top 5 ajustées**. La barre du bloc hashtag place maintenant le bouton retour à gauche et route les recherches via une seule règle : `#tag` ouvre `/feed?hashtag=...&tab=top`, une recherche texte ouvre Explorer pour trouver des profils. Explorer accepte aussi les requêtes `#hashtag` et propose un résultat hashtag cliquable. `À la une` trie les posts hashtag par `likes_count`, puis `reposts_count`, puis commentaires/récence ; Swagger a été régénéré pour refléter ce contrat. Le bloc Tendances droite reste limité au Top 5 mais avec espacements réduits pour éviter que le 5e item soit coupé. Vérifs : `docker run ... go test . ./internal/...` post-service OK, `npm run build` frontend OK, `doc/openapi.{json,yaml}` régénérés via conteneur Linux.*

*Last updated: 11/06/2026 — feat(feed) : **vue hashtag avec onglets À la une / Récent / Média**. `GET /posts` accepte maintenant `sort=top|recent` (Swagger mis à jour) ; `top` trie les posts hashtag par engagement (`likes_count`, `reposts_count`, `comments_count`) puis récence, tandis que `recent` conserve le tri chronologique. Front : sur `/feed?hashtag=...`, le bandeau de filtre devient une barre de recherche hashtag avec bouton retour ; les onglets `À la une`, `Récent`, `Média` pilotent l'URL (`tab=top|recent|media`) ; `Média` affiche une grille d'images/vidéos issues des posts du hashtag, sans cartes de posts. Le bloc Tendances reste limité au Top 5 et ouvre l'onglet `À la une`. Vérifs : `docker run ... go test . ./internal/...` post-service OK (`go test ./...` complet bloqué par le package généré `docs`/`swag`, connu après Swagger), `npm run build` frontend OK, `doc/openapi.{json,yaml}` régénérés via conteneur Linux.*

*Last updated: 11/06/2026 — feat(posts) : **hashtags cliquables + feed filtré + tendances dynamiques**. post-service extrait les hashtags sur `CreatePost`/`UpdatePost` (`hashtags: []string` normalisés, index Mongo), expose `GET /posts?hashtag=...` sur les modes global/profil/abonnements et `GET /posts/trends` avec comptage des hashtags après barrière de visibilité profil (pas de fuite via compte privé). Front : `FeedView` lit `?hashtag=...`, recharge les pages filtrées et affiche un bandeau de filtre ; `MentionText` rend les `#tags` cliquables vers `/feed?hashtag=...` ; la colonne droite Tendances appelle le backend et remplace les placeholders. Swagger annoté et `doc/openapi.{json,yaml}` régénérés via conteneur Linux (`make swagger` local échoue toujours sur bash Windows). Vérifs : `docker run ... go test ./...` post-service OK, `npm run build` frontend OK.*

*Last updated: 12/06/2026 — ux(posts privacy) : **pas de cadenas privé sur ses propres posts**. Le badge `LockOpen` des auteurs privés est maintenant masqué quand l'auteur du post est l'utilisateur connecté, y compris dans les citations et la modale photo. Vérifs : `npx tsc --noEmit`, `npm run lint`.*

*Last updated: 12/06/2026 — ux(posts privacy) : **badge cadenas ouvert sur les posts d'un compte privé visible**. Les auteurs enrichis des posts transportent désormais `visibility`; `PostCard`, les citations et la modale photo affichent un `LockOpen` près du pseudo quand l'auteur est privé, avec infobulle « Ce compte est privé ». Le texte du switch “Compte privé” dans `/parametres` reste stable pendant la sauvegarde (plus de remplacement par “Enregistrement…”). Vérifs : `npx tsc --noEmit`, `npm run lint`.*

*Last updated: 12/06/2026 — ux(settings visibility) : **contraste renforcé sur l'interrupteur désactivé**. État OFF du switch “Compte privé” plus lisible : piste grise plus marquée, bordure visible et curseur blanc avec léger ring, y compris en dark mode. Vérifs : `npx tsc --noEmit`, `npm run lint`.*

*Last updated: 12/06/2026 — ux(settings visibility) : **interrupteur iPhone “Compte privé”**. Le réglage de visibilité dans `/parametres` est maintenant un simple switch on/off : OFF = profil public, ON = profil privé, avec fond coloré quand activé et cadenas sur le curseur. Vérifs : `npx tsc --noEmit`, `npm run lint`.*

*Last updated: 12/06/2026 — ux(settings visibility) : **le réglage de visibilité reprend le style du changement d'apparence**. Le bloc `/parametres` n'affiche plus deux gros boutons public/privé : il utilise un switch compact avec curseur animé, œil côté public et cadenas côté privé, état optimiste et toast conservés. Vérifs : `npx tsc --noEmit`, `npm run lint`.*

*Last updated: 12/06/2026 — feat(profile visibility settings) : **nouveaux profils publics par défaut + réglage public/privé dans `/parametres`**. Back profil-service : `Create` pose explicitement `visibility=public`, `UpdateProfilRequest` accepte `visibility` (`public|private`), `planUpdate`/validateur Mongo/seed admin mis à jour + test unitaire. Front : nouveau `VisibilitySettings` dans les paramètres, `saveMyVisibility` (`PATCH /profils/me`), état optimiste + toast, i18n FR/EN. Vérifs : `go test ./...` profil-service, `npx tsc --noEmit`, `npm run lint`.*

*Last updated: 11/06/2026 — perf(messages) : **dérivation Argon2id dans un Web Worker (UI réactive)**. La passphrase étant dérivée hors du thread principal, l'interface ne gèle plus pendant le calcul (spinner animé, page réactive). `key-backup.ts` scindé en partie lente (`deriveKEK`) / partie rapide (`wrapWithKEK`/`unwrapWithKEK`) ; nouveau `key-backup.worker.ts` (importe uniquement `@noble/hashes/argon2`, bundle léger) + `key-backup-async.ts` (`createBackupAsync`/`openBackupAsync`, worker éphémère par appel, **fallback synchrone** si `Worker` indisponible). `messages.ts` utilise les variantes async. Vérifs : `vitest` (82/82), `tsc` propre.*

*Last updated: 11/06/2026 — fix(messages) : **déblocage de la passphrase instantané sur mobile**. Argon2id est synchrone (bloque le thread JS) → 64 Mio/3 passes gelait l'UI ~15-25 s sur téléphone (« tourne à l'infini », le déblocage finissait pourtant par aboutir en arrière-plan). Paramètres ramenés au **minimum OWASP (19 Mio / 2 passes)** (~0,4 s desktop, ~2-4 s mobile). **Auto-upgrade** : au déblocage, si la sauvegarde a été créée avec des paramètres plus lourds, elle est ré-emballée avec les paramètres légers (best-effort, non bloquant) → les anciennes sauvegardes 64 Mio se réparent seules. `sameKDFParams` ajouté + testé. Vérifs : `vitest` (16/16), `tsc` propre.*

*Last updated: 11/06/2026 — feat(messages) : **sauvegarde chiffrée de la clé E2EE par phrase de passe (multi-appareils)**. Jusqu'ici la clé privée d'identité vivait uniquement en IndexedDB → inaccessible sur un autre appareil/navigateur. Nouveau socle zero-knowledge : la clé privée est emballée côté client (`XChaCha20-Poly1305`) par une clé dérivée d'une **phrase de passe** (Argon2id, 64 Mio/3 passes) et sauvegardée côté serveur sous forme de blob opaque. Le serveur ne voit jamais la passphrase ni la clé → admin-proof DM préservé. **Back message-service** : collection `key_backups` (schéma + index unique `user_id` + purge RGPD), `PUT/GET /messages/keys/backup` + `GET /messages/keys/backup/status`, routes déclarées AVANT `/keys/:userId` (sinon `backup` capté comme userId). **Front** : `lib/key-backup.ts` (Argon2id wrap/unwrap, pur+testé), `lib/passphrase-strength.ts` (barre de complexité, pur+testé), **clé IndexedDB scopée par `userId`** (corrige le `self` global partagé entre comptes du même navigateur, qui empêchait le prompt et republiait la mauvaise clé) + migration de l'ancienne clé `self` adoptée seulement si sa clé publique == celle publiée par le compte, `messages.ts` ne génère plus de clé automatiquement (`getIdentityState` → `ready`/`unlock`/**`setup`** ; `setup` est aussi déclenché pour un **compte existant sans sauvegarde** → il sauvegarde sa clé actuelle ; `setupPassphrase`/`unlockWithPassphrase`, `IdentityLockedError`), `PassphraseGate` (overlay flouté définir/débloquer) intégré à `MessagesView`. i18n FR/EN `messages.passphrase.*`. Limites assumées : passphrase perdue = backup irrécupérable (zero-knowledge) ; reset admin non implémenté (sémantique « nouvelle clé » à acter) ; clé générée sur un appareil sans backup ⇒ historique antérieur illisible. Vérifs : `vitest` (14/14 nouveaux tests OK), `tsc --noEmit` propre sur le code touché. ⚠️ `make swagger` (annotations backup ajoutées) + `graphify update .` à rejouer en conteneur (Go indispo localement).*

*Last updated: 10/06/2026 — feat(auth login) : **connexion par adresse e-mail ou username dans le même champ**. Front login : champ unique “Adresse e-mail ou Username”, validation assouplie et i18n FR/EN dédiée. BFF `/api/auth/login` : détecte email vs username, résout les usernames via `GET /users/by-username/:username` à travers la gateway, puis appelle auth-service avec `email` ou `user_id`. Auth-service : `LoginRequest` accepte `email` ou `user_id`, vérification bcrypt inchangée, JWT/refresh identiques. Swagger annoté et `doc/openapi.{json,yaml}` régénérés via conteneur Go Linux car `make swagger` échoue localement à lancer `bash` sur Windows. Vérifs : `npm run build`, `docker run ... go test . ./internal/...` auth-service OK ; `go test ./...` complet auth-service bloqué par le package généré `docs` qui importe `github.com/swaggo/swag` absent du `go.mod`.*

*Last updated: 10/06/2026 — fix(frontend/feed) : **photo/nom de profil propagés instantanément dans le feed**. Le cache auteur de `lib/posts.ts` est maintenant resynchronisé quand `saveMyProfil` émet `breezy:profil-updated`, puis les posts déjà affichés sont remappés sans refetch, y compris les posts cités. `FeedView` et `ProfilView` écoutent cet event pour remplacer l’auteur visible immédiatement après modification du profil. Aucun changement de route/handler backend → Swagger non régénéré. Vérifs : `npm run lint`, `npx tsc --noEmit`, `npm test` (66/66).*

*Last updated: 10/06/2026 — feat(mail) Phase 2 : **mot de passe oublié (forgot + reset)**. auth-service (réutilise l’infra Phase 1, zéro nouvelle brique) : `models` ajoute `ForgotPasswordRequest{email}` + `ResetPasswordRequest{token,new_password min=8}` ; constantes `purposeReset=”reset”` + `resetTokenTTL=1h` ; `services` : `sendResetMail` (token `reset` TTL 1h → lien `APP_BASE_URL/reset-password?token=…`, sujet/HTML/texte FR, best-effort), `ForgotPassword(email)` (**anti-énumération : renvoie TOUJOURS nil** ; envoie le mail seulement si le compte existe ET est actif, sinon no-op silencieux), `ResetPassword(token,newPassword)` (`consumeAccountToken` purpose=reset → `ErrInvalidToken` si KO ; sinon bcrypt → `UPDATE credentials SET password, email_verified=true` puis **`DELETE refresh_tokens WHERE user_id` = révocation de toutes les sessions**). **Décision : `email_verified=true` posé au reset** — cliquer le lien prouve la possession de l’adresse, ce qui débloque un compte non vérifié. Handlers (`password.go`) : `ForgotPassword` → **toujours 200** `{message générique}` ; `ResetPassword` → 200 `{message}` ou `400 {error,code:”invalid_token”}` / 500. Routes **publiques** `/auth/password/{forgot,reset}` (pas de middleware JWT). Front : passthrough BFF `api/auth/password/forgot` (POST `{email}`) + `api/auth/password/reset` (POST `{token,new_password}`, aucun cookie) ; pages `(auth)` publiques `/forgot-password` (form email → écran de confirmation **générique** anti-énum) et `/reset-password` (lit `?token` au mount, form nouveau mdp + confirmation → succès `→ /login` / lien invalide-expiré-consommé `→ /forgot-password`, loading géré) ; lien « Mot de passe oublié ? » du login pointe désormais vers `/forgot-password` ; routes `lib/routes.ts` (`forgotPassword`/`resetPassword`) + i18n FR/EN (namespaces `auth.forgot.*` / `auth.reset.*`). Vérifs : auth-service `go build`/`go vet ./internal/...` OK, `make swagger` (routes `password/{forgot,reset}` dans `doc/`), front `npm run lint` OK + `npx tsc --noEmit` propre (hors artefact `.next/types` obsolète `oauth/[provider]`), `graphify update`. **E2E `make dev` à valider** (forgot → log mail montre le lien reset ; reset via lien → succès ; ancien access/refresh invalidé ; login nouveau mdp OK / ancien 401 ; rejouer le lien → 400 invalid_token ; email inexistant → 200 générique ; compte non vérifié débloqué). NE TOUCHE PAS : mail-service, flux verify-email, gateway. Phase 3 = rate-limiting, emails EN, templates HTML soignés, tests Go DB-coupled. Détails DECISIONS.md « Email (vérification & reset) ».*

*Last updated: 10/06/2026 — feat(mail) Phase 1 : **vérification d'e-mail (blocage dur au login)**. auth-service : nouveau client `internal/notify` (POST `{MAIL_SERVICE_URL}/internal/send`, header `X-Internal-Secret`, timeout 5s, best-effort — erreur loggée jamais propagée) ; `config` ajoute `MAIL_SERVICE_URL`/`MAIL_INTERNAL_SECRET`/`APP_BASE_URL` (secret absent → mailer nil = no-op loggé, auth bootable seul) ; `services` : helpers `createAccountToken` (invalide les tokens non consommés du même `(user_id,purpose)` puis insère un jeton opaque haché SHA-256, renvoie le clair) + `consumeAccountToken` (lookup par hash, refuse inconnu/expiré/`used_at`/mauvais purpose, pose `used_at`, usage unique) + `sendVerificationMail` (token `verify` TTL 24h → lien `APP_BASE_URL/verify-email?token=…`, FR) ; `Register` envoie le mail (best-effort) **en gardant l'émission des tokens** ; `Login` ajoute `email_verified` au SELECT/scan et renvoie `ErrEmailNotVerified` **après** le bcrypt ; `VerifyEmail`/`ResendVerification` (anti-énumération : renvoie toujours nil). Handlers : `login` mappe `ErrEmailNotVerified` → `403 {error,code:"email_not_verified"}` ; `verify.go` : `ConfirmVerifyEmail` (200 / `400 {code:"invalid_token"}`) + `RequestVerifyEmail` (toujours 200 générique) ; routes publiques `/auth/verify-email/{confirm,request}`. **Décision provisioning (cf. DECISIONS.md) :** `register` continue d'émettre les tokens, mais le BFF Next les utilise UNIQUEMENT côté serveur pour provisionner `users`+`profils` (username/birth_date/gender du formulaire) puis les JETTE (pas de cookie refresh, pas d'access token renvoyé) → **aucune session client**, blocage réel au login. Front : `api/auth/register` (201 `{emailVerificationRequired}`, redirige vers `/check-email`), `api/auth/login` (relaie `code:email_not_verified`), passthrough `api/auth/verify-email` + `/resend` ; pages `(auth)` publiques `/check-email` (« consulte ta boîte mail » + renvoi) et `/verify-email` (lit `?token` au mount, succès / invalide-expiré + renvoi) ; bannière login non-vérifié + bouton renvoyer ; routes `lib/routes.ts` + i18n FR/EN. Compose : env auth-service (`MAIL_SERVICE_URL`/`MAIL_INTERNAL_SECRET`/`APP_BASE_URL`) ; `auth-service/.env.example` (`MAIL_SERVICE_URL`) ; `make env-sync`. Vérifs : auth-service gofmt/vet/`go build` OK, `make swagger` (routes verify-email dans `doc/`), `docker compose config` (base+dev) OK, front `npm run lint` OK + `npx tsc --noEmit` propre (hors artefact `.next/types` obsolète root-owned `oauth/[provider]`), `graphify update`. **E2E `make dev` à valider** (register→check-email sans session, log mail, login bloqué `403`, lien→succès, login OK + provisioning, renvoi invalide l'ancien token, anti-énum). Reset password = **Phase 2**. Détails DECISIONS.md « Email (vérification & reset) ».*

*Last updated: 10/06/2026 — feat(mail) Phase 0 : **socle e-mail (mail-service + données auth)**. Nouveau micro-service `mail-service` (port 8089, Go/Gin, module autonome) : `POST /internal/send` (secret partagé `X-Internal-Secret` = `MAIL_INTERNAL_SECRET`, hors gateway, best-effort 202) + `/health` ; mailer à 2 transports — **SMTP via `net/smtp` (stdlib, STARTTLS auto sur Gmail 587, aucune dépendance ajoutée)** ou **repli console** (SMTP non configuré → e-mails loggés sur stdout, dev sans Gmail) ; transport content-agnostic (`{to,subject,html,text}`, à l'image du media-service). auth-service (PG) : colonne `email_verified BOOLEAN DEFAULT false` (CREATE + `ALTER … ADD COLUMN IF NOT EXISTS` → migration idempotente, les comptes existants passent à `false`) + table `account_tokens(purpose 'verify'|'reset', token_hash UNIQUE, used_at, expires_at)` (tokens opaques hachés usage-unique, même pattern que `refresh_tokens`) ; admin seedé forcé `email_verified=true` (`ON CONFLICT (id) DO UPDATE`). Câblage : `docker-compose` + `docker-compose.dev` (volumes air `go-mod/build-mail`) + `.env.example` racine (`MAIL_INTERNAL_SECRET`, `APP_BASE_URL`) + `mail-service/.env.example` (`SMTP_*` vides → console) + Makefile (`SERVICES`, `logs-mail`, `sh-mail`, `dev-logs`, help) + CI (`ci-go` matrice + paths, `ci-integration` `STACK_SERVICES` + boucle `.env`) + ligne ports `CLAUDE.md`. **Aucun changement de comportement visible** : le login reste inchangé tant que la Phase 1 n'active pas la barrière. Vérifs : `mail-service` gofmt/vet/`go build` OK + `go mod tidy` (go.sum généré), `docker compose -f … -f docker-compose.dev.yml config` (base+dev) OK, `graphify update`. Intégration auth→mail (register envoie le mail, login bloque les non-vérifiés) = **Phase 1**. Détails DECISIONS.md « Email (vérification & reset) ».*

*Last updated: 10/06/2026 — docs(api) : **Swagger Phase 3 — GitHub Pages Redoc UI**. `doc/index.html` (Redoc CDN, charge `./openapi.json`). Workflow `.github/workflows/pages.yml` : déclenchement `push develop` + `workflow_dispatch`, permissions `pages:write`/`id-token:write`, `actions/configure-pages@v5` + `upload-pages-artifact@v3` (source `doc/`) + `deploy-pages@v4`. Cible `make swagger-site` (serveur local port 8088). URL : `https://gosyfrone.github.io/WebDad/`. Activation manuelle requise : Settings > Pages > Source = GitHub Actions.*

*Last updated: 10/06/2026 — ci(swagger) : **Phase 2 — job CI drift check + lint**. Nouveau job `swagger` dans `.github/workflows/ci-go.yml` : (1) installe `swag@latest` + `pyyaml`, régénère le spec via `make swagger` ; (2) **drift check** `git diff --exit-code doc/openapi.{json,yaml}` → échec bloquant avec message `run \`make swagger\` and commit` ; (3) **validation OpenAPI** `swagger validate doc/openapi.json` (`go-swagger`, Go pur, purpose-built Swagger 2.0 — pas de runtime Node/Java). Path triggers étendus : `doc/**` + `scripts/swagger-aggregate.sh`. Spec committé propre → drift check passe sur l'état actuel de la branche. YAML workflow validé (`python3 yaml.safe_load`).*

*Last updated: 09/06/2026 — feat(docs) : **Swagger/OpenAPI Phase 1 — annotations + spec agrégé**. 7 services annotés (swaggo/swag code-first, zéro DTO inventé) : auth (5 routes), user (16), profil (7), post (20 : posts/likes/comments/bookmarks), message (8), notification (5), media (4). `doc.go` par service (title/version/host/BasePath/BearerAuth), annotations `@Summary/@Param/@Success/@Failure/@Security/@Router` sur chaque handler. `scripts/swagger-aggregate.sh` : `swag init` par service → `jq`-merge → `doc/openapi.{json,yaml}` (106 KB / 69 KB, 58 paths). `make swagger` ajouté au Makefile racine. Per-service `docs/` gitignorés (artefacts rebuild), spec agrégé commité (contrat API). `go build` + `make swagger` OK sans erreur.*

*Last updated: 09/06/2026 — fix(posts badges) : **badges repost/pin dynamiques dans `PostCard`**. Le badge `Reposté` ne dépend plus seulement de la prop initiale `post.repostedById` : état local `repostedById`, MAJ sur repost, nettoyage à l'annulation si le repost affiché appartient à l'utilisateur courant, rollback en cas d'échec. Le badge `Épinglé` reste public sur profil (`showPinBadge`) mais apparaît aussi immédiatement dans le feed pour les posts de l'utilisateur courant (`post.canPin`) après pin. Vérifs : `npm run lint`, `npx tsc --noEmit`.*

*Last updated: 09/06/2026 — fix(posts pin/feed) : **les posts épinglés ne sont plus visibles comme épinglés dans le feed d'un autre utilisateur**. Back post-service : `GetPosts`/`GetFeed` retournent une copie sans `pinned_at` (`withoutProfilePins`, test unitaire, source non mutée) ; `GET /posts?author_id=` conserve le pin et le tri profil. Front : `PostCard` accepte `showPinBadge` (défaut false) ; `ProfilView` l'active, feed/signets/détail ne rendent plus le badge. Vérifs : `docker compose exec -T post-service gofmt ...`, `docker compose exec -T post-service go test ./...`, `npm run lint`, `npx tsc --noEmit`, `localhost:3000` HTTP 200.*

*Last updated: 09/06/2026 — feat(feed) : **vidéos autoplay façon Twitter** + plan GIFs documenté. Nouveau `FeedVideo` (remplace le `<video>` de `MediaGallery`) : lecture auto muet + `loop` + `playsInline` quand ≥ 60 % visible (`IntersectionObserver` 0.6), pause hors-champ ; **chargement paresseux** via un 2ᵉ observer `rootMargin:400px` (la `<video>` n'est montée que près du viewport → vidéos lointaines du fil ni chargées ni en boucle) ; **vitesse 0.5×→2×** (overlay Popover → `playbackRate`) ; contrôles natifs conservés. Cellule vidéo en ratio défini (`aspect-video`/`aspect-square`). i18n `media.speed`/`media.speed_normal`. tsc/lint OK, vitest 46/46. **+ Plan GIFs acté** (§5, à implémenter) : route BFF Next `/api/gifs/*` (clé serveur, façon traduction) + `GifPicker` ; posts = URL externe (zéro backend), messages = octets rapatriés via BFF puis chiffrés (pipeline pièce jointe). Détails §5 « Vidéos de fil » / « GIFs ».*

*Last updated: 09/06/2026 — feat(media) : **clic sur une photo → agrandir**. (1) **Message privé** : `MediaLightbox` (nouvel overlay générique `components/ui/media-lightbox.tsx`, fond sombre, ✕/Échap/clic-fond, bouton télécharger) ; l'image est l'objectURL **déjà déchiffré** → téléchargement en clair (`att.name`), E2EE intact. `AttachmentView` (chat-pane) rend l'image cliquable (`cursor-zoom-in`). (2) **Post** : `PostPhotoModal` (nouveau, deux volets façon X) — média en grand à gauche (nav ‹ › + flèches clavier + compteur) avec **barre de stats sous la photo**, en-tête + texte + `CommentSection` à droite. La barre d'actions like/repost/citer **extraite de `PostCard` → `PostActions`** (composant partagé carte + modale, zéro duplication, état optimiste identique) ; `ActionButton`/`formatCount` déplacés. `MediaGallery` : images cliquables (`onOpen`), vidéo garde ses contrôles. i18n `media.download/previous/next`, `messages.open_image` FR/EN. tsc/lint OK, vitest 46/46. Détails §5 « Visionneuse média ». TODO (perspective) : route partageable `/posts/:id/photo/:n`, sync d'état carte↔modale.*

*Last updated: 09/06/2026 — feat(media) Phase 3 : **pièces jointes messagerie chiffrées (E2EE)**. Le payload chiffré d'un message passe d'une chaîne brute à une enveloppe `{v:1,text,media[]}` **uniquement si pièce jointe** (sinon texte brut → rétrocompatible) : `encodeMessageBody`/`decodeMessageBody` (pures, testées). Envoi : chaque fichier est chiffré client (`encryptSymmetric` clé de contenu) → **ciphertext** uploadé via `POST /media/encrypted` (blob opaque) → `{id,nonce,mime,name,size}` dans l'enveloppe chiffrée. Lecture : `fetchMediaBytes` (GET public, `fetch` simple sans Bearer → pas de preflight) → `decryptSymmetric` → `Blob`/`objectURL` (révoqué au démontage). **Serveur toujours aveugle, ZÉRO changement de schéma `message-service`.** UI `chat-pane` : bouton trombone + aperçus pré-envoi (`PendingAttachments`), bulles avec `AttachmentView` (spinner→image/vidéo, icône si clé absente), aperçu liste « Pièce jointe » (`hasMedia`). `lib/media.ts` : `fetchMediaBytes` en `fetch` public. i18n `messages.add_attachment/attachment_failed/attachment_preview` FR/EN. tsc/lint OK, **vitest 46/46** (+6). Briques vérifiées : crypto round-trip (crypto.test), `/media/encrypted` (Ph.0), CORS ACAO (Ph.1), enveloppe (nouveaux tests). Détails §5 « Stockage média » (Phase 3), §3, §6.*

*Last updated: 09/06/2026 — feat(settings/feed) : **mots filtrés par utilisateur** dans `/parametres` sous la langue. UI champ + bouton icône + chips supprimables, zone scrollable après ~3 lignes. Persistance locale scopée compte (`breezy-muted-words:<user_id>`, résolution via `getMe()` après reconnexion), filtrage feed côté front sur contenu/post cité/hashtags, avec exception pour les propres posts de l'utilisateur. Vérifs : lint/tsc/localhost:3000 OK.*

*Last updated: 09/06/2026 — feat(signets) : **enregistrement de posts en collections (back + front)** + **collection par défaut non supprimable**, branche `116-signets` (rebasée sur `origin/develop` = notifications #138 puis mots filtrés #140). Back post-service : 3 collections (`bookmark_collections`/`bookmarks` many-to-many/`bookmark_prefs`), collection `is_default` (en tête, non supprimable/renommable → 403), modèle de rafale `BOOKMARK_SESSION_WINDOW` (`filed`/`needs_choice`), vue « Tous » dédupliquée, nettoyage à la suppression post/collection. Front : `lib/bookmarks.ts` + `FeedPost.bookmarked`, bouton signet `PostCard` (clic court / appui long / dé-signer + toast « Ranger… »), `BookmarkDialog`, page `/signets` (`BookmarksView`), nav + i18n FR/EN (23 clés). Détails §5 « Signets — collections & rafale ». **Vérifié** : back gofmt/build/`go test` + e2e gateway 24/24 ; front tsc/lint/vitest + parité i18n.*
*Last updated: 09/06/2026 — feat(media) Phase 2 : **images / vidéos de post**. Back post-service : champ `media []MediaRef {url,type}` sur le post (modèle + validateur `$jsonSchema` `maxItems:4`, items `url`+`type` enum image/video), `CreatePostRequest.Media` (`binding:"max=4,dive"`), `content` passe optionnel (texte OU média requis, vérifié handler), `CreatePost(... , media)`. Front : composer = bouton image réel → upload multi best-effort (`uploadMedia`, cap 4, spinner, aperçus supprimables, `composer.media_*` FR/EN), `createPost(content, media[], quote?)` ; `PostCard` = `MediaGallery` (1 plein / 2-4 grille) + `<video controls>` ; `FeedPost.media` résolu en URL absolue par `resolveMediaUrl` dans `toFeedPost`. go build/vet/test OK, tsc/lint OK, vitest 40/40. **Vérifié e2e via la gateway** : post+média 201 (persisté), media-only 201, vide/`max=4`/type invalide → 400, cleanup OK. Détails §5 « Stockage média » (Phase 2), §3, §6.*

*Last updated: 09/06/2026 — feat(media) Phase 1 : **avatar / bannière uploadés pour de vrai**. `ImagePicker` (`EditProfilDialog`) n'utilise plus une data URL : il **uploade** au media-service (`uploadMedia`, spinner + toast `editprofil.upload_failed` FR/EN) et restitue l'URL ; `PATCH /profils/me` persiste `avatar_url`/`banner_url`. Forme stockée = **chemin relatif `/media/<id>`** (portable) : nouveau `resolveMediaUrl` (lecture → URL absolue gateway) appliqué aux **5 mappers centraux** (`profil-client`, `posts`, `notifications`, `user-cache`, `api`) → tous les `<img>` (sidebar/composer/feed/notifs/messages) résolus sans toucher chaque composant ; `toStoredMedia` fait l'inverse à la sauvegarde (`profil-client`). tsc/lint OK, vitest 40/40. **Vérifié e2e via la gateway** : POST /media→201 (url relative), GET→200 image/png + Cache immutable + ETag + Accept-Ranges + octets identiques, CORS preflight 204, ACAO présent. Détails §5 « Édition photo/bannière (Phase 1) », §3, §6.*

*Last updated: 09/06/2026 — feat(messages) : **badge de messages non lus app-wide + état « lu » côté serveur**. Back : `members.last_read_at` (+ validateur), `PUT /messages/conversations/:id/read`, `GET /messages/unread-count` (agrégation `$lookup` par métadonnées, jamais le `ciphertext` → E2EE intact). Front : `MessagesProvider` (monté dans `(app)/layout`) **possède l'unique WS messages** ; `MessagesView` s'y abonne (plus de WS propre) ; badge dégradé sur l'icône Messages (sidebar + barre mobile), `messages.badge_aria` FR/EN. `message-reads.ts` (localStorage) supprimé ; `computeDivider` prend un timestamp (`lastReadAt`). **+ Mise en sourdine par conversation** : `members.muted_at`, `PATCH/DELETE /conversations/:id/mute`, `ConversationView.muted` ; `CountUnreadConversations` exclut les sourdines (`muted_at: null`) → exclue du badge mais **toujours non-lue dans la liste** ; toggle menu « … » + icône cloche barrée, `toggleMute` optimiste + `refresh()`. go build/vet/test OK, tsc/lint OK, vitest 40/40. Détails §5 (décision « état lu serveur + badge ») / §6.*

*Last updated: 09/06/2026 — feat(media) Phase 0 : **fondation upload images/vidéos**. Nouveau **`media-service`** (Go, port 8087) adossé à **MinIO** (conteneur + volume `minio-data`, healthcheck `curl /minio/health/live`, version épinglée `RELEASE.2025-09-07`). Agnostique du contenu, autonome (bucket `breezy-media` au boot), `owner_id` en user-metadata MinIO (pas de base à côté). Endpoints : `POST /media` (JWT, multipart, détection MIME par magic bytes `gabriel-vasile/mimetype`, allowlist jpeg/png/webp/gif+mp4/webm, caps 5/50 Mo), `POST /media/encrypted` (blob E2EE opaque), `GET /media/:id` (public, `http.ServeContent` → Range/seek + Cache immutable + ETag), `DELETE /media/:id` (propriétaire/admin). **Tout via la gateway** (préfixe `/media` ajouté, MinIO jamais exposé). Front `lib/media.ts` (`uploadMedia`/`uploadEncryptedMedia`/`mediaUrl`). Infra : compose (minio + media-service + override gateway + depends_on), `docker-compose.dev.yml` (air + caches go-mod/build-media), **Makefile** (SERVICES += media-service, `db-only` lance minio, `logs-media`/`sh-media`, dev-logs), CI (`ci-go` matrice + `ci-integration` STACK + .env). go build/vet/test OK (validate 9/9), tsc/lint OK. **Smoke test e2e réel** contre MinIO : 401/201/download identique/415/403/404/206 ✓. Détails : décision §5 « Stockage média (MinIO via gateway) », §3, §6.*

*Last updated: 09/06/2026 — chore(docs) : **allègement de `CLAUDE.md`** (dépassait la limite de chargement de contexte ~40 KB, il faisait 113 KB). (1) Tout l'historique « Last updated » déplacé dans **`CHANGELOG.md`** (non auto-chargé) ; ne restent ici que les dernières entrées. (2) Colonnes « Status » des tableaux §3 (services + features) **condensées** (état + endpoints clés + renvoi vers §5 + TODO) — le détail d'implémentation vit dans le code et la §5. Résultat : 113 KB → ~64 KB. §5 (décisions techniques, utile pour la soutenance) et §6 (issues) conservées intégralement.*

*Last updated: 08/06/2026 — fix(legal) : « Retour à Breezy » revient à la dernière page Breezy (non légale) mémorisée dans `sessionStorage` (`RouteOriginTracker` + `LegalBackButton` via `router.push`), pas un `router.back()`. tsc/lint OK.*

*Last updated: 08/06/2026 — chore(rebase) + feat(legal i18n) : branche pages légales rebasée sur `origin/develop` (reposts #133, messagerie #134). Pages légales `(legal)` rendues bilingues FR/EN (`lib/legal-content.ts` + `LegalDocView` client) et adaptées aux fonctionnalités réelles (E2EE, posts/reposts/communautés, profil, follow). Parité i18n 310/310, vitest 31/31, pages → 200.*

*Last updated: 08/06/2026 — fix(legal) : **« Retour à Breezy » revient à la dernière page Breezy, pas un retour navigateur**. Avant : `router.back()` → en naviguant entre pages légales, on revenait à la page légale précédente. Maintenant : un `RouteOriginTracker` (monté à la racine, `usePathname`) mémorise en continu le dernier pathname **non légal** dans `sessionStorage` (`lib/legal-origin.ts`) ; `LegalBackButton` fait `router.push(origine ?? accueil)`. Naviguer entre pages légales ne touche pas l'origine → depuis /messages on revient à /messages, depuis /feed à /feed. tsc/lint OK.*
*Last updated: 08/06/2026 — chore(rebase) + feat(legal i18n) : branche `89-pages-légales` **rebasée sur `origin/develop`** (qui avait avancé : reposts #133, messagerie #134). Conflits résolus : `sidebar-right` (on garde `WhoToFollow` réel + masquage `/messages` de develop, on AJOUTE les liens légaux ; placeholder `SUGGESTED_USERS` abandonné), `parametres` (imports settings #126 + légal #84 fusionnés, corps déjà uni), `CLAUDE.md` (table services = develop + note légale ; changelog = entrées develop + entrée légale). **Pages légales adaptées** : (1) **DA refonte clair/sombre déjà OK** (coquille `legal-shell` en tokens `bg-page`/`glass-strong`/`brand-text`) → passée en **Client Component** pour localiser badge/date/liens ; (2) **contenu rendu bilingue FR/EN** et **adapté aux fonctionnalités réelles** (messagerie E2EE X25519+XChaCha20-Poly1305, DM/groupes admin-proof + communautés semi-publiques, posts/reposts/citations/emoji, profil, follow, rôles) via nouveau registre `lib/legal-content.ts` (par locale) + `LegalDocView` client (lit `useLanguage`) ; pages = Server Components avec `metadata` montant le composant client. `legal-links`/`legal-back-button` localisés (`useT`), 7 clés `legal.*` FR/EN. **Vérifié** : rebase OK (HEAD sur #134), tsc + `next lint` + `vitest 31/31` OK (conteneur), parité i18n **310/310**, `/mentions-legales`+`/cgu`+`/confidentialite` → 200, contenu FR vérifié (E2EE/IndexedDB/reposts/RGPD). (précédemment : feat(frontend #84) : **pages légales** (mentions légales / CGU / confidentialité). Route group public `(legal)` (hors garde de session) sur coquille partagée `components/legal/legal-shell.tsx` (DA refonte : `bg-page`/`glass-strong`/`brand-text`, prose en tokens sémantiques → clair + dark mode) + `FloatingThemeToggle` monté dans le layout `(legal)`. Composant de liens `components/legal/legal-links.tsx` réutilisé **sous les suggestions** (sidebar droite, ≥xl), dans **Paramètres** (mobile) et au pied de login/register. Contenu FR avec placeholders `[à compléter]` (éditeur/hébergeur/contact). `lib/routes.ts` étendu. (précédemment : feat(frontend #84) pages d'erreur + halo retiré).*

*Last updated: 08/06/2026 — fix(messages) : **navigation arrière corrompue après ?dm= corrigée**. Le nettoyage de l'URL `?dm=` se faisait via `window.history.replaceState(null, …)` → l'API History brute **écrase l'état interne de l'App Router Next** → au retour (bouton précédent) Next réaffichait le mauvais contenu (profil sur l'URL de recherche). Correctif : nettoyage via `router.replace('/messages', { scroll: false })` (routeur Next) → historique cohérent. (précédemment : fix(messages) : **bouton « Message » d'un profil ouvre directement le DM** (au lieu de retomber sur la liste). Cause : course au montage de `MessagesView` — `loadConversations` pouvait renvoyer un instantané SANS le DM tout juste créé par `startDM(?dm=)`, puis **écraser** la sélection → `selected` introuvable → écran liste. Correctif : `loadConversations` **préserve la conversation sélectionnée** absente de l'instantané (via `selectedIdRef`), et le flux `?dm=` sélectionne PUIS nettoie l'URL (suppression du flag `cancelled` qui pouvait annuler l'ouverture). tsc/lint OK. (précédemment : feat(messages) : **recherche de messages DANS une conversation** (DM / groupes / communautés), dans le panneau « Détails » (le « i »). E2EE oblige → recherche **côté client** : `listAllMessages` récupère l'historique complet déchiffré (pagination par curseur, borne `MAX_HISTORY_PAGES`=40), `searchMessages` (PURE, testée) filtre par sous-chaîne insensible à la casse en ignorant les messages non déchiffrés. Composant `message-search.tsx` (champ debouncé, chargement paresseux de l'historique à la 1re recherche, résultats plus récents d'abord avec expéditeur résolu + date + **surlignage** du terme), monté en tête du `conversation-info-dialog` (rendu `max-h-[85vh]` scrollable). 2 clés i18n FR/EN. tsc/lint/**vitest 31/31** (4 nouveaux tests) OK. (précédemment : fix(messages) : **rôle de communauté mis à jour en temps réel** (la personne promue talker ne pouvait pas écrire sans recharger). Cause : `connectRealtime` ne traitait QUE les événements `message` et ignorait `member_role_changed` (et les autres) → le `myRole` de la personne promue restait `viewer` côté client → composer en lecture seule. Correctif **100 % front** (back vérifié OK : viewer 403 → promotion → talker 201) : `connectRealtime(onMessage, onEvent?)` expose désormais les événements non-message (`RealtimeEvent`) ; `messages-view` les gère (`handleEvent`) en resynchronisant MA vue de la conversation via `getConversation` — `member_role_changed`/`conversation_updated`/`member_added(autrui)` → refetch (mon rôle/titre à jour, composer s'active sans reload) ; `member_added(moi)` → `loadConversations` ; `member_removed(moi)`/`conversation_deleted` → retrait de la liste. tsc/lint/vitest (27/27) OK. (précédemment : feat(messages) : **Épinglage + suppression « côté user » des conversations (back + front)**. **State par-utilisateur** porté par la collection `members` du message-service : `pinned_at` (épinglage) + `cleared_at` (suppression côté user). **Back** : routes `PATCH /conversations/:id/pin`, `DELETE /conversations/:id/pin`, `DELETE /conversations/:id/me` (membre requis, **pas de diffusion** — c'est personnel) ; `ListConversations` masque les conv « cleared » sans message plus récent + **tri épinglées d'abord** (`convLess` PURE testée) ; `ListMessages` coupe l'historique à `cleared_at` (`created_at > cleared_at`) ; repo `SetMemberPinned`/`SetMemberCleared`/`HasMessagesAfter` ; validateur `members` + 2 champs date. **Comportement** : supprimer masque la conv + coupe MON historique (les autres ne sont pas affectés) ; elle **réapparaît au prochain message, sans l'ancien historique** (façon WhatsApp). **Front** : `pinConversation`/`unpinConversation`/`clearConversation` (`lib/messages.ts`), `Conversation.pinnedAt`, **tri épinglées-d'abord** réappliqué après chaque mutation (`sortConversations`/`touchAndSort`), **menu « … » par ligne** (Épingler/Désépingler + Supprimer pour moi, ligne en `<div role=button>` pour éviter le bouton-dans-bouton) + **icône épingle** sur les lignes épinglées + pin optimiste/rollback. 5 clés i18n FR/EN. **Vérifié** : back gofmt/vet/build/`go test` OK (+ `convLess` + 3 routes 401), front tsc/lint/vitest (27/27) OK, **e2e 13/13 sur stack live** (JWT mintés : pin personnel non vu par l'autre, suppression masque+coupe l'historique, réapparition sans ancien historique). (précédemment : feat(messages UX) : **Aperçu + non-lus + ligne « Nouveaux messages »**. Liste des conversations : **aperçu du dernier message** (récupéré via `listMessagesPage(conv,1)` au chargement + déchiffré localement, MAJ temps réel via WS/envoi, préfixe « Vous : » pour mes messages), **pastille mauve** (`#8D3DFF`, couleur de marque) sur les conversations non lues (+ titre/sous-titre en gras). Dans le fil : **séparateur « Nouveaux messages »** placé avant le 1ᵉʳ message non-lu qui n'est pas le mien, **ancré sur le dernier message lu capturé à l'ouverture** (gelé ensuite) — gère le scénario A→B(moi)→C. **État lu/non-lu 100% client** (`lib/message-reads.ts`, `localStorage breezy-msg-reads`, **par appareil** : le back n'a pas d'accusés de lecture). Logique de tri/aperçu/non-lu centralisée dans `messages-view` (`ingest`/`loadConversations`/`selectConversation`) ; `computeDivider` **extraite en fonction PURE** dans `lib/messages.ts` + **6 tests Vitest** (27 au total). 3 clés i18n FR/EN (`new_messages_divider`/`you_prefix`/`unread_aria`). Refactor : `ConversationPreview` déplacé dans `conversation-list` (supprime le cycle de types). **Vérifié conteneur** : tsc + `next lint` + `vitest run` (27/27) OK, hot-reload sans erreur. (précédemment : feat(messages UX) : **Front de la messagerie E2EE (DM + groupes + communautés)**. UI complète au-dessus des libs existantes (aucune modif back) : page `/messages` **deux volets larges** (liste + chat, colonne droite masquée via `sidebar-right`), `components/messages/` = `messages-view` (orchestrateur, **1 WebSocket** partagé, déchiffre+route les messages entrants, bump de liste, point d'entrée `?dm=<id>`), `chat-pane` (historique **défilement infini par curseur** `before` + restauration de scroll au prepend + composer Enter-pour-envoyer optimiste, gère viewer lecture-seule & clé absente), `conversation-list` (+ menu `+` DM/groupe/communauté/découvrir), `conversation-meta` (titre/avatar par type), `user-search` (réutilise `searchUsers`), et 5 dialogues (`new-dm`, `new-group` multi-membres à chips, `create-community`, `discover-communities` = annuaire public + rejoindre, `conversation-info` = membres + inviter/renommer/promote↔demote/exclure/quitter/supprimer selon rôle). Résolution id→nom/avatar mémoïsée (`lib/user-cache.ts` + `lib/use-resolved-user.ts`). **Bouton « Message »** ajouté sur les profils (`profil-header`) → `/messages?dm=<id>`. **i18n FR/EN** (~60 clés `messages.*`, parité 60/60). Page `(app)/messages/page.tsx` = Server Component + `Suspense` (couvre `useSearchParams`). **Setup** : `message-service/.env` créé depuis l'exemple (il manquait et bloquait tout le compose). **Vérifié sur stack dev live** : `tsc --noEmit` + `next lint` OK **dans le conteneur** (`node_modules` local toujours root-owned), 12 conteneurs healthy, `/messages` protégé (307→login), gateway proxifie `/messages` REST+WS (401 sans token). TODO : aperçu du dernier message dans la liste, accusés de lecture, pièces jointes. (précédemment : feat(messages) : **Messagerie chiffrée E2EE — Phases 1+2+3 (DM, groupes, communautés)**, rebasée sur develop (i18n + posts épinglés + traduction auto). Nouveau **message-service** (Go/Gin/MongoDB, port 8085) : DM E2EE (sealed box X25519 + XChaCha20-Poly1305, serveur aveugle), groupes (owner+membres, tout le monde invite, owner-only exclure/renommer/supprimer, cap 32, nom chiffré, clé unique), communautés (**hybride** : ≤32 talkers + viewers illimités, auto-join, annuaire public, **clé détenue par le serveur** → lisibles par un admin ; DM/groupes restent admin-proof). **WebSocket** temps réel, **pagination par curseur**. Front = **couche technique seule** (UX déléguée) : `lib/{crypto,key-store,messages}.ts` + **21 tests Vitest**. Gateway préfixe `/messages` ; compose `mongo-message`+`message-service` ; CI (3 workflows + `npm test`). **Testé e2e** : Phase 1 13/13, Phase 2 21/21, Phase 3 28/28. **Rebase** : conflits **uniquement dans CLAUDE.md** (doc) résolus en réintégrant la doc messagerie sur la version develop ; aucun conflit de code (`package.json`/`post-service`/`message-service` intacts). build/vet/gofmt/tests Go + tsc/lint/vitest OK après rebase. (précédemment : feat(#115) : **i18n FR/EN du frontend (maison, zéro dépendance)** — registre `lib/i18n.ts` (`LOCALES` + `messages` FR/EN, parité de clés vérifiée, dont `composer.pin_profile` pour l'épinglage) + `LanguageProvider` (Context, `localStorage breezy-locale`, pose `<html lang>`) + hook `useT()`/`useLanguage()` + `LanguageSelector` (**dropdown globe 🌐 scalable à N langues**) monté **au-dessus du ThemeToggle** (sidebar PC + tiroir mobile) + `FloatingLanguageSelector` translucide sur login/register. **Tous les textes UI traduits** (nav/layout, auth login+register, feed/posts/commentaires threadés, profil/relations/édition, explorer/qui-suivre, pages stub via `PlaceholderPage`, 404) ; dates/heures localisées (`timeAgo`/`Intl` par `locale`). Défaut FR (rendu serveur), bascule client sans mismatch d'hydratation. Ajout futur ES/IT/ZH = 1 entrée `LOCALES` + 1 bloc `messages`. **Rebasé sur develop** (posts épinglés + traduction auto) : conflit `post-composer` résolu (bouton « épingler » conservé, `aria-label` traduit), conflit changelog résolu. ⚠️ tsc/lint **non lancés en local** (`frontend/node_modules` vide/root-owned — env, pas code ; OK en CI/checkout neuf).*
*Last updated: 08/06/2026 — feat(#45) : **Notifications repost + citation** (tag implicite de l'auteur). 2 nouveaux types `repost`/`quote`. post-service émet : **repost** (`POST /posts/:id/repost`) → auteur du post, **agrégé** (`repost:<post>`, retract à l'unrepost) ; **citation** (`POST /posts` avec `quote_post_id`) → auteur cité, **une par citation** (`quote:<postCitant>`, `post_id`=post citant → navigation vers la citation ; purge auto via la cascade `post_deleted`). notification-service : enum validateur + `groupKeyFor` + branche `HandleEvent` (recipient fourni, self-skip). Front : type union + `TYPE_ICON` (Repeat2/Quote) + `describe()` + i18n FR/EN (`repost_one`/`repost_other`/`quote`, parité). **Vérifs** : Go build/gofmt/tests (notif+post) OK, frontend tsc/lint/vitest OK. **Testé e2e LIVE** : repost notif + agrégation (count=2) + unrepost retract, citation notif (post_id=post citant), self-repost skip.*
*Last updated: 08/06/2026 — feat(#45) : **Notifications temps réel agrégées (WebSocket)**. Nouveau **notification-service** (Go/Gin/MongoDB, port 8086, branche `45-notifications-temps-réel`) calqué sur message-service (+ hub `realtime` WS). **Agrégation façon Instagram** : une notif = un GROUPE `group_key` unique/destinataire (`like:<post>`/`comment:<post>`/`reply:<rootComment>`/`mention:<source>`), `$inc count` → 300 likes = 1 notif « X et N autres ». **Émission** : post-service `internal/notifier` (best-effort fire-and-forget → `POST /internal/events`, secret `X-Internal-Secret`, **hors gateway**, réseau Docker) sur like/comment/reply/mention/post_deleted ; `retract` défait (unlike, suppr. commentaire/post) ; jamais à soi-même ; reply notifié à l'auteur de la RACINE du fil ; mentions `@handle` résolues via user-service (parser anti-email). **API** `/notifications` (JWT : list curseur, unread-count, read, :id/read) + **WS** `/notifications/ws`. **Front** : `lib/notifications.ts` (+9 tests Vitest), `NotificationsProvider` (badge app-wide + WS unique + liste vivante, dans `(app)/layout`), badge non-lus (sidebar PC + `MobileTabBar`), page `/notifications` réelle, **page détail `/posts/[id]`** (clic→post, réutilise `PostCard`), i18n FR/EN (parité). Gateway préfixe `/notifications` ; compose `mongo-notification`(27020)+`notification-service` (+ overlay dev) ; CI (`ci-go` matrice +1, `ci-integration` +2 conteneurs). **Vérifs** : Go build/vet/gofmt/tests (notification+post) OK, image Docker build OK, frontend tsc/lint/vitest (30) OK, `docker compose config` OK. **Testé e2e LIVE** (stack dev) : like+agrégation(count=2)+unlike retract, comment, reply-to-comment, mention résolue, self-skip, mark-all-read→0, **cascade post supprimé**, **push WS via gateway** reçu instantanément. Le branchement émission requiert `NOTIFICATION_SERVICE_URL`+`INTERNAL_EVENT_SECRET` (sinon post-service reste autonome, notifs off).*
*Last updated: 08/06/2026 — fix(posts UI) : badge `Reposté` regroupé sur la même ligne que `Épinglé` dans `PostCard` ; si les deux états sont présents, `Reposté` s'affiche à droite de `Épinglé`, sinon il garde l'emplacement du badge unique. Vérif : `npm run lint` OK.*
*Last updated: 08/06/2026 — feat(posts social) : **emojis dans les réponses + repost simple + citation fonctionnels**. Back post-service : collection `reposts` (index unique `post_id+user_id`), `reposts_count`, routes `POST/DELETE /posts/:id/repost`, `GET /posts/me/reposted-ids`, `quote_post_id` optionnel sur `POST /posts`, profil `?author_id=` enrichi avec les reposts simples annotés `reposted_by_id`/`reposted_at`, nettoyage des reposts à la suppression. Front : `CommentSection` réutilise `EmojiPicker` sur commentaire racine et réponse inline ; `PostCard` ouvre un popover Repost/Citer, repost persistant avec compteur/état optimiste, citation via dialog `PostComposer` + aperçu du post cité ; `FeedPost` mappe citations/reposts ; `profil-view` écoute les créations/reposts pour afficher le repost sur le profil sans refetch. Vérifs : `docker compose -f docker-compose.yml -f docker-compose.dev.yml build post-service`, `gofmt`, `go test ./...` post-service OK ; `npm run lint`, `npx tsc --noEmit`, `npm run build` OK ; image prod `docker compose build post-service` + `docker compose up -d post-service` ; gateway `POST /posts/<id>/repost` sans token → 401 (route active + auth) ; `http://localhost:3000/feed` → 200.*
*Last updated: 08/06/2026 — chore(rebase) : branche `123-ajout-dun-bouton-paramètre` rebasée sur `origin/develop` (`bbd230b`, messagerie privée/chiffrement). Conflit `CLAUDE.md` résolu en conservant le nouveau `Message Service`/gateway messages de develop + la note **Paramètres centralisés**. Vérifs : `npm ci` pour récupérer les nouvelles dépendances `@noble/*`, `npm run build` OK, suppression de `frontend/.next`, redémarrage frontend Docker, `/parametres` et `layout.css` en `200`. `message-service/.env` local créé depuis `.env.example` pour permettre à Docker Compose de lire la nouvelle config ; fichier ignoré par Git.*
*Last updated: 08/06/2026 — fix(posts pin) : erreur UI « Épinglage impossible » diagnostiquée. Cause immédiate : le process `post-service` en cours n'avait pas encore la route `PATCH /posts/:id/pin` chargée → logs `404` sur `/pin` malgré le code présent. Action : `docker compose restart post-service`; vérifié ensuite via gateway `PATCH /posts/<id>/pin` sans token → `401 token manquant` (route active + middleware). Correctif préventif ajouté : `post-service/internal/database/init.go` resynchronise désormais les validateurs Mongo des collections existantes via `collMod`, comme `profil-service`, pour que les volumes anciens acceptent `pinned_at`. Vérif : `docker compose exec -T post-service gofmt ...` + `go test ./...` OK.*
*Last updated: 08/06/2026 — fix(dev) : erreur Next `Cannot find module './276.js'` résolue. Cause : cache `frontend/.next` incohérent après `npm run build` local (présence de `.next/standalone` + manifests prod) alors que le conteneur dev utilise `next dev` avec bind-mount. Action : suppression de `frontend/.next`, redémarrage `docker compose restart frontend`; vérifié ensuite sur `http://localhost:3000/feed` → `200 OK` après recompilation dev.*
*Last updated: 08/06/2026 — feat(posts) : **animation du cœur au like + post épinglé profil persistant**. Back post-service : `pinned_at`, routes `PATCH/DELETE /posts/:id/pin`, autorisation auteur-only, désépinglage automatique des autres posts de l'auteur, tri profil par `pinned_at DESC, created_at DESC`, index dédié, tests `canPin` + routes 401. Front : `lib/posts.ts` mappe `pinned_at` + `pinPost`/`unpinPost`, `PostComposer` propose l'épinglage à la création, `PostCard` affiche badge/menu pin + animation cœur pop/burst, `feed-view`/`profil-view` synchronisent les posts visibles. Vérifs : `docker compose exec -T post-service gofmt ...`, `docker compose exec -T post-service go test ./...`, `npx tsc --noEmit`, `npm run lint`, `npm run build` OK.*
*Last updated: 08/06/2026 — fix(dev) : erreur Next `Cannot find module './682.js'` résolue. Cause probable : cache `frontend/.next` incohérent après `npm run build` local + montage live Docker dev. Action : suppression de `frontend/.next`, redémarrage de `webdad-frontend-1`; vérifié ensuite sur `http://localhost:3000/feed` (`200`) et `POST /api/translate` (`200`, anglais→fr OK).*
*Last updated: 08/06/2026 — fix(frontend) : **traduction posts/commentaires fiabilisée**. Cause : LibreTranslate public pouvait renvoyer une réponse vide → l'UI restait sur « Traduction... ». Correctif : route BFF `/api/translate` avec timeout serveur 8s, fallback Google public prioritaire si pas d'instance LibreTranslate privée configurée, LibreTranslate configurable sinon ; client `lib/post-translation.ts` avec timeout 12s et suppression du cache sur échec ; nouveau composant `TranslatedContent` réutilisé par `PostCard` et `CommentRow` → traduction aussi sur commentaires racine et réponses. Test local `POST http://localhost:3000/api/translate` anglais→fr OK (« Bonjour à tous... », source `en`). Vérifs : `npx tsc --noEmit`, `npm run lint`, `npm run build` OK.*
*Last updated: 08/06/2026 — feat(frontend) : **traduction automatique des posts**. Ajout de la route BFF Next `POST /api/translate` (compatible LibreTranslate, `source:auto`, URL `TRANSLATION_API_URL`, clé optionnelle `TRANSLATION_API_KEY`), du client `lib/post-translation.ts` (langue cible `html lang` → navigateur, cache mémoire par post/langue/contenu, fallback silencieux), et intégration `PostCard` (texte traduit par défaut si langue source différente, indicateur « Traduit automatiquement », bascule « Voir l'original » / « Voir la traduction »). Config `.env.example` + `docker-compose.yml`. Vérifs : `npx tsc --noEmit`, `npm run lint`, `npm run build` OK (20 routes, dont `/api/translate`). Serveur dev confirmé sur `http://localhost:3000/` via `webdad-frontend-1` (`next dev -p 3000`, réponse HTTP 200).*
*Last updated: 07/06/2026 — feat(#11) : **CRUD des publications de bout en bout** (post-service → 🟢 v1) + **commentaires threadés (2 niveaux) avec pagination dynamique**, **avatars/noms cliquables → profil**, **bouton Suivre⇄Abonné sur le profil** (hover « Ne plus suivre », via `useFollow`). Threading : `parent_id`+`reply_count` (back), réponses à plat sous la racine (`resolveParentID`), `GET .../comments/:commentId/replies`, suppression cascade ; front `comment-section` threadé (« Voir les N réponses »/« Voir plus », « Répondre » inline). `ProfilLink` (lien profil sur photos+noms posts/commentaires). Tests back (resolveParentID). Back : likes (`POST/DELETE /posts/:id/like` idempotents + `GET /posts/me/liked-ids`), commentaires (`GET/POST /posts/:id/comments`, `DELETE .../:commentId`), compteurs dénormalisés `likes_count`/`comments_count` (`int32`, `$inc`), fil « Abonnements » via `?author_ids=` (`$in` indexé, ids fournis par le front), nettoyage des orphelins à la suppression, `canAct` (autz posts+comments), tests (canAct/GetFeed/splitIDs/routes 401). Front : `lib/posts.ts` (CRUD+like+comments, cache auteur mémoïsé), `feed-view` (fil réel Pour toi/Abonnements + prepend via event `breezy:post-created`), `PostCard` câblé (like optimiste, **section commentaires repliable** `comment-section`, suppression auteur/mod/admin), `post-composer`→`createPost`, `profil-view` posts réels + compteur. `tsc` + lint OK (⚠️ `next build` local bloqué par `.next/server` root-owned d'un `make dev` antérieur — env, pas code ; OK en CI/checkout neuf). (précédemment : feat(#68) : **recherche de comptes (Explorer) + « Qui suivre » + suivre depuis partout**. Back : `GET /users/search` (username) & `GET /users/suggestions` (top followers) [user-service], `GET /profils/search` (display_name, regex insensible casse) [profil-service] — testés e2e sur stack live. Front : `/explorer` recherche debouncée (`@`=username sinon display_name, orchestration cross-service), `WhoToFollow` (sidebar), hook partagé `use-follow`, `lib/api.ts` étendu (searchUsers/getSuggestions/getUser + enrichissement factorisé). Ordre compteurs profil aligné sur la modale. **Fix drift validateur Mongo** : `EnsureSchema` resync le validateur `profiles` via `collMod` à chaque boot (le `POST /profils` du register échouait sur les vieux volumes → recherche display_name vide) — résolu, testé e2e. (précédemment : abonnés/abonnements re-codés sur develop —* (ancienne branche #68 périmée par la refonte #79 + déménagement de `display_name` vers profil-service → repartie de zéro). Nouveau client métier `lib/api.ts` au-dessus de `apiFetch`. Modale `RelationsDialog` (onglets Abonnés/Abonnements) chargeant les vraies listes `GET /users/:id/followers\|following` **enrichies** du décoratif via profil-service (N+1 best-effort, repli @username). Ligne réutilisable `UserListItem`, bouton Suivre⇄Abonné → `POST/DELETE /follow` (optimiste + rollback/toast), état initial via `getFollowingIds(moi)`. Compteurs de `profil-header` cliquables. `/profil` hydraté côté client (`getMe`+`getProfilMe`). `tsc` + lint + `next build` OK. (précédemment : feat(frontend #81) dark mode refondu — tokenisation `globals.css`, interrupteur clair/sombre PC+mobile).*

<!-- prev: 04/06/2026 — feat(profil-service) : routes lecture/édition implémentées (`birth_date` set-once, `PATCH /profils/me`). **Création = `POST /profils` UNIQUEMENT** (`display_name` obligatoire = username au register via le BFF) ; **`GET /profils/me` est en lecture seule, ne crée RIEN** (404 si absent, le front crée via POST) — pas de provisioning paresseux, jamais de display_name deviné. `DELETE` admin. **Archi cooldown identitaire posée des deux côtés** : `display_name_changed_at` (profil) + `username_changed_at` (user-service, colonne + `CASE` SQL), enregistrement systématique, refus configurable (`*_CHANGE_COOLDOWN`, défaut 0 = off). BFF provisionne aussi le profil au login/register (`lib/provision.ts`). Tests Go (routes/401 + unit pur `planUpdate`). build+vet+gofmt+tests OK (profil + user). (précédemment : feat(profil-service) squelette + migration BDD).*
*Last updated: 04/06/2026 — ci(pipeline) : 3 workflows GitHub Actions séparés par domaine (`ci-go` : matrice 5 services, gofmt/vet/build/`test -race`/tidy ; `ci-frontend` : lint+build ; `ci-integration` : `docker compose up` de la stack sans le front + attente « all healthy » sur les healthchecks BDD/services). Caches + concurrency. Path-filters par domaine. La CI a déjà détecté+corrigé un fichier non formaté (`auth-service/internal/config/config.go`). TODO perspective : golangci-lint/govulncheck/gitleaks/Dependabot/CD GHCR. (précédemment : feat(profil-service) : routes lecture/édition implémentées (`birth_date` set-once, `PATCH /profils/me`). **Création = `POST /profils` UNIQUEMENT** (`display_name` obligatoire = username au register via le BFF) ; **`GET /profils/me` est en lecture seule, ne crée RIEN** (404 si absent, le front crée via POST) — pas de provisioning paresseux, jamais de display_name deviné. `DELETE` admin. **Archi cooldown identitaire posée des deux côtés** : `display_name_changed_at` (profil) + `username_changed_at` (user-service, colonne + `CASE` SQL), enregistrement systématique, refus configurable (`*_CHANGE_COOLDOWN`, défaut 0 = off). BFF provisionne aussi le profil au login/register (`lib/provision.ts`). Tests Go (routes/401 + unit pur `planUpdate`). build+vet+gofmt+tests OK (profil + user). (précédemment : feat(profil-service) squelette + migration BDD).*
<!-- prev: 03/06/2026 — feat(profil-service) : squelette + migration BDD. Service complet calqué sur post (config/database/models/repository/service/handler/middleware), schéma autonome (`EnsureSchema` : collection `profiles` + validateur + index unique `user_id` + seed admin), `profil-init.js` supprimé + mount compose retiré (les 4 services sont désormais autonomes). Routes câblées mais stubs `501` sauf `/health` (`GET /profils/:userId`, `GET/PATCH /profils/me`, `POST/DELETE /profils`). **display_name déplacé de user-service vers profil-service** (profil = propriétaire de tout l'éditable → édition = 1 seul PATCH). Tests Go (routes/health/501/401). build + vet + gofmt + tests OK. (précédemment : feat(auth) refresh tokens) -->

