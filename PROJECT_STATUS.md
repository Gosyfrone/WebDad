# PROJECT STATUS — WebDad / Breezy

> Current state only. Historical progression lives in `CHANGELOG.md` / `CHANGELOG_ARCHIVE.md`.
> Update status markers (🔴 todo → 🟡 WIP → 🟢 done) after each significant change.
> **Team:** Zaid, Perujan, Candis, Théo.

---

## Services

| Service | Status | DB | Current state |
|---|---|---|---|
| Auth | 🟡 WIP | PostgreSQL | `/auth/{register,login,refresh,logout,validate,verify-email/confirm,verify-email/request,password/forgot,password/reset,password/change,email/change/request,email/change/confirm}` + `POST /auth/users` (admin create) + **`GET /auth/users/roles?ids=...`** (JWT, rôle public d'affichage minimal pour badges staff) + `/auth/oauth/{provider}/{url,exchange,complete}` + `/health`. JWT HS256 + bcrypt, access 5m + refresh 24h (rotation, revoke). **Changement d'e-mail vérifié et strict sur la remise** : `pending_email` unique, ancienne adresse conservée jusqu'au lien 24h ; si mail-service refuse la remise, le jeton exact et `pending_email` sont annulés puis l'API répond 503. Confirmation transactionnelle, révocation des sessions et nouvelle paire JWT. **Logs métier slog** (ERROR/WARN/INFO/DEBUG, `FromGin`). **Admin-created accounts** : `must_change_password=true` (mot de passe temporaire mailé) + **vérif e-mail obligatoire** (`email_verified=false` en prod ; e-mail = lien de vérif + mot de passe temporaire ; le lien vérifie ET ouvre la session → feed + modale changement de mot de passe ; `VerifyEmail` propage le drapeau). Court-circuit DEV via `ADMIN_CREATE_AUTO_VERIFY=true`. Drapeau porté par le JWT + survit au refresh, levé par `POST /auth/password/change` (vérifie le pwd actuel, révoque les autres sessions, ré-émet des tokens sans drapeau). **OIDC Login with Google** (code→tokens server-side, ID token verified via JWKS/issuer/audience; `email_verified` required; generic provider registry). Nouveau compte Google = `oauth_signup_tokens` pending 15 min (aucun credential/JWT avant `/complete`). **Blocage dur login non-vérifié** (`403 email_not_verified`) ; register envoie le mail de vérif (best-effort) + tokens vérif opaques `account_tokens` (24h, usage unique, anti-énum sur le renvoi). **`verify-email/confirm` = auto-login** : confirme le token *et* émet une session (access + refresh, comme login ; garde compte désactivé). **Reset password** (Phase 2) : forgot anti-énum (toujours 200) + reset token 1h usage unique → bcrypt + `email_verified=true` + **révocation de toutes les sessions** (`DELETE refresh_tokens`). **MFA TOTP optionnelle (opt-in, RFC 6238)** : `mfa_enabled`/`mfa_secret` (secret chiffré AES-256-GCM at-rest, clé `MFA_ENCRYPTION_KEY`, nil=off→503), `POST /auth/mfa/{setup(QR PNG serveur),enable,disable(code|mdp),verify}` + `GET /auth/mfa/status`. Login MFA actif → pas de JWT, renvoie `{mfa_required, challenge}` (TTL 5min, réutilise `account_tokens` purpose `mfa_challenge`) ; `/mfa/verify` (public) valide le TOTP **sans brûler le challenge** sur code faux. **Sans codes de secours** (tranché) → perte du téléphone = lockout. MFA = login mot de passe seul (OAuth = 2FA provider). Autonomous schema + optional admin seed. **🔒 Durcissement pentest (18/06)** : **rate-limiting par IP** (`middleware/ratelimit.go`) sur login/mfa-verify (10/5min) + register/forgot/resend (5/15min) ; **challenge MFA brûlé après 5 codes faux** (anti brute-force TOTP) ; **claim `email_verified`** propagé sur tous les chemins (register/login/verify/refresh/oauth) → gate `VerifiedOnly` sur la création de contenu (post/message). **Acceptation CGU versionnée (22/06)** : `credentials.terms_accepted_version`/`terms_accepted_at` ; claim JWT `terms_accepted` (= `version >= models.CurrentTermsVersion`, dérivé dans `GenerateToken`, re-chargé centralement dans `issueTokens` pour ne pas retomber faux au refresh) ; `POST /auth/terms/accept` (JWT → UPDATE version+`NOW()`, ré-émet la paire de tokens). Migration rétro-compatible `DEFAULT 0` → tous les comptes existants doivent accepter au prochain login (Rule 5b) ; `Register`/OAuth `complete` insèrent à la version courante ; admin-created restent à 0. **TODO:** Go tests (repo MFA flows), refresh-reuse detection. *(OAuth new-user provisioning = onboarding gate côté front, cf. Frontend.)* |
| User | 🟢 OK (v1) | PostgreSQL | repo/service/handlers, autonomous schema (`users`+`follows`+`follow_requests`+`blocks`). CRUD users, follow (public edge / private `pending`→accept/reject), followers/following lists+counts, **blocage/déblocage utilisateur** (`POST/DELETE /users/:id/block`, `GET /users/me/blocks`, coupe follows/follow_requests dans les deux sens), search/suggestions, lazy provisioning on `/users/me`. **`POST /users/admin`** (admin) provisionne l'id créé par auth ; username suffixé `_<8 hex>` + `username_pending` si pris (exposé par `/users/me`, levé sur changement effectif). **🔒 `GET /users` (annuaire complet) authentifié (RIV-003) ; `GET /users/:id` valide la forme UUID → 404 au lieu de 500 (RIV-007).** **TODO:** repo integration tests. |
| Post | 🟢 OK (v1) | MongoDB | Autonomous. CRUD posts with **profile-visibility + user-block filtered reads**, **hashtags + filtered feed + trends**, reposts/quotes, profile pin (hidden in feeds), likes, **`GET /posts/liked?author_id=` (likes-visibility-gated + blocage)**, 2-level threaded comments **with per-post reply audience (`reply_audience` everyone/followers, enforced in `CreateComment`)**, **likes sur commentaires** (`POST/DELETE /posts/:id/comments/:commentId/like` + `GET /posts/comments/stats` pour polling batch), **polls with duration/audience/vote uniqueness, author live results, manual close**, denormalized int32 counters, **media `[]MediaRef` (cap 4)**, **bookmark collections + burst model**, **realtime broadcast WS `GET /posts/ws`** (ping `{post_id, author_id}` on new public root post → live "a posté" banner; public-account-gated, fire-and-forget). **TODO:** post edit front (back ready). |
| Profil | 🟢 OK (v1) | MongoDB | Autonomous. Owns decorative fields including optional **nationality ISO alpha-2** + **`visibility`** (`public` by default on creation, `PATCH /profils/me` to switch public/private) + **`likes_visibility`** (`public` by default, `PATCH /profils/me` to switch) + **`activity_visibility`/`last_login_at`/`is_online`** + **`certification`** (`none/political/public_figure`, backfill legacy à `none`, `PATCH /profils/:id/certification` réservé mod/admin, puis émission best-effort `identity_updated` vers notification-service pour rafraîchissement WS sans reload). Activity preference is independent from private profile, but private-profile reads reveal activity only to the owner or accepted followers when `activity_visibility=public`. `GET /profils/:id/likes-visibility`, `PATCH /profils/me/activity`, `PATCH /profils/me/activity/offline`, `GET /profils/:id/activity`. `GET/PATCH /profils/me`, `POST` (unique creation), search, admin delete. `birth_date` set-once, display_name cooldown baseline and **display_name restricted to Unicode letters/digits/spaces/`-`/`_` on creation or real rename** (legacy unchanged tolerated). Avatar/banner upload wired. **TODO:** front aggregated read. |
| Message | 🟢 OK | MongoDB | E2EE (DM/groups/communities), blind server, X25519 keys, cursor pagination, WS, **encrypted attachments** (no schema change), owner-only message edit with encrypted original preserved, server-side read cursor + app-wide unread badge, per-conversation mute, **passphrase-protected key backup (`key_backups`, zero-knowledge, multi-device)**, **read/delivery receipts « Envoyé/Ouvert » (`last_delivered_at` cursor + WS `receipt` event + `member_receipts`, DM+groups, server metadata only)**, **message delete "for everyone" (tombstone `deleted_at`, author + group owner/admin)**, **typing indicator (ephemeral `POST /typing` → WS `typing`)**. Profile-to-DM deep-links report an unavailable recipient key instead of failing silently ; en DM, avatar/display name/@username ouvrent le profil avec retour explicite vers `/messages?conv=<id>`. DM/groups privés émettent une notification `message` agrégée par expéditeur unique. Back + UX complete. **TODO:** admin passphrase reset (deferred) ; cleanup orphaned media blobs on delete. |
| Notification | 🟢 OK | MongoDB | Aggregated (Instagram-style), ingest `/internal/events`, types like/comment_like/comment/reply/mention/repost/quote/follow/follow_request/follow_request_accepted/follow_request_accept_confirm/message/message_mention/post_deleted + **`identity_updated`** (broadcast WS cosmétique pour badges certification/staff, non persisté comme notification utilisateur). JWT API + WS. `message` est groupé globalement par destinataire avec `actor_ids` optionnel (`count` = expéditeurs uniques). |
| Report | 🟢 OK (v1) | MongoDB | **Nouveau service (port 8090, préfixe gateway `/reports`)**. Tickets de signalement : `tickets` (un parent agrégé par entité signalée en modération, clé unique partielle `(entity_type, entity_id)` ; bug = ticket autonome `entity_type=app`), `warnings`. `POST /reports` (tout utilisateur, valide motif + entité + texte 255 modération / 500 bug), `GET /reports/tickets` (filtres cumulables, tri par volume décroissant), `GET /reports/tickets/:id`, `POST .../replies` + `PATCH .../status` (mod, trace le modérateur), `POST .../transfer` (admin, bug→modération), warns `POST /reports/warnings` + `GET /reports/warnings/pending` + `POST .../ack` (+ `GET /reports/users/:id/warnings/count` mod/admin → profil de risque). **Auto-masquage** : `POST .../approve` (mod, statut terminal `approved` → démasque + verrouille le re-signalement ; **refus 409 `ErrAlreadyActioned` si une sanction `content_removed`/`warned` existe déjà**), `GET`/`PATCH /reports/settings` (seuil réglable, admin), collection singleton `settings`, appel off-gateway → post-service `/internal/posts/:id/auto-hide|auto-unhide` (`X-Internal-Secret`). Action **`warned`** journalisée sur le ticket quand un avertissement porte un `ticket_id` (ticket auto-descriptif). `EnsureSchema` idempotent. Pièce jointe via media-service. |
| Mail | 🟡 WIP | — | **Phases 0+1 OK (code)** : module Go autonome (8089), `POST /internal/send` (hors gateway, `MAIL_INTERNAL_SECRET`) + `/health`, transport SMTP (`net/smtp`) ou console en dev. **Le mode console signale désormais la non-remise par 503** au lieu d'un faux 202 ; chaque appelant choisit strict (changement d'e-mail) ou best-effort (inscription/admin/reset). **Phase 1 : intégration auth→mail câblée**. **Phase 2 : mail de reset câblé** (`sendResetMail`, lien 1h). **Templates HTML de marque** (coquille partagée `brandedEmailHTML`, clear mode, vérif + reset) ✓. 🟢 après configuration SMTP et e2e validé. **TODO Phase 3 :** rate-limiting, emails EN. |
| Media | 🟢 OK | MinIO | Cross-cutting opaque storage, autonomous bucket. `POST /media` (sniff+caps), `POST /media/encrypted` (E2EE blob), public `GET /media/:id` (Range/seek), owner/admin delete. Wired on profils/posts/messages. **Upload cap uniforme 5 Mo (image/vidéo/blob) pour les non-admins ; admins non plafonnés (bypass `claims.Role == "admin"`).** Front : garde UX `exceedsMediaLimit` (admin-aware) à la sélection. **🔒 `X-Content-Type-Options: nosniff` + `Content-Disposition: attachment` (blobs) sur le download (BRZ-004).** |
| API Gateway | 🟡 WIP | — | stdlib reverse proxy, prefix routing, WS proxy, CORS, media streaming. `/internal/events` not routed (server-to-server). **TODO:** JWT middleware to protect prefixes. |
| Frontend | 🟡 WIP | — | Next.js 14, X-style responsive layout, refresh-token auth, business clients over `apiFetch`. Wired: feed/posts (**likes de posts et de commentaires avec animation cœur + auto-refresh polling**, comments/pin/repost/quote, **menu post `...` signaler le post + bloquer l'auteur**, **popup realtime "a posté" filtrée par utilisateurs bloqués**, **action bars sans bouton stats/vues**, clickable hashtags + hashtag composer autocomplete + hashtag results tabs + X-style hashtag/profile suggestions), dynamic Top 5 trends + searchable/zoom-safe/responsive right-column suggestions with recent history, Explorer discovery sections (Top 10 trends, non-followed profile suggestions, feed publications, submitted search filters Publications/Utilisateurs) with trend post counts aligned to the feed layout, bookmark collections, hydrated profile + privacy + **certifications visibles à droite du display name partout où l'identité apparaît** (profil/hover/modération, posts/citations/commentaires, recherche/Explorer/suggestions, messages/mentions/notifications/partages/navigation ; rosette verified façon X jaune politique, bleue personnalité publique, logo Breezy staff quand le rôle est connu, tooltip hover/clic) + **blocage profil** (menu `...` partager/message/signaler/bloquer, bouton suivre remplacé par Débloquer, onglets Posts/Réponses/J'aime remplacés par “Utilisateur bloqué”, **liste/déblocage dans Paramètres › Généraux**) + **activité profil (“En ligne” / dernière connexion) masquable, rafraîchie profil/hover/DM + pastille verte/grise sur avatars hors profil**, **fallback avatar uniforme sur la première lettre réelle** et remise à zéro avatar/bannière, **combobox de nationalité via `/api/countries` + badges genre/drapeau**, **paramètres généraux/utilisateur avec visibilité compte/J'aime/activité, changement de mot de passe et d'e-mail vérifié, activation/désactivation MFA TOTP (QR + écran de challenge au login)**, follow pending/accept/reject, E2EE messaging + attachments, notifications (badge+WS), **sons d'action/temps réel** (publication d'un breeze, notification reçue, message reçu), **i18n 12 langues avec préférence persistée par compte**, traduction automatique prudente des posts/commentaires, thème clair/sombre + **personnalisé en troisième option**, legal pages + **CGU obligatoires à l'inscription (lecture `/cgu` avant checkbox classique ; page bloquante `/auth/oauth/terms` pour OAuth pending)** + **modale d'acceptation CGU bloquante post-login (`TermsAcceptGate`, claim JWT `terms_accepted` ; comptes existants forcés au prochain login ; « J'accepte » → `/api/auth/terms/accept`, « Refuser » → logout)**, muted words, @mentions, image/video upload (lightbox, Twitter-style autoplay), **OAuth Google/GitHub/Facebook/Spotify (boutons login/register + BFF + callback page ; nouveaux comptes finalisés publiquement sans JWT avant création)**, **onboarding gate legacy comptes OAuth sans profil** (modale bloquante username + date de naissance, signal profil absent), **vue visiteur (fil public sans inscription : `AuthPromptProvider`/`useAuthGate`, middleware ouvre `/feed`+`/posts/:id`, actions réservées → modale connexion)**, **restauration du scroll au retour depuis le détail d'un post (store mémoire module-level)**, **store utilisateur connecté `CurrentUserProvider`/`useCurrentUser` (un seul fetch profil partagé, `isAdmin`/`isModerator`, fin des refetch redondants — cf. DECISIONS)**. **TODO:** post edit ; reste à découper `chat-pane.tsx` (1207 l) en fichiers (reporté). |
## Features

| Feature | Type | Status |
|---|---|---|
| Registration / Login | Primary | 🟢 End-to-end (UI→BFF→gateway→auth), login by email or username, provisioning, username pre-check, logout + session guard. Inscription classique bloquée tant que les CGU ne sont pas lues puis acceptées (`acceptedTerms` vérifié par le BFF). |
| Login social (Google/GitHub/Facebook/Spotify) | Secondary | 🟢 End-to-end, **multi-provider** via registry à 2 familles : `kindOIDC` (Google — discovery + vérif `id_token`) et `kindOAuth2` (GitHub, Facebook, Spotify — code→access_token puis userinfo ; GitHub : repli `/user/emails` pour l'email primaire vérifié + `User-Agent` requis). BFF `GET/POST /api/auth/oauth/[provider]` + callback `/(auth)/auth/callback/[provider]` génériques. Nouveau compte OAuth : redirection vers page publique bloquante `/auth/oauth/terms` sans JWT, username + date de naissance + bouton CGU obligatoire, puis BFF `/complete` → credential + provisioning + cookie refresh. **Rangée d'icônes `grid-cols-4`** sur login + register (logos `public/{github,facebook,spotify}.svg`, spinner par provider, i18n `auth.oauth.continue_with`). Enum PG migré (`github/facebook/spotify` via `ADD VALUE IF NOT EXISTS`). Swagger régénéré. **⚠️ Credentials à renseigner** : seul Google configuré ; GitHub/Facebook/Spotify attendent leurs `CLIENT_ID/SECRET` dans `auth-service/.env` (apps à créer côté consoles, redirect `…/auth/callback/<provider>`) — provider non configuré = 404 inoffensif. *(LinkedIn remplacé par GitHub — exigeait une Page entreprise ; Microsoft retiré antérieurement, cf. CHANGELOG.)* |
| JWT auth + protected routes | Primary | 🟢 access 5m + refresh 24h + `/auth/validate`, front single-flight refresh + `(app)` guard. **TODO:** gateway JWT middleware. |
| Role management (User/Mod/Admin) | Primary | 🟡 Role in JWT, user-service enforces admin delete, role-based nav, **admin peut créer un compte de force** (`/admin` → mot de passe temporaire mailé + username/mot de passe provisoires corrigés via modales bloquantes). **TODO:** generalize to other services, real role front. |
| Post creation/reading | Primary | 🟢 End-to-end, infinite feed (For you / Following), hashtag filter, visibility-filtered, likes, **likes de commentaires**, threaded comments, reposts/quotes, pin, emoji, images/videos, **polls (choices, duration, everyone/followers audience, immutable unique vote, post-vote results, author live results, manual close + winner display)**, **reply audience (`reply_audience` everyone/followers, set at creation, server-enforced in `CreateComment`, mods/admins bypass, `can_reply` UX hint, composer pill)**, **realtime "a posté" banner (WS `/posts/ws`, X-style: floating avatar pill → reveals new posts + scrolls up)**. **TODO:** post edit. |
| User profile | Primary | 🟡 View + edit + by-username page, visibility configurable in settings, private-account locking, **blocage utilisateur wired** (menu profil + onglets masqués + déblocage), real avatar/banner upload avec retour aux visuels par défaut, initiale = première lettre Unicode réelle du nom puis username, nom validé front+back (lettres/chiffres/espaces/`-`/`_`), **activité en ligne/dernière connexion affichée si autorisée** (profil privé visible seulement par propriétaire/abonnés acceptés si activité publique). **Onglet Réponses wired** : `GET /posts/comments?author_id=` (optionalAuth, barrière de visibilité sur le post parent) + `ReplyCard` front (label « En réponse à @X » + clic → thread). **TODO:** wire aggregated user+profil+post read. |
| Social graph (follow/followers/block) | Secondary | 🟢 follow + lists + counts + private requests + follower removal + **auto-acceptation des demandes en attente au passage privé→public** (profil-service → user-service `/internal/.../accept-all-follow-requests`, best-effort) + **blocage/déblocage utilisateur** (coupe follows/follow_requests, masque l'activité côté post-service), front wired, e2e à valider. |
| Search / Explorer (accounts + hashtags) | Secondary | 🟢 Explorer discovery page with Top 10 trends, non-followed profile suggestions, regular feed publications, and submitted search filters (Publications/Utilisateurs, at least one required); search by `@username`, plain username, or display_name; direct `#hashtag` navigation; hashtag/right-column/Explorer search suggest trends then profiles; recent clicked searches can be cleared globally or one by one. **TODO:** post full-text search. |
| Automatic post translation | Secondary | 🟢 BFF `/api/translate`, auto-détection fournisseur pour toute phrase significative, cible = langue du compte, veto local si déjà clairement dans la cible, posts + commentaires, cache. Matrice 12×12 couverte. |
| Account language preference | Secondary | 🟢 12 langues (`fr/en/zh/es/pt/ru/ja/ko/ar/hi/de/it`), `users.preferred_locale` nullable, persistance inter-déconnexion propre au compte, navigateur si aucune préférence. |
| Muted words in feed | Secondary | 🟢 `/parametres`, per-account local persistence, feed masks others' matching posts. |
| Private encrypted messaging (E2EE) | Secondary | 🟢 End-to-end (back + UX). DM/groups admin-proof, hybrid communities, server-side read state + app-wide badge, per-conversation mute, encrypted attachments, owner-only edit with original shown subdued, **passphrase key backup (zero-knowledge, multi-device)**, **« Envoyé/Ouvert » read/delivery receipts (1 coche grise = envoyé, 2 coches couleur = ouvert ; groupe : nombre de lecteurs si partiel ; coches cliquables → libellé)**, **suppression de message pour tous (tombstone « Message supprimé », auteur + owner/admin de groupe)**, **indicateur « en train d'écrire »**, identité de l'interlocuteur cliquable dans l'en-tête DM avec retour profil→conversation. Branche #215 complète. |
| Real-time notifications | Secondary | 🟢 End-to-end (e2e live), Instagram aggregation, + `follow` + `message` (nouveau message privé, expéditeurs uniques) + `message_mention` + `follow_request` + `follow_request_accepted` + `follow_request_accept_confirm`, WS, badge, detail page. |
| Mentions (@handle) | Secondary | 🟢 End-to-end: posts/comments autocomplete + clickable render; messages (E2EE) member-ids only → `message_mention`. i18n FR/EN. |
| Hashtags & trends | Secondary | 🟢 End-to-end: hashtags extracted server-side on posts, composer autocomplete + blue highlight, clickable render, `?hashtag=` results view (`À la une`/`Récent`/`Média`), and dynamic Top 5 trends from visible posts. |
| Bookmarks (collections) | Secondary | 🟢 End-to-end, collections (many-to-many) + non-deletable default, burst model, `/signets` page. |
| Image / video upload | Secondary | 🟢 Complete (Phases 0→3): media-service + MinIO + gateway `/media`; avatar/banner; post media; encrypted message attachments. |
| GIFs (GIPHY) | Secondary | 🟢 End-to-end posts: gateway `/gifs/search` proxifié par media-service (clé `GIPHY_API_KEY` côté serveur), picker GIF dans le composer entre photo et épingle, grille tendances/recherche GIPHY. **Le GIF choisi est capturé en MinIO** (`POST /gifs/capture`, anti-SSRF allowlist giphy) et stocké `/media/<id>` — plus de hotlink externe (corrige 403/404 prod + compatible CSP `self`). ⚠️ Ops : poser `GIPHY_API_KEY` en prod + purger entrées mortes de `fallbackGifCatalog`. Résiduel : vignettes picker encore hotlinkées (UI éphémère). |
| Vérification e-mail | Secondary | 🟡 Code complet (Phase 1) — 🟢 après e2e `make dev`. Register → mail de vérif + page « consulte ta boîte mail » (aucune session) ; blocage dur login non-vérifié (`403`, bannière + renvoi anti-énum) ; page `/verify-email` (succès → **auto-login + accès direct au feed** / invalide-expiré) ; tokens opaques 24h usage unique. Mails de marque (clear mode). |
| Double authentification (MFA TOTP) | Secondary | 🟡 Code complet (back+front) — 🟢 après e2e `make dev` + scan Authenticator. Opt-in dans /parametres (interrupteur on/off style iOS au-dessus du mot de passe : on → QR + clé manuelle + code ; off → confirmation code/mdp), écran de challenge au login. TOTP seul (pas de codes de secours). Secret chiffré AES-256-GCM. |
| Mot de passe oublié | Secondary | 🟡 Code complet (Phase 2) — 🟢 après e2e `make dev`. Page `/forgot-password` (anti-énum, écran générique) → mail lien reset 1h ; page `/reset-password` (lit `?token`, nouveau mdp + confirmation) → bcrypt + `email_verified=true` + révocation de toutes les sessions ; rejouer le lien → 400 invalid_token (usage unique). Lien depuis le login. |
| Partage de post / profil | Secondary | 🟢 Front-only. Boutons Partager : carte du fil (`post-card.tsx`) **et** vue photo (`post-actions.tsx`), + bouton sur la vue profil (`profil-header.tsx`, à gauche du message) → `ShareDialog` réutilisable. 3 voies : **message privé** (recherche → `startDM`+`sendMessage`, lien en clair dans le message chiffré E2EE, format inchangé), **partage natif** (`navigator.share()`), **copier le lien**. **Aperçu enrichi dans le chat** : `shared-link-preview.tsx` + `extractSharedRef` reconstruisent une carte cliquable (post : avatar/nom/extrait/miniature ; profil : photo+nom) via `getPostById`/`getUserByUsername` (cache module-level) ; URLs linkifiées dans `MentionMessageText`. **Destinataires récents** persistés (`localStorage`) → remontés en tête de la recherche et listés champ vide (`UserSearch` props `recentUsers`/`recentLabel`). Partage de profil = lien seul (sans préfixe). Helpers purs `lib/share.ts`. i18n FR/EN `share.*`. Aucun backend, pas de swagger. |
| Vue visiteur (fil public) | Secondary | 🟢 Front-only, zéro backend (lecture publique déjà ouverte côté post-service). Non-connecté : fil « Pour toi » + détail post en lecture seule, hashtags cliquables (feature autre branche) ; pas de recherche/profils/messages/notifications/signets. `AuthPromptProvider`/`useAuthGate` (`isVisitor`/`requireAuth`/`promptLogin`), middleware ouvre `/feed`+`/posts/:id`, `/`→`/feed`, sidebar/tabbar/header adaptés (boutons Se connecter/S'inscrire), actions réservées → modale connexion. Appels `/me` gardés derrière `getAccessToken()` (anti-redirection forcée). |
| Moderation | Secondary | 🟢 Système de signalement complet : `report-service` (tickets agrégés + warns), `ReportDialog` réutilisable (posts/profils/messages, motif déroulant dont « Bug technique », texte 255/500, pièce jointe ≤ 5 Mb), onglet **Signalements** (après Comptes) avec filtres cumulables (statut/volume/plage), `TicketDetail` (en-tête agrégé, **encart « profil de risque »** de la cible — nb d'avertissements + **Bannir**, fil enfant, journal d'actions, réponse interne, cycle de vie open/closed/reopened, retrait soft-delete **gated derrière la validation de l'avertissement (atomique : supprime → avertit → clôture auto ; Annuler = aucun effet)**, **Valider (conforme)** → statut terminal `approved` (démasque + verrouille), masqué après sanction, **garde-fou de clôture** « non fondé ? » si aucune action, **Warn** asynchrone), filtres Comptes (Bannis/Mods/Admins). **Corbeille « Tweets supprimés » filtrable** (recherche utilisateur → `author_id`, plage de date de retrait `since`/`until`, « Charger plus », filtres serveur). **Auto-masquage au seuil** (réglable Admin › Paramètres). Réutilise soft-delete + ban existants. |
| Certifications | Secondary | 🟢 `profil-service` stocke `certification=none|political|public_figure` (migration boot + enum Mongo) ; `PATCH /profils/:id/certification` est réservé mod/admin et documenté Swagger. Après changement, `identity_updated` est émis vers notification-service et broadcast en WS : les badges changent instantanément sans refresh. Dans Modération › Comptes, menu `...` → “Attribuer une certification” sous “Changer le rôle”, avec Politique / Personnalité publique / Retirer. Affichage à droite du display name partout où l'identité est rendue : profil + hover-card + modération, posts/citations/commentaires, recherche/Explorer/suggestions, messages/mentions/notifications/partages/navigation ; rosette verified façon X jaune politique, bleue personnalité publique, logo Breezy pour staff admin/modo résolu via `GET /auth/users/roles`, tooltip hover/clic. |
| Admin panel | Secondary | 🟢 `AdminView` à onglets : **Signalements (bugs)** (réception + **transfert** bug→modération) + **Paramètres** (seuil d'auto-masquage des posts signalés) + **Infrastructure** (monitoring). Warn asynchrone intercepté par `WarningsGate` (modale bloquante au focus/connexion). |
| Internationalization (12 langues) | Secondary | 🟢 Home-grown, all UI translated, localized dates, préférence persistée par compte. |
| Contenu sensible (NSFW) + majorité | Secondary | 🟡 Code complet (back+front, 12 langues) — 🟢 après e2e `make dev`. Majorité calculée serveur depuis `birth_date` (`is_adult` ≥18 dynamique, `nsfw_visible`), jamais stockée ; préférence `nsfw_enabled` (défaut ON) togglable dans /parametres (carte grisée+verrouillée si mineur, se débloque à 18 ans) ; backfill `birth_date`→`1999-01-01` au boot (prod préservée). Posts : bouton « NSFW » du composer (auteur, à la création) + `PATCH /posts/:id/nsfw` (modo/admin) → flou plein-post côté front (politique viewer serveur, façon X) avec message « rendez-vous dans les paramètres ». Register inchangé (13 ans), 18 ans = NSFW seulement. Édit profil : date de naissance grisée si posée. cf. DECISIONS. |

## Infrastructure

- [x] `docker-compose.yml` with all services
- [x] Persistent volumes for DBs
- [x] `.env` for secrets (gitignored, never commit)
- [x] README with setup
- [x] CI/CD: 3 GitHub Actions workflows (`ci-go` build+test -race + golangci-lint + govulncheck;
      `ci-frontend` lint+build; `ci-integration` docker stack + healthchecks). govulncheck = 0 vuln.
- [x] **API documentation:** Swagger/OpenAPI spec — `make swagger` génère `doc/openapi.{json,yaml}` (58 routes, 7 services annotés swaggo/swag code-first). Spec agrégé commité. Per-service `docs/` gitignorés.
- [x] **Logging structuré slog** : tous les services (api-gateway + 7 services métier) ont `internal/logging` + `RequestID`/`RequestLogger`/`Recovery`, JSON release / texte debug, `LOG_LEVEL`, attribut `service`, `user_id` corrélé par le JWT middleware.
- [x] **CI swagger job** (`ci-go.yml` job `swagger`) : drift check (`git diff --exit-code doc/`) + validation Swagger 2.0 (`go-swagger validate`). Bloquant sur PR.
- [x] **API docs GitHub Pages** (`pages.yml`) : Redoc UI déployée sur `https://gosyfrone.github.io/WebDad/` à chaque push develop. Source = `doc/` (openapi.json + index.html). Local : `make swagger-site` (port 8088).
- [ ] **Perspective:** gitleaks, Dependabot, CD (push images to GHCR).

## Open issues / TODO (active)

- [x] **Gateway JWT middleware (19/06/2026)** — `PropagateJWT` global, politique « valider-si-présent » :
  rejet 401 au plus tôt d'un token invalide/expiré, strip anti-spoof des en-têtes d'identité, propagation
  `X-User-Id`/`X-User-Role`/`X-Email-Verified` aux services (défense en profondeur : services revalident le JWT).
  *Perspective :* faire consommer ces en-têtes par les services (remplacer leur revalidation) si on veut alléger.
- **Real role / username (frontend)** — `(app)` layout still uses `PLACEHOLDER_ROLE = 'administrator'`;
  derive real `role`/`username` (via `/users/me` or JWT decode).
- **Identity cooldowns** — architecture posed; activate by setting `DISPLAY_NAME_CHANGE_COOLDOWN` /
  `USERNAME_CHANGE_COOLDOWN` (e.g. `168h`), no migration.
- **user-service coverage** — 🟢 **~95 %** (filtré 95,1 %, brut 89,2 %, flag `user-service`). Objectif 95 %
  atteint **sans changement de prod** : tests d'intégration sur **vrai PostgreSQL** via `internal/testutil`
  (`USER_TEST_DSN`, base jetable `CREATE DATABASE` par test, `t.Skip` si absent) couvrant repository/service/
  handlers (routeur complet) ; branches d'erreur DB exercées via un **pool `*sql.DB` fermé** ; branches
  « claims absents » par appels directs ; unitaires `config`/`db`/`middleware`/`main`. CI `ci-go.yml` :
  service `postgres:16` scopé par la matrice + filtrage du profil avant Codecov (`docs/`/`main.go`/
  `testutil/`). Par paquet : handlers/router/logging 100 %, middleware 97,3 %, client 95,1 %, service 92,9 %,
  config 92,6 %, repository 91,1 %, db 80 %. Résiduel : `return …err` post-vérification (panne mi-transaction),
  `config.Load` `log.Fatal`, `db.Connect` retry 20 s. **Pattern Postgres réutilisable pour auth-service.**
- **report-service coverage** — 🟢 **~98 %** (codecov-équivalent 585/597 = 97.99 %, flag `report-service`).
  Objectif 95 % atteint **sans changement de prod** : les branches d'erreur du driver Mongo sont exercées
  en passant un **`context` déjà annulé** (→ `context.Canceled`, pas besoin d'injecter une panne via une
  interface de dépôt). 5 fichiers `*_errorpaths_test.go` (repository 97.2 %, service 98.2 %, handler 99.3 %,
  database 92.7 %, client 100 %). Résiduel ~12 stmts incouvrables (config `log.Fatal`/branche `/.dockerenv`,
  `jwt` `!token.Valid` défensif, `init` erreurs `collMod`/`CreateCollection`).
- **Private follow/privacy review points:** (1) user-service `/internal/...is-following...` doesn't check
  `X-Internal-Secret` (graph leak if port reachable); (2) profile `followOverride=false` after a pending
  request can hide later real-time accept until remount; (3) `NotificationsView` accept/reject relies on WS
  to clean the item; (4) `post-service.visiblePage` may scan many filtered private posts (perf/DoS watch).

## Known assumed compromises

- **Access token in localStorage** (XSS-exposed, mitigated by 5m lifetime; refresh stays httpOnly).
- **CORS:** data calls go browser→gateway directly (`Authorization: Bearer`); verify `CORS_ALLOWED_ORIGINS`.
- **Followers/following counters calculated (COUNT)** per read — fine at project scale; denormalize if load requires.
- **Messaging:** communities are admin-readable (server key). The identity key can now follow the user across devices via
  the **passphrase backup** (zero-knowledge); a forgotten passphrase = unrecoverable backup, and a device with neither a
  local key nor a backup still generates a fresh key (prior history unreadable there). Perspectives: key rotation, ownership
  transfer, **admin passphrase reset** (deferred — "wipe backup → new identity" semantics, no escrow to keep DM admin-proof).
- **Notifications:** emission best-effort (lost if notif-service down at T); in-memory badge may over-count by 1
  (self-corrects); badge increments even while `/notifications` is open; `last_actor` cosmetic blur after retract.

## Setup reminders (fresh checkout)

- `make env` (or copy each `.env.example`) before `make dev` — several services need their `.env` to exist,
  notably `message-service/.env`, and `INTERNAL_EVENT_SECRET` + `MAIL_INTERNAL_SECRET` must be in the **root**
  `.env` (compose interpolates `${...}` from the root file only).
- `minio` and `media-service` share the same `media-service/.env` (`MINIO_ROOT_USER`/`PASSWORD`). MinIO console: `http://localhost:9001`.
- For code changes to appear, use `make dev` (hot-reload) — `make up` runs frozen prod images.
